root = true

[*]
end_of_line = lf
charset = utf-8
trim_trailing_whitespace = true
insert_final_newline = true
indent_style = space
indent_size = 2
# Note: EditorConfig cannot enforce max consecutive blank lines.
# This is enforced via oxlint's no-multiple-empty-lines rule.

trim_trailing_whitespace = false
