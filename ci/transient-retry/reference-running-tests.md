# Running tests

[Back to Transient-Retry Rule Catalogue](README.md#running-tests)

```sh
pnpm exec vitest run --project ci-tools
```

To run only the transient-retry tests:

```sh
pnpm exec vitest run --project ci-tools ci/transient-retry/
```
