# CI Tooling

CI-local reproduction and workflow support scripts. Run commands from the worktree root.

| Path                                                                                                                                                                                                   | Purpose                                                                                                                                                                                                                                                                                                                                        |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [`ci-local.mts`](ci-local.mts)                                                                                                                                                                         | Local runner for CI-equivalent command targets, exposed through [`../dev/ci-local`](../dev/ci-local).                                                                                                                                                                                                                                          |
| [`ci-local/`](ci-local/)                                                                                                                                                                               | Target registry and shared types for `ci-local.mts`.                                                                                                                                                                                                                                                                                           |
| [`ci-job-runtime-audit.mts`](ci-job-runtime-audit.mts)                                                                                                                                                 | Thin wrapper over `vouchington-tooling/gha-runtime-audit` that pins filaments `CI` pull requests and `Main CI (*)` pushes, then emits the JSON audit.                                                                                                                                                                                          |
| [`revalidate-dependabot-rerun.mts`](revalidate-dependabot-rerun.mts)                                                                                                                                   | Re-fetches the exact failed Dependabot source run and its open PR immediately before a targeted or full Actions rerun. Superseded runs, closed PRs, and moved Dependabot heads are no-op notices; malformed or mismatched identity fails before mutation.                                                                                      |
| [`check-needs-results.sh`](check-needs-results.sh)                                                                                                                                                     | Wrapper over the published `vouchington-tooling` fan-in gate for required job result JSON.                                                                                                                                                                                                                                                     |
| [`shepherd-checkpoint.mts`](shepherd-checkpoint.mts), [`shepherd-checkpoint-resume.mts`](shepherd-checkpoint-resume.mts), [`shepherd-checkpoint-cli.mts`](shepherd-checkpoint-cli.mts)                 | Render/parse/verify the durable `shepherd-checkpoint:v1` PR comment (core), select a resumable session checkpoint (`select`), and provide the CLI entry point workflows shell out to.                                                                                                                                                          |
| [`allocate-browser-safe-ports.py`](allocate-browser-safe-ports.py)                                                                                                                                     | Wrapper that execs the published allocator with this repo's runner policy and packaged Fetch-forbidden port catalog, then reaps leftover `/tmp/voucha-port-hold-*` identity dirs. `pnpm dlx`es the package when leftover `node_modules` has no allocator script.                                                                               |
| [`runner-port-policy.json`](runner-port-policy.json), [`runner-port-policy.mts`](runner-port-policy.mts)                                                                                               | Canonical repository reservation `2200–2999`, divided into 50 deterministic 16-port CI runner slices. The TypeScript loader is a thin wrapper over `vouchington-tooling/runner-port-policy` that keeps this JSON as the in-repo source of truth.                                                                                               |
| [`coverage-artifacts.sh`](coverage-artifacts.sh)                                                                                                                                                       | Merge current PR LCOV, report/enforce patch coverage, and render local HTML.                                                                                                                                                                                                                                                                   |
| [`coverage-check-gate.mts`](coverage-check-gate.mts)                                                                                                                                                   | Shared adapter for merging signed coverage pairs and running the patch-coverage gate.                                                                                                                                                                                                                                                          |
| [`coverage-local-affected.mts`](coverage-local-affected.mts), [`coverage-local-affected-output.mts`](coverage-local-affected-output.mts), [`coverage-local-full.mts`](coverage-local-full.mts)         | Select and run affected or complete registered local coverage suites and render their results.                                                                                                                                                                                                                                                 |
| [`coverage-local-provenance.mts`](coverage-local-provenance.mts), [`coverage-local-suite-catalog.mts`](coverage-local-suite-catalog.mts), [`coverage-local-utils.mts`](coverage-local-utils.mts)       | Run one signed local suite, own the local suite catalog, and share local command utilities.                                                                                                                                                                                                                                                    |
| [`download-with-diagnostics.sh`](download-with-diagnostics.sh)                                                                                                                                         | Wrapper over the published download helper. Falls back to `curl-to.sh` when the installed package has no `scripts/gha` helper.                                                                                                                                                                                                                 |
| [`curl-to.sh`](curl-to.sh)                                                                                                                                                                             | Pre-node bootstrap curl used by gitleaks, lychee, and `activate-pnpm` before `node_modules` exists.                                                                                                                                                                                                                                            |
| [`host-pressure-diagnostics.sh`](host-pressure-diagnostics.sh)                                                                                                                                         | Wrapper over the published host memory/OOM/PSI snapshot (Linux: cgroup memory, PSI, OOM journal; Darwin: `memory_pressure`, decoded `vm_pressure_level`, compressor bytes, `iostat`-based load) and cross-platform `load1 per cpu` ratio; called by `--failure-diagnostics` in host-lock wrappers and directly by failure-only workflow steps. |
| [`host-pressure-snapshot.mts`](host-pressure-snapshot.mts)                                                                                                                                             | Shared, unconditional (not failure-only) wrapper over `host-pressure-diagnostics.sh` used by the locked web build (`setup-web-integration.mts`) so a healthy-but-slow build still records the pressure context needed to re-derive `VOUCHA_BUILD_LOCK_COMMAND_TIMEOUT_SECONDS` (issue #10937). Never throws.                                   |
| [`coverage-manifest.mts`](coverage-manifest.mts)                                                                                                                                                       | Filaments suite/env/git CLI adapter for `coverage-check`'s local provenance and sparse patch-contribution APIs.                                                                                                                                                                                                                                |
| [`coverage-suite-descriptors.mts`](coverage-suite-descriptors.mts), [`coverage-suites.mts`](coverage-suites.mts)                                                                                       | Define static CI collector descriptors and self-described producer partitions; fan-in does not reconstruct a suite/shard catalog. Shared types come from `coverage-check`.                                                                                                                                                                     |
| [`coverage-suite-env.mts`](coverage-suite-env.mts), [`coverage-suites-local.mts`](coverage-suites-local.mts), [`coverage-no-data-mock-source-patterns.mts`](coverage-no-data-mock-source-patterns.mts) | Isolate local suite environments, force `BASH_ENV=/dev/null` so child bash does not source a host startup file, map sources to local suites, and centralize no-data mock coverage ownership.                                                                                                                                                   |
| [`artifact-upload-outcome.mts`](artifact-upload-outcome.mts)                                                                                                                                           | Asserts the terminal first-attempt/retry GitHub upload outcome for the coverage pair, Vitest blob, and Vitest report attempt families; exits 0 iff either attempt succeeded and reproduces each family's exhaustion marker byte-for-byte.                                                                                                      |
| [`local-patch-coverage.mts`](local-patch-coverage.mts)                                                                                                                                                 | Validate local signed pairs and preview patch coverage.                                                                                                                                                                                                                                                                                        |
| [`prepare-coverage-artifacts.mts`](prepare-coverage-artifacts.mts)                                                                                                                                     | Map discovered sparse coverage contributions into `coverage-check`. Vitest report preparation and rerun-safe producer markers use the published `vouchington prepare-vitest-reports` and `vouchington vitest-report-attempt` commands.                                                                                                         |
| [`prepare-trivy-db.sh`](prepare-trivy-db.sh)                                                                                                                                                           | Wrapper over the published `vouchington prepare-trivy-db` helper. Uses the installed package, then `pnpm dlx` when image-build jobs have no `node_modules`.                                                                                                                                                                                    |
| [`pnpm-install.sh`](pnpm-install.sh)                                                                                                                                                                   | Workspace-install entrypoint. It always `pnpm dlx`es the `package.json` spec so a stale `node_modules` cannot bootstrap from an older package. It retains Filaments' release-age remediation URL and fails fast on `ERR_PNPM_MINIMUM_RELEASE_AGE_VIOLATION`.                                                                                   |
| [`lint-links.sh`](lint-links.sh)                                                                                                                                                                       | Run `lychee` against tracked Markdown.                                                                                                                                                                                                                                                                                                         |
| [`merge-vitest-reports.sh`](merge-vitest-reports.sh)                                                                                                                                                   | Prepare validated canonical Vitest reports, then invoke Vitest's merge reporter only when expected reports exist.                                                                                                                                                                                                                              |
| [`vitest-diagnostic-report-summary.mts`](vitest-diagnostic-report-summary.mts)                                                                                                                         | Whitelisted summary of Node `--report-on-fatalerror`/`--report-uncaught-exception` diagnostic reports for failure-only backend-unit fork-crash steps (issue #8940).                                                                                                                                                                            |
| [`playwright/`](playwright/)                                                                                                                                                                           | Select PR Playwright specs from a streamed no-mistakes revision plan and fail open to the full suite.                                                                                                                                                                                                                                          |
| [`playwright-install-ubicloud-browsers.sh`](playwright-install-ubicloud-browsers.sh)                                                                                                                   | Install Chromium browser archives on Ubicloud ARM runners using system unzip.                                                                                                                                                                                                                                                                  |
| [`run-pnpm-command.mts`](run-pnpm-command.mts)                                                                                                                                                         | Spawn a `pnpm` (or `corepack pnpm`) subprocess with the working directory, env, and live per-line stderr forwarding shared by `setup-web-integration.mts`.                                                                                                                                                                                     |
| [`setup-web-integration.mts`](setup-web-integration.mts)                                                                                                                                               | One-shot setup for [`../integration-tests/web/`](../integration-tests/web/) Vitest integration runs.                                                                                                                                                                                                                                           |
| [`write-web-build-timings-summary.mts`](write-web-build-timings-summary.mts)                                                                                                                           | Renders `build-web-targets`'s incremental timing JSON into `$GITHUB_STEP_SUMMARY`, printing the host pressure snapshot's raw text un-escaped instead of JSON-embedded (issue #10937).                                                                                                                                                          |
| [`test-plan.mts`](test-plan.mts)                                                                                                                                                                       | Normalize no-mistakes plans and runnable targets for CI selectors.                                                                                                                                                                                                                                                                             |
| [`no-mistakes-config.mts`](no-mistakes-config.mts)                                                                                                                                                     | Typed access to no-mistakes policy data consumed by CI contracts.                                                                                                                                                                                                                                                                              |
| [`no-mistakes-workflows/`](no-mistakes-workflows/)                                                                                                                                                     | Non-executable workflow projections that let repository analysis combine top-level trigger semantics with filters owned by reusable workflows; these files live outside `.github/workflows` and never run in GitHub Actions.                                                                                                                   |
| [`tooling-test-runner.mts`](tooling-test-runner.mts)                                                                                                                                                   | Runs the centrally registered tooling projects once while project setup routes worktree resources.                                                                                                                                                                                                                                             |
| [`repo-topology.mts`](repo-topology.mts)                                                                                                                                                               | Shared memoized `loadRepoTopology()` for CI scripts. Vitest tests must not call it, spawn the CLI, or call `testsPlan()`. Pin: `ci/no-mistakes-ci-contention.test.mts`.                                                                                                                                                                        |
| [`check-live-workflow-topology.mts`](check-live-workflow-topology.mts)                                                                                                                                 | Sequential live topology audits (policy, JOBS.md freshness, secrets, caller/callee permissions) after `no-mistakes check` in static-code-analysis.                                                                                                                                                                                             |
| [`harness-session-dispatch.mts`](harness-session-dispatch.mts)                                                                                                                                         | Create/resume Auto Harness sessions. Session drain is the official Auto Harness dispatch action.                                                                                                                                                                                                                                               |
| [`harness-session-dispatch-metadata.mts`](harness-session-dispatch-metadata.mts)                                                                                                                       | Builds the `HARNESS_METADATA` JSON payload for a dispatch step from its env vars, run by `harness-dispatch.yml` ahead of `harness-session-dispatch.mts`.                                                                                                                                                                                       |
| [`with-node-test-options`](with-node-test-options)                                                                                                                                                     | Shared wrapper for Node/Vitest test process options.                                                                                                                                                                                                                                                                                           |
| [`vitest/`](vitest/)                                                                                                                                                                                   | Route one no-mistakes PR plan to Vitest jobs and resolve workflow/action changes through CI topology.                                                                                                                                                                                                                                          |
| [`write-github-multiline-output.sh`](write-github-multiline-output.sh)                                                                                                                                 | Wrapper over the published collision-safe `$GITHUB_OUTPUT` writer.                                                                                                                                                                                                                                                                             |

| [`cleanup-artifacts-patterns.json`](cleanup-artifacts-patterns.json), [`cleanup-artifacts-pattern-matcher.mjs`](cleanup-artifacts-pattern-matcher.mjs), [`cleanup-run-artifacts.mjs`](cleanup-run-artifacts.mjs) | Canonical Actions artifact keep/delete policy, its dependency-free exact-or-prefix matcher, and the minimal same-run fan-in cleanup used without installing workspace dependencies. |

CI scripts belong here when they reproduce or prepare GitHub Actions behavior.

[`download-optional-run-artifacts.sh`](download-optional-run-artifacts.sh) is a thin wrapper over
the published `vouchington download-optional-run-artifacts` GHA helper. The fan-in-only same-run
GitHub artifact probe downloads one exact name or pattern, writes a typed `availability` output,
preserves downloader diagnostics, and turns expected absence into one bounded warning so terminal
validation decides whether evidence is complete.

## Historical coverage baseline comparison

The released [`coverage-check`](https://www.npmjs.com/package/coverage-check) package owns
historical Istanbul summary comparison. It protects test-reduction PRs from historical coverage
regressions and is separate from the changed-line gate: follow
[Test Value and Safe Reduction](../docs/development/reference-tests-value-and-reduction.md) for the
complete reduction invariant and [Local Patch Coverage Preview](../docs/development/reference-tests-local-patch-coverage-preview.md)
for patch coverage.

Generate both `coverage-summary.json` artifacts with the same coverage producer, suite selection,
coverage include/exclude configuration, Node/toolchain versions, and environment. Run the base
checkout at the exact merge-base SHA and the head checkout at the candidate SHA, preferably in
separate worktrees. Do not compare artifacts produced after changing those inputs: a passing result
only proves parity for identical collection conditions.

```bash
pnpm --dir ci exec coverage-check compare-summary \
  --base-summary /abs/base/coverage/coverage-summary.json \
  --head-summary /abs/head/coverage/coverage-summary.json \
  --base-root /abs/base \
  --head-root /abs/head \
  --json /abs/head/coverage/baseline-comparison.json
```

The package owns argument validation, source normalization, comparison semantics, structured
output, and exit codes. Use `pnpm --dir ci exec coverage-check compare-summary --help` for its current CLI
contract. Filaments owns only the reproducible base/head artifact conditions documented above.

`activate-pnpm.sh` is the single Node/pnpm activation path shared by the setup composites. It keeps
the verified manual Node fallback and isolated pnpm bootstrap out of workflow YAML so callers use
the same portable setup contract. `setup-backend-install.sh` owns setup-backend's persistent-runner
lifecycle check and delegates the full installation to `pnpm-install.sh`.

Workspace packages import the shared listener binder through
`@voucha/ci/runner-port-policy` so deploy restoration preserves the package boundary. That export
loads this repository's JSON via `vouchington-tooling/runner-port-policy`.

Gitleaks CI intentionally installs its checksum-verified binary in an isolated per-job temporary
directory; local `gitleaks` comes from the matching mise `aqua:` pin. The two versions are guarded
without changing this workflow isolation. Both GitHub Release downloads retry transport errors and
retryable HTTP responses up to three times inside the job before checksum verification. Each
attempt is capped at 15 seconds, and retries may start only inside a 35-second window, bounding each
download at 50 seconds and both sequential downloads at 100 seconds inside the two-minute install
step. The main-only transient classifier remains a fallback after those bounded retries are
exhausted.

`with-host-lock.sh` is a `VOUCHA_HOST_LOCK_*` → `HOST_LOCK_*` bootstrap shim so
host-package-manager locking works before `node_modules` or Node exist. It execs the
packaged `vouchington with-host-lock` script, `pnpm dlx`, or a curl+tar fetch of the
published tarball. `with-build-lock.sh` selects its shared `expensive-build` family. `with-heavy-slot.sh` owns the
single oxlint-only `memory-heavy` slot. GitHub Actions normally waits at most 60 seconds for either
scheduling lock before running unlocked. Next builds instead wait up to 300 seconds and fail closed
so compiler-heavy work never overlaps after admission times out; local acquisition remains fail-closed. Nested host locks are rejected,
so use canonical workspace entrypoints with one lock owner per command path. Failed covered
commands emit host-pressure diagnostics. The complete coverage and recovery contract is in
[Per-User Host Locks](../docs/development/host-locks.md).

## Contents

- <a id="github-multiline-output-helper-contract"></a>[GitHub multiline output helper contract](reference-github-multiline-output-helper-contract.md)
- <a id="download-helper-contract"></a>[Download helper contract](reference-download-helper-contract.md)
- <a id="trivy-database-preparation-contract"></a>[Trivy database preparation contract](reference-trivy-database-preparation-contract.md)
- <a id="inline-workflow-shell-footguns-actionlint--shellcheck"></a>[Inline-workflow shell footguns (actionlint + shellcheck)](reference-inline-workflow-shell-footguns-actionlint-shellcheck.md)
