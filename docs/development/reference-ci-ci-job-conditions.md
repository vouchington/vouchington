# CI Job Conditions

[Back to CI Reference](ci.md#ci-job-conditions)

The PR CI orchestrator starts on every PR (any base branch, including stacked PRs) and manual
dispatch, then lets `detect-changes` decide
which reusable workflows should run. On PRs, `select-ci` additionally emits fixed `full-ci` and
`run-<root-job>` topology outputs from pair-2 revision analysis (`origin/<base_ref>` vs merge
`HEAD`; see [Workflow Topology Contracts](reference-ci-workflow-topology-contracts.md)). A bounded result
adds only its affected producer to that producer's existing path predicate; a missing output or
global fallback runs every otherwise eligible producer. Vitest's separate `full-suite` and
`full-test-<job>` contracts remain responsible only for test execution narrowing. Workflow and
local-action edits use the topology report on PRs; the dedicated `workflow-action-changes` bit is
only a non-PR/manual-dispatch fail-open fallback.
Path-scoped `main-*` workflows own main tests. A trusted completed-run receiver dispatches successful
source metadata to private infrastructure, which owns artifact builds and publication. Static Code Analysis runs
directly on every main push. Keep PR gating at job level instead of
top-level `paths-ignore`: the fan-in jobs need to report skipped checks consistently, and
workflow-owned docs such as `.github/workflows/README.md`, `.github/workflows/RUNNERS.md`, `.github/workflows/WORKFLOWS.md`, and `.github/workflows/AUTHORING.md` are test fixtures
that must still run the tooling suite. `detect-changes` `shell-scripts` is initialize-smoke input
(`dev/initialize*`, `dev/lib/**`, `dev/host-storage-preflight.mts`, `ci/runner-port-policy.*`,
and `**/*.sh`), not every file under `ci/` or `dev/`. PR `test-portability` uses a dedicated
`portability` filter. On `main`, `main-checks.yml` still starts from its union path list, then
`select-main-checks` starts only the matching suite.

| Job / gate                                        | Pull requests                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `main` pushes                                                                                                                                                                                                                                                          | Manual dispatch                                                                    |
| ------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- |
| `static-code-analysis`                            | Runs for every change through the PR orchestrator. Docs-only PRs pass `docs_only=true` so the reusable workflow keeps checkout, `setup-node-pnpm`, mise, full `no-mistakes check`, `oxfmt --check`, and `repo-file-policy` plus `targeted-guardrails` plus `config-inventory-policy`; TypeScript, oxlint, knip, and graph checks stay off. Direct `main` push stays full.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Runs directly for every push.                                                                                                                                                                                                                                          | Runs directly on dispatch.                                                         |
| `test-web` / `test-web-api` / `test-backend-unit` | Runs when its refined area paths match. An explicit empty Vitest selection emits a per-job `skip-…=true`; smoke is no longer a shard-1 duty. Web unit runs before the API and full-stack integration sibling consumers.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Runs with the path-scoped Main workflow.                                                                                                                                                                                                                               | Same path filter as pushes.                                                        |
| `backend-smoke`                                   | Runs once the backend static gate succeeds or is skipped, gated like the other per-area jobs by topology selection: skipped on docs-only PRs unless full-CI/full-suite selection or `select-ci`'s own `run-backend-smoke` output applies, or backend paths changed directly. There is no draft check; the removed `vitest:full` label override is gone and no other label/selector gates this job. It remains parallel with backend test families after that gate. The required `tests` fan-in directly checks its result; `test-coverage` does not depend on it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Runs after backend static and in parallel with backend test families, gated by the same docs-only/path check; a failed workflow is not dispatched by the completed-run receiver.                                                                                       | Runs for full CI dispatch; the reusable workflow also supports direct dispatch.    |
| `storybook`                                       | Trusted PRs plus exact same-repository Dependabot/Renovate PRs; runs regardless of draft state. Runs for Storybook-owned files, `pnpm-lock.yaml` (real dep changes), Vitest/workflow inputs, and runtime web source. Bot runs are test-only with no presign capability, coverage publication, or static publish artifact. Root `package.json` version-only bumps are **excluded** — automated version bumps only touch the `version` field (not the lockfile), so they should not fire the expensive Storybook run. Skips docs/test-helper/test-only. The required `tests` fan-in directly checks its result.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Same path filter as PRs; successful `main-storybook` completion is dispatched by the trusted receiver.                                                                                                                                                                 | Same source-validation boundary as pushes; manual runs are not deployment sources. |
| `test-playwright`                                 | Runs on every triggering pull request, draft or ready, but only after topology selection: skipped on docs-only PRs (the removed `playwright:full` label no longer overrides that), then gated by full-CI/full-suite selection, `select-ci`'s own `run-test-playwright` output, or a direct Playwright path match (see [Playwright CI Selection](reference-ci-playwright-ci-selection.md) for the `select` job's own path/spec selection). Waits for application-stack Vitest jobs to settle, then runs even when one of those tests failed or skipped. Workflow cancellation and a hard area-static failure stop it; ordinary upstream test outcomes do not. Tooling, native, portability, and Storybook are not prerequisites.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Runs after web Vitest and web integration settle, gated by the same docs-only check and a direct Playwright path match; the two Playwright suites remain siblings. In trusted `main` context it emits Sentry plus optional OTel artifacts for `store-playwright-otel`. | Runs as an explicit full-suite escape hatch, subject to the same docs-only gate.   |
| `test-playwright-credentialed`                    | Trusted PRs + main; runs regardless of draft state. Runs for `playwright/credentialed/**` paths, when full CI/full-suite selection applies, or on `workflow_dispatch`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Same path filter as PRs when trusted.                                                                                                                                                                                                                                  | n/a                                                                                |
| `test-web-integration`                            | Runs regardless of draft state, after web Vitest settles, including after an ordinary web-test failure; it skips Markdown, Vitest-only, test-helper-only, and Storybook-only changes. It is matrix-capable but defaults to one shard; the required `tests` gate waits on this job.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Runs after the web static gate and web Vitest settle.                                                                                                                                                                                                                  | Same path filter as pushes.                                                        |
| Docker image builds                               | Runs regardless of draft state, once the `tests` gate succeeds or is skipped, only for same-repository PRs whose actor is neither Dependabot nor Renovate and whose corresponding positive-only `build-*-infra` filter matches. Shared inputs are `ci.yml`, the image workflow, the `clean-workspace` implementation, the `setup-aws` action manifest, Docker/Hadolint/Trivy config, and root lock/workspace files. Backend additionally matches its bake file, worker deployment policy, both deployed-dependency packaging helpers, and only manifests copied by `backend/Dockerfile`; web matches only manifests copied by `web/Dockerfile`. PR builds load images locally for validation. Backend uses one 10-minute Bake action for the selected target set: API plus worker-cpu normally, or all three images when worker-io automation is enabled. The targets share the job-local BuildKit solve. Web alone reads and writes GHA cache. [#10864](https://github.com/jonathanong/filaments/issues/10864) owns a durable repository-independent remote cache and performance design. PR builds publish no runtime images to ECR. Build jobs assume `secrets.AWS_TEST_ROLE_ARN` for test/smoke credentials, but ECR auth/push permissions are not available to this PR-assumable role. | No image build runs in Filaments. Path-scoped workflows test the source revision and dispatch it to the private infrastructure receiver, which builds and publishes runtime images.                                                                                    | Same source-validation and dispatch boundary as pushes.                            |
| `test-coverage` fan-in                            | Runs unconditionally, including on docs-only changes. Upstream suites may be skipped. The `tests-processing` job's pnpm setup/report-expectation/blob-download/merge steps also run unconditionally now (no docs-only bypass).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Always runs as a stable gate; upstream suites may be skipped.                                                                                                                                                                                                          | Always runs as a stable gate; upstream suites may be skipped.                      |
| `tests` fan-in                                    | Always runs as a stable branch-protection gate; merges Vitest reports when expected and fails if any required upstream job failed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Same gate behavior as PRs.                                                                                                                                                                                                                                             | Same gate behavior as pushes.                                                      |
| `build` fan-in                                    | Always runs as a stable branch-protection gate; leaf build jobs may be skipped.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Same gate behavior as PRs.                                                                                                                                                                                                                                             | Same gate behavior as pushes.                                                      |
| Artifact publication and dispatch                 | Runtime artifacts are not published from PRs.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | A trusted completed-run receiver dispatches source metadata after successful path-scoped workflows; private infrastructure owns artifact builds, publication, retention, and deployment.                                                                               | Manual CI dispatch does not publish or deploy runtime artifacts.                   |

The backend Dockerfile copies the root and required workspace manifests before one filtered
`pnpm install --ignore-scripts && pnpm rebuild --pending` layer. The web Dockerfile follows the same
single-layer pattern for the declared `web...` dependency closure. Do not add a separate
`pnpm fetch` pass to either image: BuildKit cache mounts are not restored by the exported layer cache
on ephemeral runners, and fetch also runs package lifecycle scripts before the filtered install
recreates `node_modules`, duplicating native downloads and snapshot work. Source files remain below
each dependency layer, so source-only changes still reuse it during PR validation. Runtime image
targets, publication, retention, and live worker placement are private-infrastructure configuration.
PR validation builds use one shared `max-parallelism = 3` BuildKit setting. Treat the 8-arm PR
runner as the limiting environment; if it produces a confirmed
BuildKit OOM/EOF, restore the single shared value to 2 rather than assigning branch-specific limits.
