# Classifying Transient Infrastructure Failures

## Historical native CI fingerprints

The native workflow names and commands below describe the pre-extraction Filaments CI topology.
Current native CI and reproductions are owned by
[`vouchington/vouchington-clients`](https://github.com/vouchington/vouchington-clients).

[Back to CI Reference](ci.md#classifying-transient-infrastructure-failures)

Before rerunning a failed CI job, confirm the failure matches a known infrastructure signature
**and** that the latest runs on the target branch are green. A look-alike failure that matches
your diff is a real bug — do not rerun it. On non-`main`, non-Dependabot PR branches, once you
have confirmed the run matches a known-transient fingerprint, use
`node ci/transient-retry/rerun-known-transient.mts <run-id>` instead of manually matching rules
and calling `gh run rerun`.

Automatic `main` recovery runs only while `HARNESS_DISPATCH_ENABLED` and
`HARNESS_FIX_MAIN_ENABLED` are both exactly `true`. When either is unset, `fix-main.yml` does not
classify failures, rerun workflows, request fresh plans, reap locks, or dispatch Harness. The manual
PR-branch helper above remains available independently.

Every rerun rule has its own `maxAttempts` budget. Prior attempts are replayed chronologically,
and only a rule that wins ordered evaluation with a rerun decision increments its counter, so
unrelated failures do not consume the budget even when they created jobs. `ignore` decisions retain
shared attempt accounting. If evidence requested by a later
matcher is unavailable, reconstruction retains the proven ordered-rule prefix and omits the
failing rule and suffix, which use the conservative shared attempt count that discounts only
prior zero-job attempts. If the first rule's evidence is unavailable, every rule uses that fallback.

Bare job-timeout kills on `CI` `static-code-analysis / no-mistakes`, the `select-ci`
selector, and the Playwright `select` selector (via either `CI` `test-playwright` or
`Main CI (web)` `playwright-tests`) have no auto-rerun rule. Hosted runners isolate separate
jobs, but a timeout within one job gives no evidence that distinguishes an infrastructure
stall from a real code-bug deadlock. The
[timeout forward guard](../../ci/transient-retry/no-mistakes-job-timeout-ceiling-inconclusive.test.mts)
derives each current timeout from its owning workflow and requires these ambiguous signatures to
dispatch for investigation instead of automatically rerunning.

Cancelled idempotent workflows, including standalone `Static Code Analysis` runs, with no failed-job
signal are ignored instead of dispatching Codex. Unclassified timeouts are never blind-rerun.
Each path-scoped `main-*` workflow tests and publishes its artifact unit independently, then
dispatches the matching private-infrastructure receiver. Vouchington does not migrate databases,
mutate providers, or deploy runtime artifacts. The receiver remains disabled during the cutover.
After explicit Harness activation, `fix-main` auto-reruns catalogued transient failures on
individual Vouchington workflows; until then this path remains disabled. Deployment recovery belongs
to the private infrastructure repository.

The standalone Gitleaks workflow handles GitHub Release transport errors and retryable HTTP
responses in the job on pull requests, pushes, and manual dispatches: both the archive and checksum
download use up to three curl retries before the checksum is verified. Each attempt is capped at 15
seconds, and retries may start only inside a 35-second window, so each download is bounded at 50
seconds and both sequential downloads fit within the two-minute install step. The
`gitleaks-install-releases-download-flake` classifier remains a `main`-run fallback only after those
in-job retries are exhausted; it does not provide pull-request recovery.

The `static-web`/`build-web-targets` shared `expensive-build` host-lock watchdog and
acquisition-timeout rules retired with the self-hosted runner fleet: GitHub-hosted runners are
ephemeral and single-job-per-VM, so those shared-host lock races can no longer occur. The
`runner-shutdown-leaf-rerun` rule remains live for a clean cancellation of an idempotent leaf job:
its shared `runnerShutdownLeafRerunMatch` predicate requires complete logs and rejects OOM,
application, setup, and non-SIGTERM failure evidence before rerunning up to twice. The same
predicate narrows the three coverage-artifact rules below (`coverage-transport-exhausted`,
`coverage-artifact-download-timeout`, `coverage-artifact-stale-rerun-missing`) so their sibling
handling uses the identical conservative contract. This is distinct from
`main-web-static-build-silent-exit`, which matches an unexplained non-143 exit code and remains a
live, independent rule.

Credentialed-provider classification keeps each provider's specific evidence boundary. AWS accepts
only a bare Vitest timeout or the known SDK abort/timeout markers. Bedrock additionally requires its
500/runtime-host markers. OpenAI 429s require both the provider message and `rate_limit_exceeded`;
the provider-side 500 case requires every failing OpenAI block to contain the 500 message and
`OpenAI.makeStatusError`. OpenRouter accepts only the structured-decision client's HTTP 429/5xx
outcome. Assertion, validation, authentication, parser/schema, mixed-project, and non-test terminal
failures remain look-alikes that must be investigated. Project ownership comes from each Vitest
project's configured include glob rather than a hand-maintained test-title list.

| Signature (log marker)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Surfaces in                                                                                                                                                                                                                                                                                                                  | Confirm it's transient — not a look-alike bug                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Action                                                                                                                                                                                                                                                                                                                                  |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Cloudflare Worker workflow cancellation before deploy evidence exists                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Jobless `Main CI (cloudflare-worker)` cancellations                                                                                                                                                                                                                                                                          | GitHub cancelled the retry before any Cloudflare Worker deploy job was created. **Look-alike:** created jobs, deploy/rollback logs, or any deploy/rollback failure signal needs investigation.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Rerun — **auto-rerun once on `main`** by the `cloudflare-worker-cancelled-before-job-signal` rule (`maxAttempts: 1`); if a later `Main CI (cloudflare-worker)` retry is still jobless, the broad `workflow-cancelled-without-failure-signal` rule ignores it instead of dispatching Codex.                                              |
| Idempotent workflow cancellation before any failure signal exists                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Jobless cancellations in idempotent workflows such as `CI`, `Main CI (checks)`, `Main CI (lambdas)`, `Main CI (web)`, `Portability Tests`, and `Static Code Analysis`; cancelled-only jobs in the same allowlisted workflows when no job concluded `failure` or `timed_out`                                                  | GitHub cancelled the run before any producer job ran or after every unsuccessful job was reported only as `cancelled`. Skipped stateful fan-out jobs, such as a skipped `deploy-web` after an earlier cancellation, do not count as stateful execution. **Look-alike:** any stateful job that was not skipped, any job conclusion of `failure` or `timed_out`, or any non-allowlisted workflow needs investigation.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Ignore - the `workflow-cancelled-without-failure-signal` rule records that no code-fix signal exists instead of dispatching Codex; a later run with a real producer failure will fall through to the matching failure-specific rule or Codex.                                                                                           |
| `permission denied while trying to connect to the Docker daemon socket at unix:///var/run/docker.sock`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Self-hosted Docker jobs (e.g. `tests-backend-unit.yml` postgres/valkey service containers)                                                                                                                                                                                                                                   | Runner-side daemon or socket permission failure, and your diff did not touch Dockerfiles, compose config, service container definitions, workflow YAML, shared actions, or scripts that invoke Docker                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Rerun the failed job; if your diff touched any of those, treat as a real failure                                                                                                                                                                                                                                                        |
| Docker image pull reset: `connection reset by peer`, `net/http: TLS handshake timeout`, or unexpected `EOF` during `docker pull` or service-container start                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Any job pulling images or starting service containers                                                                                                                                                                                                                                                                        | Registry or network flake, not a bad image tag introduced in your diff                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Rerun the failed job                                                                                                                                                                                                                                                                                                                    |
| Lambda deploy AWS CLI transport error: `aws lambda update-function-code` followed by a shared AWS transport marker such as `Connection was closed before we received a valid response from endpoint URL: "https://lambda.us-west-2.amazonaws.com/2015-03-31/functions/.../code".` or `connection reset by peer` on the same Lambda `/code` endpoint line, and exit code 255                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `Main CI (lambdas)` `deploy / deploy (...)` matrix jobs for `@lambdas/*`                                                                                                                                                                                                                                                     | Only Lambda deploy matrix jobs failed, and each failed log reached the AWS Lambda control-plane update endpoint after lambda tests, dependency install, and build completed. **Look-alike:** Lambda tests, dependency install, build, `get-function`, `ResourceNotFoundException`, `ResourceConflictException`, access denied, malformed package, missing function, endpoint lines without a shared AWS transport marker, or mixed non-deploy failures need investigation.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Rerun — **auto-rerun up to twice on `main`** by `main-lambdas-update-function-code-aws-transport-error` (`maxAttempts: 2`); if attempt 3 repeats, Codex is dispatched                                                                                                                                                                   |
| Backend credentialed provider smoke-test transient: a single Vitest failure block under one of the four credentialed vitest projects' own `include` glob (`backend-aws`, `backend-bedrock`, `backend-openai`, `backend-stripe` — any test file matched by that glob, not a fixed list) carries a provider-transport marker: a bare `Test timed out in ` (any digit count — the project's own `testTimeout` owns the number), an AWS-SDK request abort/timeout (`AbortError: Request aborted` and `TimeoutError: The operation was aborted due to timeout`), a Bedrock 500 (`InternalFailure: UnknownError`, `statusCode: 500`, `bedrock-runtime.`, `.amazonaws.com`, `x-amzn-errortype`), or an OpenAI 429 (`429 We're currently processing too many requests` and `rate_limit_exceeded`); or — uniquely for the OpenAI 500 case, which can take out more than one probe at once — every simultaneously failing block in `backend-openai` carries `Error: 500 The server had an error processing your request` and `OpenAI.makeStatusError` | `CI` and `Main CI (backend)` `test-backend-credentialed / backend-credentialed-tests`; aggregate fan-ins (`Patch Coverage`, `tests`, `build`) may fail downstream                                                                                                                                                            | The failing test file is owned by one of the four credentialed vitest projects' own `include` glob — `backend-aws` (`backend/**/*.s3.test.mts`, `backend/modules/aws/s3.test.mts`, `backend/modules/aws/ses.generated.test.mts`), `backend-bedrock` (`backend/**/*.bedrock.test.mts`), `backend-openai` (`backend/**/*.openai*.test.mts`), or `backend-stripe` (`backend/**/*.stripe.test.mts`) — so any new probe test added under that glob is covered automatically, with no per-test path or title hand-copied anywhere. The credentialed producer is the only real failed leaf job. **Look-alike:** provider assertion failures, SDK validation errors, access/credential errors, missing credentialed Vitest command evidence, non-test terminal error sections such as unhandled, teardown, reporter, or coverage errors, OpenAI errors without the rate-limit code or without the provider-side 500 plus `OpenAI.makeStatusError` markers, OpenAI 500s mixed with any other OpenAI/backend failure block, a failure in a test file not owned by any of the four projects' own `include` globs, non-500 Bedrock errors, or any additional unknown leaf job failing in the same run need investigation. | Rerun — **auto-rerun up to twice on `main`** by the `backend-credentialed-provider-smoke-test-transient` rule (`maxAttempts: 2`); if a third matching occurrence repeats the fingerprint, Codex is dispatched                                                                                                                           |
| Lychee reports only GitHub-hosted `[5xx]` link failures and exits 2                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `Lint Links` `lint-links` job                                                                                                                                                                                                                                                                                                | The only failed job is `lint-links`, every reported failed URL is on `github.com`, and every reported status is 5xx. **Look-alike:** 404s, non-GitHub 5xxs, mixed link failures, filesystem link failures, or setup/download failures are real link-check issues unless another row covers them.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Rerun — **auto-rerun once on `main`** by the `lint-links-github-5xx` rule (`maxAttempts: 1`); if attempt 2 repeats the fingerprint the rule exhausts and Codex is dispatched. Scheduled cron sweeps stay outside this auto-retry path because Codex Fix Main handles only push workflow_run completions                                 |
| The `detect-changes` job fails because `dorny/paths-filter`'s pull-request-files GitHub API call returns a 5xx; the check-run annotation is GitHub's branded server-error HTML page (`<!DOCTYPE html>` ... `<title>Unicorn! &middot; GitHub</title>`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | `CI` `detect-changes` job                                                                                                                                                                                                                                                                                                    | The sole genuinely-failed job is `detect-changes` and every annotation on it is the branded GitHub 5xx HTML error page. **Look-alike:** a non-5xx API failure, a config/filter error, a non-GitHub 5xx page, mixed failures with another genuinely-failed job, or `detect-changes` reported as `cancelled` rather than `failure` are not this fingerprint.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Rerun — **auto-rerun once on `main`** by the `detect-changes-paths-filter-github-5xx` rule (`maxAttempts: 1`); if attempt 2 repeats the fingerprint the rule exhausts and Codex is dispatched                                                                                                                                           |
| `setup-lychee` fails before link checking with a GitHub Releases download flake for `github.com/lycheeverse/lychee/releases/download/`, such as `curl: (28) SSL connection timeout` followed by `DOWNLOAD FAILED: ... -> HTTP 302`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `Lint Links` `lint-links` job                                                                                                                                                                                                                                                                                                | The only failed job is `lint-links`, the setup action started, the terminal log slice names the pinned Lychee release asset, and the download failure is a GitHub Releases transport timeout or 5xx/000 response. The Markdown link checker never started. **Look-alike:** unsupported runner platforms, checksum mismatches, extraction failures, 404s, or a successful setup followed by real Lychee link failures need investigation unless another row covers them.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Rerun — **auto-rerun once on `main`** by the `lint-links-setup-lychee-download-flake` rule (`maxAttempts: 1`); if attempt 2 repeats the fingerprint the rule exhausts and Codex is dispatched                                                                                                                                           |
| CI web-integration wrangler/workerd logs `Uncaught Error: Network connection lost` and tests fail with undici `TypeError: fetch failed` / `SocketError: other side closed` / `code: 'UND_ERR_SOCKET'` against the local worker                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `CI` `test-web-integration / web-integration-tests` after services are ready, with optional aggregate `Patch Coverage` / `tests` / `build`                                                                                                                                                                                   | Hundreds of tests passed until wrangler dropped the local socket. Instant auth/fetch failures against `127.0.0.1` after that crash are the worker already dead. **Look-alike:** route-baseline `expected 500 to be 404`, other assertion `FAIL` without `Network connection lost`, extra leaf job failures, or a handle-resolver/OAuth stack in `backend/modules/bluesky-oauth`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Rerun — **auto-rerun once** by the `web-integration-wrangler-socket-closed` rule (`maxAttempts: 1`); `fix-dependabot.yml` evaluates this catalogue from `main`, so Dependabot branches need this rule on `main` (or a rebase onto it) before auto-retry; if attempt 2 repeats the fingerprint the rule exhausts and Codex is dispatched |
| Web Vitest reports `[vitest-pool]: Failed to start threads worker for test files ...` caused by `[vitest-pool-runner]: Timeout waiting for worker to respond` after `Test Files ... passed` and `Tests ... passed`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `Main CI (web)` `test-web / web-tests (*)`; simultaneous `playwright-tests / playwright-tests (*)` leaf failures may be present only when they independently match clean runner shutdown, and `store-playwright-otel` may be skipped/cancelled downstream or fail with `No Playwright OTel artifacts found`                  | The web Vitest coverage command finished with all test files and tests passed, then Vitest emitted a single unhandled worker-pool startup timeout. **Look-alike:** any failed web test summary, assertion error, test timeout, independent Playwright assertion/build/setup failure, independent `store-playwright-otel` failure, or other unknown failed leaf job is a real failure signal.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Rerun — **auto-rerun once on `main`** by the `main-web-vitest-worker-start-timeout-after-pass` rule (`maxAttempts: 1`); if attempt 2 repeats the fingerprint the rule exhausts and Codex is dispatched                                                                                                                                  |
| The `static-checks / static-web` job runs `##[group]Run ./.github/actions/build-web-targets`, `Creating an optimized production build ...`, and `##[error]Process completed with exit code 1.` without any Next.js compiler diagnostic                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | `Main CI (web)` `static-checks / static-web`; companion `test-web-integration / web-integration-tests` and `playwright-tests / playwright-tests (*)` may fail only with clean runner-shutdown markers; `store-playwright-otel` may fail downstream with `No Playwright OTel artifacts found` after a failed Playwright shard | The static-web job's singleton build exited before `Compiled successfully`/filesystem-cache completion, and no `Failed to compile`, `Type error:`, `Module not found:`, prerender/export error, smoke-test failure, migration failure, Playwright assertion, or unknown companion leaf job appears. This job is decoupled from the `test-web / web-tests (*)` Vitest shards, so their results don't gate this fingerprint. Reproduce `pnpm run build` from `web/`; if it fails locally with a diagnostic, treat as a real code bug.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Rerun — **auto-rerun once on `main`** by the `main-web-static-build-silent-exit` rule (`maxAttempts: 1`); if attempt 2 repeats the fingerprint the rule exhausts and Codex is dispatched                                                                                                                                                |
| Playwright setup reports `Could not get lock /var/…` plus `Failed to install browsers` or `Failed to install browser dependencies`, or `Timed out waiting for apt/dpkg locks after …`, before tests run                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | `Main CI (web)` / `CI` Playwright or Playwright credentialed setup jobs; `CI` `storybook / storybook`; `Main CI (storybook)` `storybook-build / storybook`                                                                                                                                                                   | The failed job is still inside `./.github/actions/setup-playwright`, no Playwright spec has started, and no non-Playwright/non-aggregate job failed. `Main CI (web)` `store-playwright-otel` may also fail downstream when non-credentialed Playwright shards fail before producing OTel artifacts; `CI` `Patch Coverage` may fail downstream when `storybook / storybook` fails. **Look-alike:** browser binary checksum/download failures, app build errors, Playwright assertion failures, or `store-playwright-otel` beside only credentialed setup failures are real failures unless another row covers them.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Rerun — **auto-rerun once on `main`** by the `main-web-playwright-setup-apt-lock` rule (`maxAttempts: 1`); if attempt 2 repeats the fingerprint the rule exhausts and Codex is dispatched                                                                                                                                               |
| Main CI web Playwright shard starts specs, then emits repeated `WARN logger_core: received error - timed out`, a `navigateTo()` `page.goto` timeout, and `Error: retryOnConnectionLost: connection retry budget exceeded` from `playwright/helpers/navigate-to.mts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `Main CI (web)` `playwright-tests / playwright-tests (*)`; aggregate fan-ins (`tests`, `build`) may fail downstream, and `store-playwright-otel` may be skipped/cancelled or fail with `No Playwright OTel artifacts found` after the shard failure                                                                          | The shard has already started the production web stack and Playwright specs, and the terminal failure is worker/runtime unresponsiveness during `navigateTo()`, regardless of the exact `page.goto` timeout duration or number of failed specs. **Look-alike:** route-specific navigation timeouts without the worker timeout logs, normal Playwright assertion failures, build failures, migrations, setup failures, independent `store-playwright-otel` failures, unexpected non-shard failed jobs, or credentialed Playwright failures need investigation.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Rerun — **auto-rerun once for this fingerprint on `main`** by the `main-web-playwright-worker-navigation-timeout` rule (`maxAttempts: 1`); unrelated earlier failures do not consume its per-rule budget, and a second actual worker navigation timeout dispatches Codex                                                                |
| `Error acquiring the state lock` with `Lock Info:` in an infrastructure run                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Infrastructure receiver                                                                                                                                                                                                                                                                                                      | A concurrent run or manual OpenTofu invocation can still hold the remote state lock.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Follow the `vouchington-infra` OpenTofu recovery procedure (the private `vouchington-infra` repository); Vouchington does not re-dispatch or unlock infrastructure state.                                                                                                                                                               |
| `Failed to fetch dynamically imported module:` `virtual:/@storybook/builder-vite/project-annotations.js` (alongside `Failed to import test file` and `/web/.storybook/vitest.setup.ts`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | `storybook.yml` browser job                                                                                                                                                                                                                                                                                                  | Dropped virtual-module fetch after most stories already loaded; not a story or assertion failure. **⚠ Look-alike:** `new dependencies found:` plus `optimized dependencies changed. reloading` is a missing `storybookBrowserOptimizeDeps` include (issue #10042), not this transient.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Full workflow rerun once by the `storybook-browser-startup-transient` rule (`maxAttempts: 1`); later workflow attempts escalate immediately.                                                                                                                                                                                            |
| Storybook browser job timeout with `[vite] (client) [optimizer] scanning dependencies` and `The action 'Run Storybook browser tests' has timed out after`, and no `.vitest-reports/*.json` or `coverage/lcov.info` produced                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `storybook.yml` browser job                                                                                                                                                                                                                                                                                                  | Timeout occurs during the Vite optimizer dependency scan with no test report emitted. **⚠ Look-alike:** a generic Storybook/Vite ~10-min timeout was once a real fixed-port collision bug fixed by code (commit `a9833a19`). Only the narrow "optimizer scanning dependencies" fingerprint is transient; a timeout during server warmup or with a consistent port-bind error is a real bug                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Full workflow rerun once by the `storybook-browser-startup-transient` rule (`maxAttempts: 1`); later workflow attempts escalate immediately.                                                                                                                                                                                            |
| `Failed to import test file .../@storybook/addon-vitest/.../vitest-plugin/setup-file.js`, `Vitest failed to find the runner`, and `Tests no tests` before any `\|web-storybook-browser` output                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `storybook.yml` browser job                                                                                                                                                                                                                                                                                                  | Chromium loaded the Storybook add-on's Vitest setup file before a Vitest runner context was available, and no story output or assertions ran. When internal-attempt markers exist, only the terminal attempt is classified. **Look-alike:** story import errors, assertion failures, the same setup error after Storybook browser test output starts (`\|web-storybook-browser`), runner-missing evidence from an earlier internal attempt, or terminal `new dependencies found:` plus `optimized dependencies changed. reloading` evidence need investigation. `[storybook-browser-progress]` module-collected / module-start / module-end markers without that banner are still this in-job runner-missing retry, not a story failure.                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Full workflow rerun once by the `storybook-browser-startup-transient` rule (`maxAttempts: 1`); later workflow attempts escalate immediately.                                                                                                                                                                                            |
| Terminal Storybook browser watchdog attempt with `[storybook-browser] no test output for ... after Vite startup — suspected Vitest/Chromium tester-connection hang`, plus `dependencies optimized`, `Hash is consistent. Skipping.`, or `VITE v... ready in ...ms`, no Storybook browser test-output marker, with `Process completed with exit code 1.`, `Process completed with exit code 143.`, or runner shutdown plus operation-canceled markers with no unexpected process-exit marker                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `storybook.yml` browser job                                                                                                                                                                                                                                                                                                  | Vite startup completed in the same terminal internal attempt, and the repo watchdog terminated a silent Vitest/Chromium tester-connection hang before any test report appeared. **⚠ Look-alike:** assertion failures, story import errors, hangs before Vite is ready, earlier watchdog attempts followed by a later real failure, unexpected process exit codes before a shutdown footer, or hangs after Storybook browser test output starts need investigation because they may indicate a real story or runner bug.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Full workflow rerun once by the `storybook-browser-startup-transient` rule (`maxAttempts: 1`); later workflow attempts escalate immediately.                                                                                                                                                                                            |
| `Failed to connect to the browser session "..." [web-storybook-browser (chromium)] within the timeout.` after Vite reports `dependencies optimized`, `Hash is consistent. Skipping.`, or `VITE v... ready in ...ms`, with `Tests no tests` and no Storybook browser test-output marker                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | `storybook.yml` browser job                                                                                                                                                                                                                                                                                                  | Vitest timed out the Chromium browser-session handshake after Vite startup completed and before any story test output or assertions ran. **⚠ Look-alike:** story assertion failures, import failures, browser-session errors after `\|web-storybook-browser\|` output starts, or failures before Vite is ready are not this fingerprint and need investigation.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Full workflow rerun once by the `storybook-browser-startup-transient` rule (`maxAttempts: 1`); later workflow attempts escalate immediately.                                                                                                                                                                                            |

**Explicit OOM disqualifier:** `runner-shutdown-leaf-rerun` and its shared
`runnerShutdownLeafRerunMatch` predicate disqualify a sibling leaf job from counting as a clean
runner shutdown when any
fetched unsuccessful-job log contains either `Out of memory: Killed process <pid>` or a positive
`oom_kill` counter in its `== cgroup memory ==` diagnostics section. The rule and predicate require
every unsuccessful-job log to be available before treating a sibling as a clean shutdown, so missing
evidence cannot pass through a downstream-job bypass. The
Storybook watchdog row
does the same only for its runner-shutdown/operation-cancelled variant; its process-exit watchdog
and other bootstrap fingerprints are unchanged. The original failed-job log retains the killed
PID/RSS and cgroup evidence, so this does not add a classifier output schema.

The Storybook browser runner owns one monotonic 300-second session budget across at most three
whole-process attempts. Vitest's browser connection timeout and the runner's pre-test hang
threshold both use
`STORYBOOK_BROWSER_HANG_MS` (120 seconds by default), preventing the inner handshake timeout from
ending an attempt before the outer watchdog's promised startup window. Attempt 1 uses the
persistent Vite cache `.cache/vite/storybook-browser` unless
`STORYBOOK_BROWSER_PERSISTENT_CACHE=0` disables it or
`STORYBOOK_BROWSER_PERSISTENT_CACHE_DIR` overrides the directory. Optimizer stalls, add-on
runner-context races, virtual-module fetch failures, silent pre-test watchdog hangs, and pre-test
browser-session timeouts may recycle Vitest and Chromium before the shared deadline. Pure
browser/session failures preserve the ready Vite cache; optimizer, module-fetch, and missing-runner
evidence rotate it. The
`vitest-storybook-progress-reporter` emits bounded, monotonic `module-collected`, `module-start`,
and `module-end` markers; after the first marker, only an explicit localhost dynamic-module fetch
failure or add-on setup-file `Vitest failed to find the runner` before `|web-storybook-browser`
output remains retryable. A later no-progress interval or assertion failure is terminal. Bootstrap diagnostics record honest provider, page,
navigation, and orchestrator-WebSocket milestones; they do not refresh the watchdog. Verbose
browser-API and request-failure output is filtered or capped, retry diagnostics add bounded
`pw:browser*`, and the runner removes raw `pw:protocol*`. On failure,
`storybook-browser-diagnostics.log` preserves every attempt summary and the bounded output tail
inside a 1 MiB cap. See the [Storybook diagnosis reference](../../web/storybook/README.md) for
`bootstrapStage` interpretation and the upstream issue-filing evidence checklist.

Coverage transport exhaustion: when a failed coverage producer emits the exact
`::error::COVERAGE_TRANSPORT_EXHAUSTED suite=<suite>` annotation after both retried GitHub artifact
fallback attempts fail (the shared coverage-transport library always treats the S3 primary leg as
skipped in this repo), rerun once by `coverage-transport-exhausted`. Tooling
`inspectVitestBlobBundle` `ENOENT` after `[coverage-transport] No vitest blob available` is a
repository defect, not a known-transient fingerprint; do not auto-rerun it. The leftover-holder
`--stop` bind wait in
the same `test-tooling / tooling` job is #9766.
`Failed to write timing report to /tmp/setup-web-timings-write-failure.json: Error: EIO: simulated timing-report write failure`
is expected output from `ci/setup-web-integration.mock.test.mts` on every healthy tooling run. A
production `writeTimingReport` `ENOSPC` (Node `code: 'ENOSPC'`, `, write`) is a real disk signal and
must not be classified from that fixture string; it came from a historical test-fixture incident.
A route-selector 30s timeout plus `Timeout terminating threads worker` plus a 16-minute step timeout
is a repository test/hang defect (`dispatch`), not a known-transient. Every other
non-aggregate failed leaf must independently satisfy the same contract or match the clean
runner-shutdown classifier. The contract also requires GitHub step data to show the matching
`Require a persisted <suite> coverage pair` step failed and every failed, timed-out, or cancelled
step in that producer belongs to the suite's exact transport tail. Missing step data,
provenance-stamp failures, mixed test/assertion failures, upload timeouts without the final marker,
coverage-check failures, and unrelated failed leaves remain real failures.

Coverage artifact download timeout: when `Patch Coverage` logs the repository-owned
`[optional-run-artifacts] result=error selector=pattern exit=<nonzero>` marker (or the
`result=unavailable` form, paired with the bounded
`##[warning]Optional same-run artifact unavailable; continuing with validated fallback (selector=pattern exit=<nonzero>)`
warning) together with a known Go HTTP transport-transient marker, or structured step data reports
the `Download coverage artifacts from GitHub (fallback)` step as `timed_out`, and then reports
missing coverage pairs for
one or more suites, rerun once by `coverage-artifact-download-timeout`. The same job log must contain
`[optional-run-artifacts] selected artifact=coverage-<suite>` for every missing suite before
sequential downloads begin. Missing producer-group diagnostics are resolved through the shared coverage
partition contract, so `backend-unit` and `web` require a selected `backend-shard-*` or `web-shard-*`
artifact respectively and must contain at least one
`[optional-run-artifacts] attempt artifact=coverage-<suite>` marker. Those markers prove the GitHub
fallback set existed and downloading started rather than the producer never uploading it. Missing
LCOV without a transport timeout, generic authentication or configuration errors, missing artifacts
that were never selected, or unrelated failed leaves are real
failures unless another row covers them. If the same run also has failed Playwright shards, every
such shard must independently match the clean runner-shutdown classifier before this rule reruns.

On `main`, the backend ECR login TLS handshake timeout, backend ECR push TLS handshake timeout,
backend ECR push layer retry timeout, backend ECR push timeout, backend BuildKit,
tooling teardown artifact-upload timeout,
Lambda deploy AWS CLI closed connection, Lint Links GitHub 5xx, Main CI checks probe-mise hook timeout,
runner shutdown of an idempotent leaf job,
Docker Corepack pnpm registry 5xx, and Storybook rows above are auto-classified by the rule
catalogue in [`ci/transient-retry/`](../../ci/transient-retry/README.md). Those classified failures
rerun according to their rows except terminal BuildKit session expiry, which is ignored without a
retry or Fix Main dispatch. The generic Docker rows are not yet
fingerprinted (the verbatim log strings from the source incidents are unrecoverable after the 1-day
artifact retention window); they are documented here for human judgment until a real log fixture is
captured and added as a code rule. To add a new signature, see
[`ci/transient-retry/README.md`](../../ci/transient-retry/README.md) — include a real log fingerprint
and `exampleRunIds` so the automation and this table stay in sync.

A backend unit shard that fails with `Worker exited unexpectedly` after every test passed (#8259)
is a `teardownTimeout` race, not a genuine worker crash, and no transient-retry rule reruns it: the forks-pool main process closes ~7 Valkey/PSQL/glide-mq sockets in
`test-helpers/vitest.setup.data-stores.mts` `teardown()` after every test passes, and that close is
also the only socket-leak tripwire — see
[Graceful Shutdown § Test Teardown](../overview/architecture/graceful-shutdown.md#test-teardown).
Under the retired self-hosted runners' CPU contention the close occasionally missed the previous 5s
`teardownTimeout` and was force-killed mid-close, which the forks pool then misreports as
`Worker exited unexpectedly`. `vitest.config.mts`'s root-only `teardownTimeout` is now `20_000` for
contention headroom; a genuine hang still force-kills at that ceiling. On any trip,
`[vitest-teardown] phase=… start/done ms=…` lines from `teardown()` plus a `[vitest-teardown-overrun]`
process/handle snapshot from the diagnostics reporter's `onProcessTimeout()` name which phase was
still in flight. That vocabulary is deliberately neutral — it must never contain a substring
`hasBackendUnitVitestFailure` (`ci/transient-retry/runner-shutdown-fingerprints.mts`) reads as a
real failure, which `runner-shutdown-safety-regressions.test.mts` regression-tests.

The main-process tripwire above says nothing about a handle a **test** leaks inside a forks-pool
fork: each fork runs many test files sequentially in one OS process, then is force-killed at pool
teardown with any leaked in-fork handle still open, silently masking it (#8278).
`test-helpers/vitest.setup.fork-leak-detection.mts` closes that gap with a per-fork, per-test
`afterEach` growth-delta tripwire — see
[Graceful Shutdown § Fork-Side Leak Detection](../overview/architecture/graceful-shutdown.md#fork-side-leak-detection)
for the mechanism. A confirmed leak throws in the hook and fails that test with a
`[vitest-fork-leak]` block naming the type, baseline, current count, streak, file where the
streak tripped, and file with the largest observed growth step; set
`VITEST_FORK_LEAK_DETECTION=off` to disable it for a run where the heuristic misfires.

A separate, root-level `afterEach` in `test-helpers/vitest.setup.fake-timer-guard.mts` guards a
different shared-state hazard: a test that leaves `vi.useFakeTimers()` installed corrupts every later
test sharing the same fork (`isolate: false`) or file (`isolate: true`), which twice reached CI as a
real incident (#8328, #8330) before either the shared fork-leak hook or an individual test was fixed
one call site at a time. See
[Test Docs § Fake timers must be restored after every test](reference-tests-parallel-safety-and-test-root-hygiene.md#fake-timers-must-be-restored-after-every-test)
for the mechanism and the fix. Set `VITEST_FAKE_TIMER_GUARD=off` to disable it for a run where the
heuristic misfires.

> **Sync guard**: `ci/transient-retry/__tests__/doc-sync.test.mts` (`ci-tools` Vitest project)
> enforces that every rule id named in this section exists in the `RULES` catalogue. When renaming
> or removing a rule, update this table in the same commit or CI will fail.
