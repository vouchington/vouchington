Review automatic translation safety. Pick exactly one concrete, bounded improvement that is safe to ship in one PR — but when the chosen surface (the one component or callback under review) has multiple render sites sharing the same gap (e.g. several user-authored-text elements, only some of which carry a rendering boundary), close all sites on that surface in this PR, not just one.

- Check [Localization](../../../docs/requirements/users/LOCALIZATION.md) against one translated-content, content-rendering, moderation, privacy, or persistence surface.
- Rotate across these dimensions run to run rather than defaulting to the cheapest one: preserving original content and source content hashes, source/target language identity, provider/model/version metadata, status/freshness fields, and clear rendering boundaries between original and translated text. Before picking, find prior runs of this prompt — `gh pr list --search 'in:title "Automation scheduled: translation-safety.md" label:automation label:"automation:scheduled"' --state merged --json number,mergedAt,body --limit 10` — not this prompt file's own edit history, which only shows edits like this one. A title-only or label-only match is not a valid prior run ([scheduled-prompt.md](../automation/scheduled-prompt.md) requires both automation labels on a same-repository PR): discard any result whose `## Scheduled prompt` section does not name this exact prompt path, sort what remains by `mergedAt` descending (search result order is relevance, not recency), and take the two most recent. Do not repeat the same dimension two runs in a row unless no other dimension has an actionable gap.
- Private or restricted content must not be sent to external translation providers unless an explicit product requirement allows it.
- Do not add translated crawlable URLs, `hreflang`, or locale-prefixed routes unless a current requirement explicitly supports them.
- Add or tighten tests for the selected translation safety, privacy, freshness, or rendering behavior.
- If you can cite at least two prior merged PRs from this prompt exhibiting the same class of gap (e.g. missing rendering boundaries on user-authored text, with PR numbers found via the same verified search above, widened to `--limit 20` and scanned in full rather than only the two-entry rotation window — recurrence of the same gap can be separated by an intervening run that addressed a different dimension), prefer a structural fix scoped to one PR — a shared marked component, or an ast-grep rule that fails closed on new unmarked call sites — over closing one more instance. Otherwise close the instance.
