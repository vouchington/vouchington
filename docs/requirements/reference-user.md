# `user`

[Back to Entity × Action Matrix reference](reference-entity-action-matrix-table-b-entity-action-description.md#user)

| Action                                                 | Predicate              | Description                                                                                                                                                                                                                      | Endpoint                                      | Component                                           |
| ------------------------------------------------------ | ---------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------- | --------------------------------------------------- |
| <a name="user--follow"></a>Follow                      | `follow`               | Follow this user; they receive a notification. Auto-casts Like (+1) (issue #7257; unfollowing does not retract it; skipped for official/contribution-ineligible followers). Signed-out → `/login`. Not available on own profile. | `PUT /api/v1/bookmarks/user/:id/follow`       | `web/components/shared/follow-button.tsx`           |
| <a name="user--trust-choice"></a>Trust choice          | _(semantic sentiment)_ | Vouch (+2), Like (+1), Neutral (0), Dislike (-1), or Disavow (-2). Only Disavow mutes and unfollows; Clear is a distinct DELETE action.                                                                                          | `PUT` / `DELETE /api/v1/users/:id/vouch-vote` | `web/components/users/user-vouch-election-card.tsx` |
| <a name="user--subscribe-posts"></a>Subscribe to Posts | `subscribe`            | Notifies user when this person creates new posts. Not available on own profile.                                                                                                                                                  | `PUT /api/v1/bookmarks/user/:id/subscribe`    | `web/components/shared/entity-bookmark-button.tsx`  |
| <a name="user--mute"></a>Mute                          | `mute`                 | Hides this user from feed. Auth-only; not available on own profile.                                                                                                                                                              | `PUT /api/v1/bookmarks/user/:id/mute`         | `web/components/shared/entity-bookmark-button.tsx`  |
| <a name="user--block"></a>Block                        | `block`                | Bidirectional block; neither user can interact with the other. Auth-only; not available on own profile.                                                                                                                          | `PUT /api/v1/bookmarks/user/:id/block`        | `web/components/shared/entity-bookmark-button.tsx`  |
| <a name="user--tag-vote"></a>User Tag Vote             | `category`             | Vouches for or disavows a curated `Bot` or `Spammer` topic tag. Auth-only; not available on own profile; administrators participate through the same weighted vote model.                                                        | Entity-relations endpoints                    | `web/components/users/user-tags-aside.tsx`          |
| <a name="user--report"></a>Report                      | n/a (moderation queue) | Submits a report with reason + optional note; creates a `moderation_reports` row for admin review. Auth-only; not available on own profile. Placed in `UserActionsAside`.                                                        | `POST /api/v1/reports`                        | `web/components/shared/report-menu-item.tsx`        |
