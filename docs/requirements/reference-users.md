# Users

[Back to Entity × Lifecycle Flow Matrix reference](reference-entity-lifecycle-matrix-table-a-entity-flow-authorization-page-component.md#users)

| Entity | Flow                                                          | Authorization | Page/Route                                                      | Component (file:line)                                                                                                                             | Notes                                                                                                                                           |
| ------ | ------------------------------------------------------------- | ------------- | --------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| `user` | Sign up                                                       | Public        | `/auth/sign-up`                                                 | `web/app/auth/*`                                                                                                                                  |                                                                                                                                                 |
| `user` | Edit profile (bio, identity, preferences, privacy, financial) | Self          | `/my/profile`, `/my/identity`, `/my/preferences`, `/my/privacy` | `web/components/my/*-form.tsx` (one form component per settings page)                                                                             |                                                                                                                                                 |
| `user` | Edit profile links                                            | Self          | `/my/profile`                                                   | `web/components/my/profile-links.tsx:144` (`onEdit`), `:145` (`onDelete`)                                                                         |                                                                                                                                                 |
| `user` | Delete account                                                | Self          | `/my/data`                                                      | `web/app/(my)/my/data/delete-account-dialog.tsx` (`DeleteAccountDialog`, type-to-confirm phrase then delete)                                      |                                                                                                                                                 |
| `user` | Suspend / Unsuspend                                           | Admin         | `/users`, `/user/:id/admin`                                     | `web/app/(user)/user/[idOrUsername]/admin/user-admin-panel.tsx`                                                                                   | Admins search users from `/users`, then suspend or unsuspend from the per-user admin page.                                                      |
| `user` | Grant identity-verification retry                             | Admin         | `/user/:id/admin`                                               | `web/app/(user)/user/[idOrUsername]/admin/identity-verification-attempt-grant.tsx`; client-repository native identity-verification grant surfaces | Only after a terminal self-paid Free attempt; the required support note is retained in the attempt ledger. Native renders this grant form only. |
