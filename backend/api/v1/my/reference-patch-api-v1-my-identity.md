# PATCH /api/v1/my/identity

[Back to My API](README.md#patch-apiv1myidentity)

Updatable fields:

- `username` — new username string
- `use_display_name_from` — `"username"`, `"facebook"`, or `"x"`
- `profile_image_id` — UUID of an uploaded image, or `null` to remove
