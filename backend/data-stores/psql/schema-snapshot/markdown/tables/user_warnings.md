# Table `user_warnings`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Records of formal warnings issued to users by moderators or admins.

Not partitioned — growth: unbounded.

| Column           | Type                       | Nullable | Default                      | Identity | Generated | Collation | Comment                                                                                             |
| ---------------- | -------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | --------------------------------------------------------------------------------------------------- |
| `id`             | `uuid`                     | no       | `uuidv7()`                   |          |           |           |                                                                                                     |
| `user_id`        | `uuid`                     | no       |                              |          |           |           | The user who received this warning.                                                                 |
| `community_id`   | `uuid`                     | yes      |                              |          |           |           | The community scope of this warning, or NULL for a global warning.                                  |
| `issued_by_id`   | `uuid`                     | yes      |                              |          |           |           | The moderator or admin who issued this warning.                                                     |
| `reason`         | `text`                     | no       |                              |          |           |           | Internal reason for the warning (staff-visible only).                                               |
| `public_message` | `text`                     | yes      |                              |          |           |           | Optional public message shown to the warned user.                                                   |
| `report_id`      | `uuid`                     | yes      |                              |          |           |           | The moderation report that triggered this warning, if any.                                          |
| `case_id`        | `uuid`                     | no       |                              |          |           |           | The moderation case this warning belongs to.                                                        |
| `revoked_at`     | `timestamp with time zone` | yes      |                              |          |           |           | When this warning was revoked (e.g. via appeal acceptance). NULL means the warning is still active. |
| `revoked_by_id`  | `uuid`                     | yes      |                              |          |           |           | The staff user who revoked this warning.                                                            |
| `created_at`     | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                                                                     |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**
_none_

**Check constraints:**

- `user_warnings_public_message_check`: `CHECK (((public_message IS NULL) OR (char_length(public_message) <= 2000)))`
- `user_warnings_reason_check`: `CHECK (((char_length(reason) >= 1) AND (char_length(reason) <= 1000)))`

**Foreign keys:**

- `user_warnings_case_id_fkey`: `FOREIGN KEY (case_id) REFERENCES moderation_cases(id) ON DELETE CASCADE`
- `user_warnings_community_id_fkey`: `FOREIGN KEY (community_id) REFERENCES communities(id) ON DELETE SET NULL`
- `user_warnings_issued_by_id_fkey`: `FOREIGN KEY (issued_by_id) REFERENCES users(id) ON DELETE SET NULL`
- `user_warnings_report_id_fkey`: `FOREIGN KEY (report_id) REFERENCES moderation_reports(id) ON DELETE SET NULL`
- `user_warnings_revoked_by_id_fkey`: `FOREIGN KEY (revoked_by_id) REFERENCES users(id) ON DELETE SET NULL`
- `user_warnings_user_id_fkey`: `FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE`

**Indexes:**

- `idx_user_warnings_case_id`: `CREATE INDEX idx_user_warnings_case_id ON public.user_warnings USING btree (case_id)`
- `user_warnings_community_id`: `CREATE INDEX user_warnings_community_id ON public.user_warnings USING btree (community_id, id DESC) WHERE (community_id IS NOT NULL)`
- `user_warnings_pkey`: `CREATE UNIQUE INDEX user_warnings_pkey ON public.user_warnings USING btree (id)`
- `user_warnings_report_id_unique`: `CREATE UNIQUE INDEX user_warnings_report_id_unique ON public.user_warnings USING btree (report_id) WHERE (report_id IS NOT NULL)`
- `user_warnings_user_id`: `CREATE INDEX user_warnings_user_id ON public.user_warnings USING btree (user_id, id DESC)`

**Triggers:**
_none_
