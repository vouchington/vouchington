# Table `community_list_items__rss_feeds`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

RSS feeds curated into a community list by community leads.

Not partitioned — growth: unbounded.

| Column          | Type                       | Nullable | Default                      | Identity | Generated | Collation | Comment                                            |
| --------------- | -------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | -------------------------------------------------- |
| `id`            | `uuid`                     | no       | `uuidv7()`                   |          |           |           |                                                    |
| `community_id`  | `uuid`                     | no       |                              |          |           |           | The community this list item belongs to.           |
| `rss_feed_id`   | `uuid`                     | no       |                              |          |           |           | The RSS feed added to the list.                    |
| `order_index`   | `integer`                  | no       | `0`                          |          |           |           | Manual sort order within the list (lower = first). |
| `added_by_id`   | `uuid`                     | yes      |                              |          |           |           | User who added this item to the list.              |
| `created_at`    | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                    |
| `removed_at`    | `timestamp with time zone` | yes      |                              |          |           |           | When set, the item has been removed from the list. |
| `removed_by_id` | `uuid`                     | yes      |                              |          |           |           | User who removed this item from the list.          |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**
_none_

**Check constraints:**
_none_

**Foreign keys:**

- `community_list_items__rss_feeds_added_by_id_fkey`: `FOREIGN KEY (added_by_id) REFERENCES users(id) ON DELETE SET NULL`
- `community_list_items__rss_feeds_community_id_fkey`: `FOREIGN KEY (community_id) REFERENCES communities(id) ON DELETE CASCADE`
- `community_list_items__rss_feeds_removed_by_id_fkey`: `FOREIGN KEY (removed_by_id) REFERENCES users(id) ON DELETE SET NULL`
- `community_list_items__rss_feeds_rss_feed_id_fkey`: `FOREIGN KEY (rss_feed_id) REFERENCES rss_feeds(id) ON DELETE CASCADE`

**Indexes:**

- `community_list_items__rss_feeds_pkey`: `CREATE UNIQUE INDEX community_list_items__rss_feeds_pkey ON public.community_list_items__rss_feeds USING btree (id)`
- `idx_cli_rss_feeds__community`: `CREATE INDEX idx_cli_rss_feeds__community ON public.community_list_items__rss_feeds USING btree (community_id)`
- `idx_cli_rss_feeds__rss_feed`: `CREATE INDEX idx_cli_rss_feeds__rss_feed ON public.community_list_items__rss_feeds USING btree (rss_feed_id)`
- `idx_cli_rss_feeds__uniq`: `CREATE UNIQUE INDEX idx_cli_rss_feeds__uniq ON public.community_list_items__rss_feeds USING btree (community_id, rss_feed_id) WHERE (removed_at IS NULL)`

**Triggers:**
_none_
