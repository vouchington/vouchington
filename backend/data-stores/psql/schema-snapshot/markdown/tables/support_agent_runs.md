# Table `support_agent_runs`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

AI agent runs for generating support response drafts. Retained indefinitely.

Not partitioned — growth: unbounded.

| Column                     | Type                                                    | Nullable | Default                      | Identity | Generated | Collation | Comment                                                                                                       |
| -------------------------- | ------------------------------------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | ------------------------------------------------------------------------------------------------------------- |
| `id`                       | `uuid`                                                  | no       | `uuidv7()`                   |          |           |           |                                                                                                               |
| `support_thread_id`        | `uuid`                                                  | no       |                              |          |           |           | The thread this agent run is responding to.                                                                   |
| `support_message_id`       | `uuid`                                                  | no       |                              |          |           |           | The inbound message that triggered this agent run.                                                            |
| `model_name`               | `agent_models`                                          | no       |                              |          |           |           | LLM model used for this agent run.                                                                            |
| `model_provider`           | `agent_model_providers`                                 | no       |                              |          |           |           | LLM provider used for this agent run.                                                                         |
| `input`                    | `jsonb`                                                 | no       |                              |          |           |           | Thread context passed to the agent.                                                                           |
| `output`                   | `jsonb`                                                 | yes      |                              |          |           |           | Generated draft text.                                                                                         |
| `error`                    | `jsonb`                                                 | yes      |                              |          |           |           | Error details if the run failed.                                                                              |
| `failed_at`                | `timestamp with time zone`                              | yes      |                              |          |           |           | When the agent run failed. Derived status is failed when set.                                                 |
| `termination_reason`       | `conversation_message_agentic_runs_termination_reasons` | yes      |                              |          |           |           | Why the run ended. Superseded runs are terminal and cannot be reclaimed.                                      |
| `started_at`               | `timestamp with time zone`                              | no       | `CURRENT_TIMESTAMP`          |          |           |           | When the agent run began processing.                                                                          |
| `completed_at`             | `timestamp with time zone`                              | yes      |                              |          |           |           | When the agent run completed successfully.                                                                    |
| `created_at`               | `timestamp with time zone`                              | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                                                                               |
| `updated_at`               | `timestamp with time zone`                              | no       | `CURRENT_TIMESTAMP`          |          |           |           |                                                                                                               |
| `idempotency_key`          | `text`                                                  | yes      |                              |          |           |           | Optional durable logical intent that prevents a replayed queue job from creating a second agent run or draft. |
| `claim_token`              | `text`                                                  | yes      |                              |          |           |           | Opaque fencing token rotated by every winning keyed claim; stale claimants cannot mutate a newer attempt.     |
| `staff_draft_requested_at` | `timestamp with time zone`                              | yes      |                              |          |           |           | Set for a staff-requested AI draft reservation before it is enqueued.                                         |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**
_none_

**Check constraints:**

- `support_agent_runs_check`: `CHECK (((completed_at IS NULL) OR (failed_at IS NULL)))`

**Foreign keys:**

- `support_agent_runs_support_thread_id_fkey`: `FOREIGN KEY (support_thread_id) REFERENCES support_threads(id) ON DELETE CASCADE`
- `support_agent_runs_support_thread_id_support_message_id_fkey`: `FOREIGN KEY (support_thread_id, support_message_id) REFERENCES support_messages(support_thread_id, id) ON DELETE CASCADE`

**Indexes:**

- `idx_support_agent_runs__idempotency_key`: `CREATE UNIQUE INDEX idx_support_agent_runs__idempotency_key ON public.support_agent_runs USING btree (idempotency_key) WHERE (idempotency_key IS NOT NULL)`
- `idx_support_agent_runs__one_active_staff_draft_per_thread`: `CREATE UNIQUE INDEX idx_support_agent_runs__one_active_staff_draft_per_thread ON public.support_agent_runs USING btree (support_thread_id) WHERE ((staff_draft_requested_at IS NOT NULL) AND (completed_at IS NULL) AND (failed_at IS NULL))`
- `idx_support_agent_runs__pending_member_intent`: `CREATE INDEX idx_support_agent_runs__pending_member_intent ON public.support_agent_runs USING btree (support_message_id) INCLUDE (support_thread_id, idempotency_key) WHERE ((idempotency_key = (('support_member_thread__'::text || (support_message_id)::text) || '__customer_support'::text)) AND (completed_at IS NULL))`
- `idx_support_agent_runs__thread_message`: `CREATE INDEX idx_support_agent_runs__thread_message ON public.support_agent_runs USING btree (support_thread_id, support_message_id)`
- `support_agent_runs_pkey`: `CREATE UNIQUE INDEX support_agent_runs_pkey ON public.support_agent_runs USING btree (id)`

**Triggers:**

- `trigger_support_agent_runs_updated_at`: `CREATE TRIGGER trigger_support_agent_runs_updated_at BEFORE UPDATE ON public.support_agent_runs FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
