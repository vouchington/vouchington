# Table `oauth_access_tokens`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Opaque access tokens stored only as purpose-bound hashes.

Not partitioned — growth: bounded.

| Column              | Type                       | Nullable | Default                      | Identity | Generated | Collation | Comment                                                |
| ------------------- | -------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | ------------------------------------------------------ |
| `id`                | `uuid`                     | no       | `uuidv7()`                   |          |           |           |                                                        |
| `grant_id`          | `uuid`                     | no       |                              |          |           |           | Delegated grant represented by the access token.       |
| `refresh_family_id` | `uuid`                     | no       |                              |          |           |           | Refresh family that issued the access token.           |
| `token_hash`        | `text`                     | no       |                              |          |           |           | Purpose-bound hash of the opaque bearer token.         |
| `resource`          | `text`                     | no       |                              |          |           |           | Protected resource audience bound to the access token. |
| `scopes`            | `text[]`                   | no       |                              |          |           |           | Canonical scopes carried by the access token.          |
| `expires_at`        | `timestamp with time zone` | no       |                              |          |           |           | Time after which the access token is invalid.          |
| `revoked_at`        | `timestamp with time zone` | yes      |                              |          |           |           | Time at which the access token was revoked.            |
| `last_used_at`      | `timestamp with time zone` | yes      |                              |          |           |           | Time at which bearer validation last succeeded.        |
| `created_at`        | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                        |
| `updated_at`        | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP`          |          |           |           |                                                        |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**

- `oauth_access_tokens_token_hash_key`: `UNIQUE (token_hash)`

**Check constraints:**

- `oauth_access_tokens_resource_check`: `CHECK (((char_length(resource) >= 1) AND (char_length(resource) <= 2048)))`
- `oauth_access_tokens_scopes_check`: `CHECK ((cardinality(scopes) > 0))`
- `oauth_access_tokens_token_hash_check`: `CHECK ((char_length(token_hash) = 64))`

**Foreign keys:**

- `fk_oauth_access_tokens__family_grant`: `FOREIGN KEY (refresh_family_id, grant_id) REFERENCES oauth_refresh_token_families(id, grant_id) ON DELETE CASCADE`
- `oauth_access_tokens_grant_id_fkey`: `FOREIGN KEY (grant_id) REFERENCES oauth_grants(id) ON DELETE CASCADE`

**Indexes:**

- `idx_oauth_access_tokens__expiry`: `CREATE INDEX idx_oauth_access_tokens__expiry ON public.oauth_access_tokens USING btree (expires_at, id)`
- `idx_oauth_access_tokens__family_grant`: `CREATE INDEX idx_oauth_access_tokens__family_grant ON public.oauth_access_tokens USING btree (refresh_family_id, grant_id)`
- `idx_oauth_access_tokens__grant`: `CREATE INDEX idx_oauth_access_tokens__grant ON public.oauth_access_tokens USING btree (grant_id, id)`
- `idx_oauth_access_tokens__refresh_family`: `CREATE INDEX idx_oauth_access_tokens__refresh_family ON public.oauth_access_tokens USING btree (refresh_family_id, id) WHERE (refresh_family_id IS NOT NULL)`
- `oauth_access_tokens_pkey`: `CREATE UNIQUE INDEX oauth_access_tokens_pkey ON public.oauth_access_tokens USING btree (id)`
- `oauth_access_tokens_token_hash_key`: `CREATE UNIQUE INDEX oauth_access_tokens_token_hash_key ON public.oauth_access_tokens USING btree (token_hash)`

**Triggers:**

- `trigger_oauth_access_tokens_updated_at`: `CREATE TRIGGER trigger_oauth_access_tokens_updated_at BEFORE UPDATE ON public.oauth_access_tokens FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
