# Table `agents__moderators`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Moderator-specific configuration extending the agents table.

Not partitioned — growth: unbounded.

| Column           | Type                       | Nullable | Default                                    | Identity | Generated | Collation | Comment                                                                                                     |
| ---------------- | -------------------------- | -------- | ------------------------------------------ | -------- | --------- | --------- | ----------------------------------------------------------------------------------------------------------- |
| `agent_id`       | `uuid`                     | no       |                                            |          |           |           | The agent this moderator config extends (PK, 1:1 with agents).                                              |
| `slug`           | `text`                     | no       |                                            |          |           |           | Unique lowercase identifier for this moderator (e.g. spam-filter).                                          |
| `on_flag_action` | `moderator_on_flag_action` | no       | `'review_queue'::moderator_on_flag_action` |          |           |           | Action taken when a moderator flags content: none (log only) or review_queue (move to review queue).        |
| `is_baseline`    | `boolean`                  | no       | `false`                                    |          |           |           | When true, this moderator runs on every approved post regardless of community opt-in (baseline safety net). |
| `created_at`     | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP`                        |          |           |           |                                                                                                             |
| `updated_at`     | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP`                        |          |           |           |                                                                                                             |

**Primary key:** `PRIMARY KEY (agent_id)`

**Unique constraints:**

- `agents__moderators_slug_key`: `UNIQUE (slug)`

**Check constraints:**

- `agents__moderators_slug_check`: `CHECK (((slug = lower(slug)) AND (TRIM(BOTH FROM slug) = slug)))`

**Foreign keys:**

- `agents__moderators_agent_id_fkey`: `FOREIGN KEY (agent_id) REFERENCES agents(id) ON DELETE CASCADE`

**Indexes:**

- `agents__moderators_pkey`: `CREATE UNIQUE INDEX agents__moderators_pkey ON public.agents__moderators USING btree (agent_id)`
- `agents__moderators_slug_key`: `CREATE UNIQUE INDEX agents__moderators_slug_key ON public.agents__moderators USING btree (slug)`
- `idx_agents__moderators__slug`: `CREATE INDEX idx_agents__moderators__slug ON public.agents__moderators USING btree (slug)`

**Triggers:**

- `trigger_agents__moderators_updated_at`: `CREATE TRIGGER trigger_agents__moderators_updated_at BEFORE UPDATE ON public.agents__moderators FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
