# Table `user_deletion_requests`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Durable user-deletion lifecycle with timestamp-derived progress and fenced worker attempts.

Not partitioned — growth: unbounded.

| Column                  | Type                       | Nullable | Default                      | Identity | Generated | Collation | Comment                                                                                                     |
| ----------------------- | -------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | ----------------------------------------------------------------------------------------------------------- |
| `id`                    | `uuid`                     | no       | `uuidv7()`                   |          |           |           |                                                                                                             |
| `user_id`               | `uuid`                     | no       |                              |          |           |           | Deleted account identifier retained without a foreign key so the durable lifecycle survives hard deletion.  |
| `requested_by_id`       | `uuid`                     | yes      |                              |          |           |           | Audit actor identifier retained without a foreign key so the durable lifecycle survives requester deletion. |
| `prior_username`        | `text`                     | yes      |                              |          |           |           | Username captured before profile scrubbing for deletion-phase cleanup.                                      |
| `queued_at`             | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP`          |          |           |           | Timestamp when the privacy fence committed and durable processing became eligible.                          |
| `processing_attempt_id` | `uuid`                     | no       | `uuidv7()`                   |          |           |           | Fencing token rotated before each successor, terminal queue failure, or stale recovery attempt.             |
| `dispatched_at`         | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP`          |          |           |           | Timestamp of the latest queue dispatch or recovery handoff.                                                 |
| `processing_started_at` | `timestamp with time zone` | yes      |                              |          |           |           | Timestamp when the exact fencing attempt claimed processing.                                                |
| `processing_attempts`   | `integer`                  | no       | `0`                          |          |           |           | Number of fenced processing attempts claimed for this request.                                              |
| `current_phase`         | `text`                     | no       | `'posts'::text`              |          |           |           | Ordered deletion phase; timestamps encode lifecycle state rather than an enum status.                       |
| `completed_at`          | `timestamp with time zone` | yes      |                              |          |           |           | Terminal marker written only after every deletion phase and external work item completes.                   |
| `last_error_message`    | `text`                     | yes      |                              |          |           |           | Truncated diagnostic from the latest failed attempt; retry eligibility remains timestamp-derived.           |
| `created_at`            | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                                                                             |
| `updated_at`            | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP`          |          |           |           |                                                                                                             |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**

- `user_deletion_requests_user_id_key`: `UNIQUE (user_id)`

**Check constraints:**

- `user_deletion_requests_check`: `CHECK (((completed_at IS NULL) OR (processing_started_at IS NULL)))`
- `user_deletion_requests_current_phase_check`: `CHECK ((current_phase = ANY (ARRAY['posts'::text, 'votes'::text, 'user-relations'::text, 'credentials'::text, 'account-data'::text, 'relation-impacts'::text, 'external-work'::text, 'finalize'::text])))`
- `user_deletion_requests_last_error_message_check`: `CHECK (((last_error_message IS NULL) OR (char_length(last_error_message) <= 2000)))`
- `user_deletion_requests_processing_attempts_check`: `CHECK ((processing_attempts >= 0))`

**Foreign keys:**
_none_

**Indexes:**

- `idx_user_deletion_requests__processing_attempt_id`: `CREATE UNIQUE INDEX idx_user_deletion_requests__processing_attempt_id ON public.user_deletion_requests USING btree (processing_attempt_id)`
- `idx_user_deletion_requests__recover_started`: `CREATE INDEX idx_user_deletion_requests__recover_started ON public.user_deletion_requests USING btree (processing_started_at, id) WHERE ((completed_at IS NULL) AND (processing_started_at IS NOT NULL))`
- `idx_user_deletion_requests__recover_unstarted`: `CREATE INDEX idx_user_deletion_requests__recover_unstarted ON public.user_deletion_requests USING btree (dispatched_at, id) WHERE ((completed_at IS NULL) AND (processing_started_at IS NULL))`
- `user_deletion_requests_pkey`: `CREATE UNIQUE INDEX user_deletion_requests_pkey ON public.user_deletion_requests USING btree (id)`
- `user_deletion_requests_user_id_key`: `CREATE UNIQUE INDEX user_deletion_requests_user_id_key ON public.user_deletion_requests USING btree (user_id)`

**Triggers:**

- `trigger_user_deletion_requests_updated_at`: `CREATE TRIGGER trigger_user_deletion_requests_updated_at BEFORE UPDATE ON public.user_deletion_requests FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
