# Table `post_moderation_attempts`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Durable provider-attempt ledger. Each current-version source receives at most three attempts.

Not partitioned — growth: unbounded.

| Column           | Type                       | Nullable | Default                      | Identity | Generated | Collation | Comment                                                                     |
| ---------------- | -------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | --------------------------------------------------------------------------- |
| `id`             | `uuid`                     | no       | `uuidv7()`                   |          |           |           |                                                                             |
| `version_id`     | `uuid`                     | no       |                              |          |           |           | Moderation version evaluated by this provider attempt.                      |
| `source`         | `post_moderation_sources`  | no       |                              |          |           |           | Automated moderation source that owns this attempt.                         |
| `attempt_number` | `smallint`                 | no       |                              |          |           |           | One-based bounded retry sequence for this version and source.               |
| `lease_token`    | `uuid`                     | no       |                              |          |           |           | Fencing token matching the work lease that created this attempt.            |
| `completed_at`   | `timestamp with time zone` | yes      |                              |          |           |           | Clock time at which this attempt completed successfully.                    |
| `failed_at`      | `timestamp with time zone` | yes      |                              |          |           |           | Clock time at which this attempt failed and scheduled a retry or exhausted. |
| `error_code`     | `text`                     | yes      |                              |          |           |           | Stable provider-neutral failure reason for a failed attempt.                |
| `created_at`     | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                                             |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**

- `post_moderation_attempts_id_version_id_source_key`: `UNIQUE (id, version_id, source)`
- `post_moderation_attempts_version_id_source_attempt_number_key`: `UNIQUE (version_id, source, attempt_number)`
- `post_moderation_attempts_version_id_source_lease_token_key`: `UNIQUE (version_id, source, lease_token)`

**Check constraints:**

- `post_moderation_attempts_attempt_number_check`: `CHECK (((attempt_number >= 1) AND (attempt_number <= 3)))`
- `post_moderation_attempts_check`: `CHECK ((num_nonnulls(completed_at, failed_at) <= 1))`
- `post_moderation_attempts_check1`: `CHECK (((failed_at IS NOT NULL) OR (error_code IS NULL)))`
- `post_moderation_attempts_error_code_check`: `CHECK (((error_code IS NULL) OR ((char_length(error_code) >= 1) AND (char_length(error_code) <= 100))))`

**Foreign keys:**

- `post_moderation_attempts_version_id_source_fkey`: `FOREIGN KEY (version_id, source) REFERENCES post_moderation_work_items(version_id, source) ON DELETE CASCADE`

**Indexes:**

- `idx_post_moderation_attempts__version_source_id`: `CREATE INDEX idx_post_moderation_attempts__version_source_id ON public.post_moderation_attempts USING btree (version_id, source, id DESC)`
- `post_moderation_attempts_id_version_id_source_key`: `CREATE UNIQUE INDEX post_moderation_attempts_id_version_id_source_key ON public.post_moderation_attempts USING btree (id, version_id, source)`
- `post_moderation_attempts_pkey`: `CREATE UNIQUE INDEX post_moderation_attempts_pkey ON public.post_moderation_attempts USING btree (id)`
- `post_moderation_attempts_version_id_source_attempt_number_key`: `CREATE UNIQUE INDEX post_moderation_attempts_version_id_source_attempt_number_key ON public.post_moderation_attempts USING btree (version_id, source, attempt_number)`
- `post_moderation_attempts_version_id_source_lease_token_key`: `CREATE UNIQUE INDEX post_moderation_attempts_version_id_source_lease_token_key ON public.post_moderation_attempts USING btree (version_id, source, lease_token)`

**Triggers:**
_none_
