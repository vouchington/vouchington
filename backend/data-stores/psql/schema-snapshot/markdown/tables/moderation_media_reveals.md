# Table `moderation_media_reveals`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Append-only log of disturbing-media reveals by moderators. Used to count session exposure and trigger cooldown prompts.

Not partitioned — growth: unbounded.

| Column         | Type                               | Nullable | Default                      | Identity | Generated | Collation | Comment                                                                                                                                                               |
| -------------- | ---------------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `id`           | `uuid`                             | no       | `uuidv7()`                   |          |           |           |                                                                                                                                                                       |
| `created_at`   | `timestamp with time zone`         | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           | Row insertion time derived from the UUIDv7 id; used for schema convention only. Windowed queries use revealed_at.                                                     |
| `moderator_id` | `uuid`                             | yes      |                              |          |           |           | Moderator who revealed the media (ON DELETE SET NULL for audit persistence).                                                                                          |
| `post_id`      | `uuid`                             | yes      |                              |          |           |           | Post whose media was revealed. NULL for report-entity reveals without a direct post.                                                                                  |
| `report_id`    | `uuid`                             | yes      |                              |          |           |           | Report entity being reviewed when media was revealed. NULL for direct post reveals.                                                                                   |
| `surface`      | `moderation_media_reveal_surfaces` | no       |                              |          |           |           | Queue surface where the reveal occurred: mod_queue (community queue), review_queue (admin review), reports (reports UI), post_page (public post page in mod context). |
| `revealed_at`  | `timestamp with time zone`         | no       | `now()`                      |          |           |           | When the moderator clicked to reveal the media. Real column (not virtual) for time-window range queries.                                                              |
| `metadata`     | `jsonb`                            | no       | `'{}'::jsonb`                |          |           |           | Structured context (e.g. community_id, image_id).                                                                                                                     |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**
_none_

**Check constraints:**

- `moderation_media_reveals_metadata_check`: `CHECK ((jsonb_typeof(metadata) = 'object'::text))`

**Foreign keys:**

- `moderation_media_reveals_moderator_id_fkey`: `FOREIGN KEY (moderator_id) REFERENCES users(id) ON DELETE SET NULL`
- `moderation_media_reveals_post_id_fkey`: `FOREIGN KEY (post_id) REFERENCES posts(id) ON DELETE SET NULL`
- `moderation_media_reveals_report_id_fkey`: `FOREIGN KEY (report_id) REFERENCES moderation_reports(id) ON DELETE SET NULL`

**Indexes:**

- `idx_moderation_media_reveals__moderator_revealed`: `CREATE INDEX idx_moderation_media_reveals__moderator_revealed ON public.moderation_media_reveals USING btree (moderator_id, revealed_at DESC)`
- `idx_moderation_media_reveals__post_id`: `CREATE INDEX idx_moderation_media_reveals__post_id ON public.moderation_media_reveals USING btree (post_id) WHERE (post_id IS NOT NULL)`
- `idx_moderation_media_reveals__report_id`: `CREATE INDEX idx_moderation_media_reveals__report_id ON public.moderation_media_reveals USING btree (report_id) WHERE (report_id IS NOT NULL)`
- `moderation_media_reveals_pkey`: `CREATE UNIQUE INDEX moderation_media_reveals_pkey ON public.moderation_media_reveals USING btree (id)`

**Triggers:**
_none_
