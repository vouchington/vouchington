# Table `follower_distribution_deliveries`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Per-recipient delivery ids for idempotent distribution chunk retries.

Not partitioned — growth: unbounded.

| Column              | Type                       | Nullable | Default                               | Identity | Generated | Collation | Comment                                                        |
| ------------------- | -------------------------- | -------- | ------------------------------------- | -------- | --------- | --------- | -------------------------------------------------------------- |
| `distribution_id`   | `uuid`                     | no       |                                       |          |           |           | Distribution intent this delivery belongs to.                  |
| `recipient_user_id` | `uuid`                     | no       |                                       |          |           |           | Follower receiving the distribution delivery.                  |
| `delivery_id`       | `uuid`                     | no       | `uuidv7()`                            |          |           |           | Stable id reused for the final feed share or notification row. |
| `created_at`        | `timestamp with time zone` | yes      | `uuid_extract_timestamp(delivery_id)` |          | virtual   |           |                                                                |
| `updated_at`        | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP`                   |          |           |           |                                                                |

**Primary key:** `PRIMARY KEY (distribution_id, recipient_user_id)`

**Unique constraints:**

- `follower_distribution_deliver_recipient_user_id_delivery_id_key`: `UNIQUE (recipient_user_id, delivery_id)`

**Check constraints:**
_none_

**Foreign keys:**

- `follower_distribution_deliveries_distribution_id_fkey`: `FOREIGN KEY (distribution_id) REFERENCES follower_distributions(id) ON DELETE CASCADE`
- `follower_distribution_deliveries_recipient_user_id_fkey`: `FOREIGN KEY (recipient_user_id) REFERENCES users(id) ON DELETE CASCADE`

**Indexes:**

- `follower_distribution_deliver_recipient_user_id_delivery_id_key`: `CREATE UNIQUE INDEX follower_distribution_deliver_recipient_user_id_delivery_id_key ON public.follower_distribution_deliveries USING btree (recipient_user_id, delivery_id)`
- `follower_distribution_deliveries_pkey`: `CREATE UNIQUE INDEX follower_distribution_deliveries_pkey ON public.follower_distribution_deliveries USING btree (distribution_id, recipient_user_id)`
- `idx_follower_distribution_deliveries__delivery`: `CREATE INDEX idx_follower_distribution_deliveries__delivery ON public.follower_distribution_deliveries USING btree (delivery_id)`

**Triggers:**

- `trigger_follower_distribution_deliveries_updated_at`: `CREATE TRIGGER trigger_follower_distribution_deliveries_updated_at BEFORE UPDATE ON public.follower_distribution_deliveries FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
