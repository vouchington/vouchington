# Table `autotagger_receipts`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Claim/lease/completion ledger for the C6 embedding-path tagging classifier. A changed digest is a new identity with its own row and batch_id.

Not partitioned — growth: unbounded.

| Column             | Type                       | Nullable | Default                      | Identity | Generated | Collation | Comment                                                                                                                                                                                 |
| ------------------ | -------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `id`               | `uuid`                     | no       | `uuidv7()`                   |          |           |           |                                                                                                                                                                                         |
| `post_id`          | `uuid`                     | yes      |                              |          |           |           |                                                                                                                                                                                         |
| `rss_feed_item_id` | `uuid`                     | yes      |                              |          |           |           |                                                                                                                                                                                         |
| `digest_version`   | `smallint`                 | no       |                              |          |           |           | Version of the receipt digest composition, bumped whenever the hashed request shape changes.                                                                                            |
| `digest`           | `bytea`                    | no       |                              |          |           |           | SHA-256 of the exact sanitized request state: wrapped state, rendered candidate IDs/text, ordered candidates, scope, effective cap, classifier/prompt IDs, and provider/model revision. |
| `batch_id`         | `uuid`                     | no       |                              |          |           |           | Assigned at receipt creation; reused as classifier_decision_batches.id once a decision commits. Not a foreign key: the batch may not exist yet.                                         |
| `lease_token`      | `uuid`                     | yes      |                              |          |           |           | Fencing token identifying the current exclusive claimant. NULL when unclaimed or completed.                                                                                             |
| `leased_at`        | `timestamp with time zone` | yes      |                              |          |           |           |                                                                                                                                                                                         |
| `lease_expires_at` | `timestamp with time zone` | yes      |                              |          |           |           |                                                                                                                                                                                         |
| `completed_at`     | `timestamp with time zone` | yes      |                              |          |           |           | Clock time at which the classifier decision committed and downstream vote application became safe to run idempotently.                                                                  |
| `created_at`       | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                                                                                                                                                         |
| `updated_at`       | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP`          |          |           |           |                                                                                                                                                                                         |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**

- `uq_autotagger_receipts__batch_id`: `UNIQUE (batch_id)`

**Check constraints:**

- `autotagger_receipts_digest_check`: `CHECK ((octet_length(digest) = 32))`
- `autotagger_receipts_digest_version_check`: `CHECK ((digest_version >= 1))`
- `chk_autotagger_receipts__lease_token_expires_at`: `CHECK (((lease_token IS NULL) = (lease_expires_at IS NULL)))`
- `chk_autotagger_receipts__lease_token_leased_at`: `CHECK (((lease_token IS NULL) = (leased_at IS NULL)))`
- `chk_autotagger_receipts__not_completed_while_leased`: `CHECK (((completed_at IS NULL) OR (lease_token IS NULL)))`
- `chk_autotagger_receipts__one_subject`: `CHECK ((num_nonnulls(post_id, rss_feed_item_id) = 1))`

**Foreign keys:**

- `autotagger_receipts_post_id_fkey`: `FOREIGN KEY (post_id) REFERENCES posts(id) ON DELETE CASCADE`
- `autotagger_receipts_rss_feed_item_id_fkey`: `FOREIGN KEY (rss_feed_item_id) REFERENCES rss_feed_items(id) ON DELETE CASCADE`

**Indexes:**

- `autotagger_receipts_pkey`: `CREATE UNIQUE INDEX autotagger_receipts_pkey ON public.autotagger_receipts USING btree (id)`
- `idx_autotagger_receipts__lease_expires_at`: `CREATE INDEX idx_autotagger_receipts__lease_expires_at ON public.autotagger_receipts USING btree (lease_expires_at) WHERE (lease_token IS NOT NULL)`
- `uq_autotagger_receipts__batch_id`: `CREATE UNIQUE INDEX uq_autotagger_receipts__batch_id ON public.autotagger_receipts USING btree (batch_id)`
- `uq_autotagger_receipts__post_digest`: `CREATE UNIQUE INDEX uq_autotagger_receipts__post_digest ON public.autotagger_receipts USING btree (post_id, digest_version, digest) WHERE (post_id IS NOT NULL)`
- `uq_autotagger_receipts__rss_feed_item_digest`: `CREATE UNIQUE INDEX uq_autotagger_receipts__rss_feed_item_digest ON public.autotagger_receipts USING btree (rss_feed_item_id, digest_version, digest) WHERE (rss_feed_item_id IS NOT NULL)`

**Triggers:**

- `trigger_autotagger_receipts_updated_at`: `CREATE TRIGGER trigger_autotagger_receipts_updated_at BEFORE UPDATE ON public.autotagger_receipts FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
