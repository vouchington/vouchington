# Table `ap_posts`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

AP-only like tally per post, trigger-maintained from ap_post_likes. Isolated from post_votes — remote likes never affect local ranking.

Not partitioned — growth: unbounded.

| Column           | Type                       | Nullable | Default             | Identity | Generated | Collation | Comment                                                                                                                                                              |
| ---------------- | -------------------------- | -------- | ------------------- | -------- | --------- | --------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `post_id`        | `uuid`                     | no       |                     |          |           |           | The local post this tally is for (1:1, lazily created on first remote like).                                                                                         |
| `ap_likes_score` | `double precision`         | no       | `0`                 |          |           |           | COUNT of active (non-Undo'd) ap_post_likes rows for this post. DOUBLE PRECISION to match the shape of other score columns even though every Like currently weighs 1. |
| `ap_likes_count` | `integer`                  | no       | `0`                 |          |           |           | Same value as ap_likes_score, stored separately as a plain display count in case score gains per-actor weighting later.                                              |
| `created_at`     | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                                                                                                                                      |
| `updated_at`     | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                                                                                                                                      |

**Primary key:** `PRIMARY KEY (post_id)`

**Unique constraints:**
_none_

**Check constraints:**
_none_

**Foreign keys:**

- `ap_posts_post_id_fkey`: `FOREIGN KEY (post_id) REFERENCES posts(id) ON DELETE CASCADE`

**Indexes:**

- `ap_posts_pkey`: `CREATE UNIQUE INDEX ap_posts_pkey ON public.ap_posts USING btree (post_id)`

**Triggers:**

- `trigger_ap_posts_updated_at`: `CREATE TRIGGER trigger_ap_posts_updated_at BEFORE UPDATE ON public.ap_posts FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
