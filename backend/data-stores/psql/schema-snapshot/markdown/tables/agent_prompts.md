# Table `agent_prompts`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Versioned prompts for agents. Each prompt targets a specific model and can be activated/deactivated.

Not partitioned — growth: unbounded.

| Column           | Type                       | Nullable | Default                      | Identity | Generated | Collation | Comment                                                                |
| ---------------- | -------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | ---------------------------------------------------------------------- |
| `id`             | `uuid`                     | no       | `uuidv7()`                   |          |           |           |                                                                        |
| `prompt`         | `text`                     | no       |                              |          |           |           | The system prompt text sent to the LLM.                                |
| `agent_id`       | `uuid`                     | no       |                              |          |           |           | The agent this prompt belongs to.                                      |
| `model_name`     | `agent_models`             | no       |                              |          |           |           | The LLM model name (e.g. gpt-5.4-nano).                                |
| `model_provider` | `agent_model_providers`    | no       |                              |          |           |           | The LLM provider (e.g. openai).                                        |
| `created_at`     | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                                        |
| `created_by_id`  | `uuid`                     | yes      |                              |          |           |           |                                                                        |
| `updated_at`     | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP`          |          |           |           |                                                                        |
| `updated_by_id`  | `uuid`                     | yes      |                              |          |           |           |                                                                        |
| `deleted_at`     | `timestamp with time zone` | yes      |                              |          |           |           |                                                                        |
| `deleted_by_id`  | `uuid`                     | yes      |                              |          |           |           |                                                                        |
| `activated_at`   | `timestamp with time zone` | yes      |                              |          |           |           | When this prompt version was activated. NULL if currently deactivated. |
| `deactivated_at` | `timestamp with time zone` | yes      |                              |          |           |           | When this prompt version was deactivated. NULL if currently active.    |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**
_none_

**Check constraints:**

- `agent_prompts_check`: `CHECK ((NOT ((activated_at IS NOT NULL) AND (deactivated_at IS NOT NULL))))`

**Foreign keys:**

- `agent_prompts_agent_id_fkey`: `FOREIGN KEY (agent_id) REFERENCES agents(id) ON DELETE CASCADE`
- `agent_prompts_created_by_id_fkey`: `FOREIGN KEY (created_by_id) REFERENCES users(id) ON DELETE SET NULL`
- `agent_prompts_deleted_by_id_fkey`: `FOREIGN KEY (deleted_by_id) REFERENCES users(id) ON DELETE SET NULL`
- `agent_prompts_updated_by_id_fkey`: `FOREIGN KEY (updated_by_id) REFERENCES users(id) ON DELETE SET NULL`

**Indexes:**

- `agent_prompts_pkey`: `CREATE UNIQUE INDEX agent_prompts_pkey ON public.agent_prompts USING btree (id)`
- `idx_agent_prompts__agent_id`: `CREATE INDEX idx_agent_prompts__agent_id ON public.agent_prompts USING btree (agent_id, activated_at DESC) INCLUDE (prompt, model_name, model_provider) WHERE (deleted_at IS NULL)`
- `idx_agent_prompts__agent_id_bare`: `CREATE INDEX idx_agent_prompts__agent_id_bare ON public.agent_prompts USING btree (agent_id)`

**Triggers:**

- `moderation_transparency_agent_prompts_lock`: `CREATE TRIGGER moderation_transparency_agent_prompts_lock BEFORE DELETE OR UPDATE ON public.agent_prompts FOR EACH ROW EXECUTE FUNCTION fn_lock_agent_moderation_transparency_prompt()`
- `trigger_agent_prompts_updated_at`: `CREATE TRIGGER trigger_agent_prompts_updated_at BEFORE UPDATE ON public.agent_prompts FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
