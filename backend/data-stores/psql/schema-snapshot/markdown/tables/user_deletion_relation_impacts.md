# Table `user_deletion_relation_impacts`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Affected entity relations that must be recomputed before account deletion completes.

Not partitioned — growth: unbounded.

| Column               | Type                       | Nullable | Default                      | Identity | Generated | Collation | Comment                                                                         |
| -------------------- | -------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | ------------------------------------------------------------------------------- |
| `id`                 | `uuid`                     | no       | `uuidv7()`                   |          |           |           |                                                                                 |
| `request_id`         | `uuid`                     | no       |                              |          |           |           | Owning durable deletion request.                                                |
| `relation_table`     | `text`                     | no       |                              |          |           |           | Concrete entity-relation table containing the affected relation.                |
| `entity_relation_id` | `uuid`                     | no       |                              |          |           |           | Affected entity-relation identifier; relation_table selects its concrete table. |
| `recorded_at`        | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP`          |          |           |           | Timestamp when deletion captured the relation for durable recomputation.        |
| `recomputed_at`      | `timestamp with time zone` | yes      |                              |          |           |           | Timestamp when all derived effects for the captured relation completed.         |
| `created_at`         | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                                                 |
| `updated_at`         | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP`          |          |           |           |                                                                                 |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**

- `user_deletion_relation_impact_request_id_relation_table_ent_key`: `UNIQUE (request_id, relation_table, entity_relation_id)`

**Check constraints:**

- `user_deletion_relation_impacts_relation_table_check`: `CHECK ((char_length(relation_table) <= 128))`

**Foreign keys:**

- `user_deletion_relation_impacts_request_id_fkey`: `FOREIGN KEY (request_id) REFERENCES user_deletion_requests(id) ON DELETE CASCADE`

**Indexes:**

- `idx_user_deletion_relation_impacts__pending`: `CREATE INDEX idx_user_deletion_relation_impacts__pending ON public.user_deletion_relation_impacts USING btree (request_id, id) WHERE (recomputed_at IS NULL)`
- `idx_user_deletion_relation_impacts__recomputed_audit`: `CREATE INDEX idx_user_deletion_relation_impacts__recomputed_audit ON public.user_deletion_relation_impacts USING btree (request_id, id) WHERE (recomputed_at IS NOT NULL)`
- `user_deletion_relation_impact_request_id_relation_table_ent_key`: `CREATE UNIQUE INDEX user_deletion_relation_impact_request_id_relation_table_ent_key ON public.user_deletion_relation_impacts USING btree (request_id, relation_table, entity_relation_id)`
- `user_deletion_relation_impacts_pkey`: `CREATE UNIQUE INDEX user_deletion_relation_impacts_pkey ON public.user_deletion_relation_impacts USING btree (id)`

**Triggers:**

- `trigger_user_deletion_relation_impacts_updated_at`: `CREATE TRIGGER trigger_user_deletion_relation_impacts_updated_at BEFORE UPDATE ON public.user_deletion_relation_impacts FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
