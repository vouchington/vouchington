# Table `list_items__posts`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Junction table tracking posts added to lists.

Not partitioned — growth: unbounded.

| Column        | Type                       | Nullable | Default                      | Identity | Generated | Collation | Comment                                                                              |
| ------------- | -------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | ------------------------------------------------------------------------------------ |
| `id`          | `uuid`                     | no       | `uuidv7()`                   |          |           |           |                                                                                      |
| `list_id`     | `uuid`                     | no       |                              |          |           |           | The list this item belongs to.                                                       |
| `post_id`     | `uuid`                     | no       |                              |          |           |           | The post added to the list.                                                          |
| `order_index` | `integer`                  | no       | `0`                          |          |           |           | Manual sort order within the list (lower = first).                                   |
| `created_at`  | `timestamp with time zone` | no       | `uuid_extract_timestamp(id)` |          | stored    |           |                                                                                      |
| `removed_at`  | `timestamp with time zone` | yes      |                              |          |           |           | Soft-delete timestamp; NULL means active. Re-adding after removal creates a new row. |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**
_none_

**Check constraints:**
_none_

**Foreign keys:**

- `list_items__posts_list_id_fkey`: `FOREIGN KEY (list_id) REFERENCES lists(id) ON DELETE CASCADE`
- `list_items__posts_post_id_fkey`: `FOREIGN KEY (post_id) REFERENCES posts(id) ON DELETE CASCADE`

**Indexes:**

- `idx_list_items__posts__list_id__fk`: `CREATE INDEX idx_list_items__posts__list_id__fk ON public.list_items__posts USING btree (list_id) WHERE (list_id IS NOT NULL)`
- `idx_list_items__posts__list_id__id`: `CREATE INDEX idx_list_items__posts__list_id__id ON public.list_items__posts USING btree (list_id, id DESC) WHERE (removed_at IS NULL)`
- `idx_list_items__posts__list_id__post_id`: `CREATE UNIQUE INDEX idx_list_items__posts__list_id__post_id ON public.list_items__posts USING btree (list_id, post_id) WHERE (removed_at IS NULL)`
- `idx_list_items__posts__post_id`: `CREATE INDEX idx_list_items__posts__post_id ON public.list_items__posts USING btree (post_id) WHERE (removed_at IS NULL)`
- `idx_list_items__posts__post_id__fk`: `CREATE INDEX idx_list_items__posts__post_id__fk ON public.list_items__posts USING btree (post_id) WHERE (post_id IS NOT NULL)`
- `list_items__posts_pkey`: `CREATE UNIQUE INDEX list_items__posts_pkey ON public.list_items__posts USING btree (id)`

**Triggers:**
_none_
