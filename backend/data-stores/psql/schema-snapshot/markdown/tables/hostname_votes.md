# Table `hostname_votes`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

RANGE partitioned on `hostname_id` (children: default, no retention owner, access class: target-scoped, growth: unbounded).

| Column              | Type                       | Nullable | Default                      | Identity | Generated | Collation | Comment |
| ------------------- | -------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | ------- |
| `user_id`           | `uuid`                     | no       |                              |          |           |           |         |
| `hostname_id`       | `uuid`                     | no       |                              |          |           |           |         |
| `id`                | `uuid`                     | no       | `uuidv7()`                   |          |           |           |         |
| `score`             | `smallint`                 | yes      |                              |          |           |           |         |
| `score_is_neutral`  | `boolean`                  | no       | `false`                      |          |           |           |         |
| `score_is_semantic` | `boolean`                  | no       | `false`                      |          |           |           |         |
| `ip_address`        | `inet`                     | yes      |                              |          |           |           |         |
| `device_id`         | `uuid`                     | yes      |                              |          |           |           |         |
| `session_id`        | `uuid`                     | yes      |                              |          |           |           |         |
| `user_agent_id`     | `uuid`                     | yes      |                              |          |           |           |         |
| `created_at`        | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |         |

**Primary key:** `PRIMARY KEY (hostname_id, id)`

**Unique constraints:**
_none_

**Check constraints:**

- `chk_hostname_votes_score_domain`: `CHECK (((score IS NULL) OR ((score >= '-2'::integer) AND (score <= 2))))`
- `chk_hostname_votes_score_is_neutral`: `CHECK (((NOT score_is_neutral) OR (NOT (score IS DISTINCT FROM 0))))`
- `chk_hostname_votes_score_is_semantic`: `CHECK (((NOT score_is_semantic) OR (score IS NOT NULL)))`
- `hostname_votes_score_check`: `CHECK (((score IS NULL) OR ((score >= '-2'::integer) AND (score <= 2))))`

**Foreign keys:**

- `hostname_votes_hostname_id_fkey`: `FOREIGN KEY (hostname_id) REFERENCES url_hostnames(id) ON DELETE CASCADE`
- `hostname_votes_user_agent_id_fkey`: `FOREIGN KEY (user_agent_id) REFERENCES vote_user_agents(id) ON DELETE SET NULL`
- `hostname_votes_user_id_fkey`: `FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE`

**Indexes:**

- `hostname_votes_pkey`: `CREATE UNIQUE INDEX hostname_votes_pkey ON ONLY public.hostname_votes USING btree (hostname_id, id)`
- `idx_hostname_votes__hostname_id__id`: `CREATE INDEX idx_hostname_votes__hostname_id__id ON ONLY public.hostname_votes USING btree (hostname_id, id)`
- `idx_hostname_votes__hostname_id__uid__id`: `CREATE INDEX idx_hostname_votes__hostname_id__uid__id ON ONLY public.hostname_votes USING btree (hostname_id, user_id, id DESC)`
- `idx_hostname_votes__uid__hostname_id__id`: `CREATE INDEX idx_hostname_votes__uid__hostname_id__id ON ONLY public.hostname_votes USING btree (user_id, hostname_id, id DESC)`

**Triggers:**
_none_
