# Table `oauth_authorizations`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Durable server-begun OAuth state machine shared by web and native Facebook, X, and GitHub authentication and account connection.

Not partitioned — growth: bounded.

| Column                        | Type                       | Nullable | Default                      | Identity | Generated | Collation | Comment                                                                                                                       |
| ----------------------------- | -------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | ----------------------------------------------------------------------------------------------------------------------------- |
| `id`                          | `uuid`                     | no       | `uuidv7()`                   |          |           |           |                                                                                                                               |
| `provider`                    | `text`                     | no       |                              |          |           |           | Provider selected at begin time; callback and completion derive it only from this durable record.                             |
| `purpose`                     | `text`                     | no       |                              |          |           |           | authenticate creates or resumes a Voucha session; connect links the provider account to the authenticated initiating user.    |
| `callback_mode`               | `text`                     | no       |                              |          |           |           | web uses an HttpOnly completion cookie; native uses a bearer token plus the app-held proof verifier.                          |
| `status`                      | `text`                     | no       | `'pending'::text`            |          |           |           | Explicit lifecycle: pending, callback_received, exchanging, completion_ready, completed, rejected, or expired.                |
| `initiating_user_id`          | `uuid`                     | yes      |                              |          |           |           | Required owner for connect flows and NULL for authenticate flows.                                                             |
| `initiating_device_id`        | `uuid`                     | no       |                              |          |           |           | Device JWT identifier that must still match when completion is consumed.                                                      |
| `initiating_session_id`       | `uuid`                     | no       |                              |          |           |           | Session JWT identifier that must still match when completion is consumed.                                                     |
| `redirect_uri`                | `text`                     | no       |                              |          |           |           | Server-selected public provider callback URI; callers cannot supply or override it.                                           |
| `state_hash`                  | `text`                     | no       |                              |          |           |           | Purpose-bound HMAC-SHA256 of the provider state; plaintext state is returned once and never persisted.                        |
| `pkce_verifier_ciphertext`    | `text`                     | no       |                              |          |           |           | Authenticated ciphertext of the server-generated provider PKCE verifier.                                                      |
| `completion_proof_challenge`  | `text`                     | yes      |                              |          |           |           | Native-only base64url SHA-256 challenge for the app-held completion verifier.                                                 |
| `callback_code_ciphertext`    | `text`                     | yes      |                              |          |           |           | Authenticated ciphertext of the one-time provider code; cleared only after durable provider-account persistence.              |
| `callback_error`              | `text`                     | yes      |                              |          |           |           | Bounded provider callback or terminal exchange error code; never contains provider credentials.                               |
| `completion_token_hash`       | `text`                     | yes      |                              |          |           |           | Purpose-bound HMAC-SHA256 of the web cookie or native callback bearer token.                                                  |
| `completion_token_ciphertext` | `text`                     | yes      |                              |          |           |           | Authenticated ciphertext of the completion token retained only so a repeated provider callback converges on the same handoff. |
| `facebook_user_id`            | `text`                     | yes      |                              |          |           |           | Facebook account produced by a successful exchange; parent deletion cascades the authorization.                               |
| `x_user_id`                   | `text`                     | yes      |                              |          |           |           | X account produced by a successful exchange; parent deletion cascades the authorization.                                      |
| `github_user_id`              | `text`                     | yes      |                              |          |           |           | GitHub account produced by a successful exchange; parent deletion cascades the authorization.                                 |
| `result_kind`                 | `text`                     | yes      |                              |          |           |           | Durable completion outcome: authenticated, MFA required, or connected.                                                        |
| `result_user_id`              | `uuid`                     | yes      |                              |          |           |           | Voucha user receiving the completion result; parent deletion cascades the authorization.                                      |
| `result_device_id`            | `uuid`                     | yes      |                              |          |           |           | Authenticated result device UUIDv7 allocated while the completion row is locked and reused by every issuance or replay.       |
| `result_session_id`           | `uuid`                     | yes      |                              |          |           |           | Authenticated result session UUIDv7 allocated while the completion row is locked and reused by every issuance or replay.      |
| `login_attempt_id`            | `uuid`                     | yes      |                              |          |           |           | UUIDv7 MFA attempt identifier returned only for an mfa_required result.                                                       |
| `exchange_claim_id`           | `uuid`                     | yes      |                              |          |           |           | Fencing token for one worker exchange attempt; atomic provider-account completion must present the current claim.             |
| `exchange_attempts`           | `smallint`                 | no       | `0`                          |          |           |           | Durable count of claimed provider exchange attempts for reconciliation and operator diagnosis.                                |
| `expires_at`                  | `timestamp with time zone` | no       |                              |          |           |           | Authorization deadline; cleanup and recovery use this timestamp rather than UUID ordering.                                    |
| `callback_received_at`        | `timestamp with time zone` | yes      |                              |          |           |           | When the provider callback first durably persisted a code or denial.                                                          |
| `exchange_started_at`         | `timestamp with time zone` | yes      |                              |          |           |           | When the current fenced provider exchange claim began.                                                                        |
| `completion_ready_at`         | `timestamp with time zone` | yes      |                              |          |           |           | When provider identity persistence completed and the result became consumable.                                                |
| `completed_at`                | `timestamp with time zone` | yes      |                              |          |           |           | When a caller first consumed and finalized the durable completion result.                                                     |
| `created_at`                  | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                                                                                               |
| `updated_at`                  | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP`          |          |           |           |                                                                                                                               |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**
_none_

**Check constraints:**

- `oauth_authorizations_callback_mode_check`: `CHECK ((callback_mode = ANY (ARRAY['web'::text, 'native'::text])))`
- `oauth_authorizations_check`: `CHECK ((((purpose = 'authenticate'::text) AND (initiating_user_id IS NULL)) OR ((purpose = 'connect'::text) AND (initiating_user_id IS NOT NULL))))`
- `oauth_authorizations_check1`: `CHECK ((((callback_mode = 'native'::text) AND (completion_proof_challenge ~ '^[A-Za-z0-9_-]{43}$'::text)) OR ((callback_mode = 'web'::text) AND (completion_proof_challenge IS NULL))))`
- `oauth_authorizations_check2`: `CHECK ((num_nonnulls(facebook_user_id, x_user_id, github_user_id) <= 1))`
- `oauth_authorizations_check3`: `CHECK ((((provider = 'facebook'::text) AND (x_user_id IS NULL) AND (github_user_id IS NULL)) OR ((provider = 'x'::text) AND (facebook_user_id IS NULL) AND (github_user_id IS NULL)) OR ((provider = 'github'::text) AND (facebook_user_id IS NULL) AND (x_user_id IS NULL))))`
- `oauth_authorizations_check4`: `CHECK (((status <> ALL (ARRAY['callback_received'::text, 'exchanging'::text, 'completion_ready'::text, 'completed'::text])) OR (callback_received_at IS NOT NULL)))`
- `oauth_authorizations_check5`: `CHECK (((status <> ALL (ARRAY['completion_ready'::text, 'completed'::text])) OR ((completion_ready_at IS NOT NULL) AND (callback_code_ciphertext IS NULL) AND (num_nonnulls(facebook_user_id, x_user_id, github_user_id) = 1))))`
- `oauth_authorizations_check6`: `CHECK ((((status <> 'completed'::text) AND (completed_at IS NULL) AND (result_kind IS NULL) AND (result_user_id IS NULL) AND (result_device_id IS NULL) AND (result_session_id IS NULL) AND (login_attempt_id IS NULL)) OR ((status = 'completed'::text) AND (completed_at IS NOT NULL) AND (result_user_id IS NOT NULL) AND (((result_kind = 'authenticated'::text) AND (result_device_id IS NOT NULL) AND (uuid_extract_version(result_device_id) = 7) AND (result_session_id IS NOT NULL) AND (uuid_extract_version(result_session_id) = 7) AND (login_attempt_id IS NULL)) OR ((result_kind = 'mfa_required'::text) AND (result_device_id IS NULL) AND (result_session_id IS NULL) AND (login_attempt_id IS NOT NULL) AND (uuid_extract_version(login_attempt_id) = 7)) OR ((result_kind = 'connected'::text) AND (result_device_id IS NULL) AND (result_session_id IS NULL) AND (login_attempt_id IS NULL))))))`
- `oauth_authorizations_exchange_attempts_check`: `CHECK ((exchange_attempts >= 0))`
- `oauth_authorizations_provider_check`: `CHECK ((provider = ANY (ARRAY['facebook'::text, 'x'::text, 'github'::text])))`
- `oauth_authorizations_purpose_check`: `CHECK ((purpose = ANY (ARRAY['authenticate'::text, 'connect'::text])))`
- `oauth_authorizations_redirect_uri_check`: `CHECK (((char_length(redirect_uri) >= 1) AND (char_length(redirect_uri) <= 2048)))`
- `oauth_authorizations_result_kind_check`: `CHECK (((result_kind IS NULL) OR (result_kind = ANY (ARRAY['authenticated'::text, 'mfa_required'::text, 'connected'::text]))))`
- `oauth_authorizations_state_hash_check`: `CHECK ((char_length(state_hash) = 64))`
- `oauth_authorizations_status_check`: `CHECK ((status = ANY (ARRAY['pending'::text, 'callback_received'::text, 'exchanging'::text, 'completion_ready'::text, 'completed'::text, 'rejected'::text, 'expired'::text])))`

**Foreign keys:**

- `oauth_authorizations_facebook_user_id_fkey`: `FOREIGN KEY (facebook_user_id) REFERENCES facebook_accounts(facebook_user_id) ON DELETE CASCADE`
- `oauth_authorizations_github_user_id_fkey`: `FOREIGN KEY (github_user_id) REFERENCES github_accounts(github_user_id) ON DELETE CASCADE`
- `oauth_authorizations_initiating_user_id_fkey`: `FOREIGN KEY (initiating_user_id) REFERENCES users(id) ON DELETE CASCADE`
- `oauth_authorizations_result_user_id_fkey`: `FOREIGN KEY (result_user_id) REFERENCES users(id) ON DELETE CASCADE`
- `oauth_authorizations_x_user_id_fkey`: `FOREIGN KEY (x_user_id) REFERENCES x_accounts(x_user_id) ON DELETE CASCADE`

**Indexes:**

- `idx_oauth_authorizations__active_web_authentication`: `CREATE UNIQUE INDEX idx_oauth_authorizations__active_web_authentication ON public.oauth_authorizations USING btree (initiating_device_id, initiating_session_id) WHERE ((callback_mode = 'web'::text) AND (purpose = 'authenticate'::text) AND (status = ANY (ARRAY['pending'::text, 'callback_received'::text, 'exchanging'::text, 'completion_ready'::text])))`
- `idx_oauth_authorizations__expiry`: `CREATE INDEX idx_oauth_authorizations__expiry ON public.oauth_authorizations USING btree (expires_at, id)`
- `idx_oauth_authorizations__facebook_user`: `CREATE INDEX idx_oauth_authorizations__facebook_user ON public.oauth_authorizations USING btree (facebook_user_id) WHERE (facebook_user_id IS NOT NULL)`
- `idx_oauth_authorizations__github_user`: `CREATE INDEX idx_oauth_authorizations__github_user ON public.oauth_authorizations USING btree (github_user_id) WHERE (github_user_id IS NOT NULL)`
- `idx_oauth_authorizations__initiating_user`: `CREATE INDEX idx_oauth_authorizations__initiating_user ON public.oauth_authorizations USING btree (initiating_user_id, id) WHERE (initiating_user_id IS NOT NULL)`
- `idx_oauth_authorizations__recoverable`: `CREATE INDEX idx_oauth_authorizations__recoverable ON public.oauth_authorizations USING btree (status, expires_at, id) WHERE (status = ANY (ARRAY['callback_received'::text, 'exchanging'::text]))`
- `idx_oauth_authorizations__result_user`: `CREATE INDEX idx_oauth_authorizations__result_user ON public.oauth_authorizations USING btree (result_user_id) WHERE (result_user_id IS NOT NULL)`
- `idx_oauth_authorizations__state_hash`: `CREATE UNIQUE INDEX idx_oauth_authorizations__state_hash ON public.oauth_authorizations USING btree (state_hash)`
- `idx_oauth_authorizations__x_user`: `CREATE INDEX idx_oauth_authorizations__x_user ON public.oauth_authorizations USING btree (x_user_id) WHERE (x_user_id IS NOT NULL)`
- `oauth_authorizations_pkey`: `CREATE UNIQUE INDEX oauth_authorizations_pkey ON public.oauth_authorizations USING btree (id)`

**Triggers:**

- `trigger_oauth_authorizations_updated_at`: `CREATE TRIGGER trigger_oauth_authorizations_updated_at BEFORE UPDATE ON public.oauth_authorizations FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
