# Table `post_moderation_work_items`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Durable current work projection with generation and lease-token fencing for bounded moderation attempts.

Not partitioned — growth: unbounded.

| Column             | Type                       | Nullable | Default             | Identity | Generated | Collation | Comment                                                                      |
| ------------------ | -------------------------- | -------- | ------------------- | -------- | --------- | --------- | ---------------------------------------------------------------------------- |
| `version_id`       | `uuid`                     | no       |                     |          |           |           | Moderation version whose current source work this row coordinates.           |
| `source`           | `post_moderation_sources`  | no       |                     |          |           |           | Automated or staff moderation source with one current work item per version. |
| `generation`       | `bigint`                   | no       | `1`                 |          |           |           | Monotonic work generation fencing retries and stale worker writes.           |
| `available_at`     | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           | Earliest time a worker may claim this source work item.                      |
| `leased_at`        | `timestamp with time zone` | yes      |                     |          |           |           | Clock time at which the current worker lease began.                          |
| `lease_token`      | `uuid`                     | yes      |                     |          |           |           | Unique fencing token required to complete or fail the current worker lease.  |
| `lease_expires_at` | `timestamp with time zone` | yes      |                     |          |           |           | Clock time after which another worker may reclaim the current lease.         |
| `completed_at`     | `timestamp with time zone` | yes      |                     |          |           |           | Clock time at which this source work reached a terminal disposition.         |

**Primary key:** `PRIMARY KEY (version_id, source)`

**Unique constraints:**

- `post_moderation_work_items_lease_token_key`: `UNIQUE (lease_token)`

**Check constraints:**

- `post_moderation_work_items_check`: `CHECK (((lease_token IS NULL) = (leased_at IS NULL)))`
- `post_moderation_work_items_check1`: `CHECK (((lease_token IS NULL) = (lease_expires_at IS NULL)))`
- `post_moderation_work_items_check2`: `CHECK (((completed_at IS NULL) OR (lease_token IS NULL)))`
- `post_moderation_work_items_generation_check`: `CHECK ((generation > 0))`

**Foreign keys:**

- `post_moderation_work_items_version_id_fkey`: `FOREIGN KEY (version_id) REFERENCES post_moderation_versions(id) ON DELETE CASCADE`

**Indexes:**

- `idx_post_moderation_work_items__due`: `CREATE INDEX idx_post_moderation_work_items__due ON public.post_moderation_work_items USING btree (available_at, version_id, source) WHERE (completed_at IS NULL)`
- `post_moderation_work_items_lease_token_key`: `CREATE UNIQUE INDEX post_moderation_work_items_lease_token_key ON public.post_moderation_work_items USING btree (lease_token)`
- `post_moderation_work_items_pkey`: `CREATE UNIQUE INDEX post_moderation_work_items_pkey ON public.post_moderation_work_items USING btree (version_id, source)`

**Triggers:**
_none_
