# Table `crawlers`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Per-hostname crawler configurations defining how to fetch and parse web pages.

Not partitioned — growth: unbounded.

| Column                        | Type                       | Nullable | Default                      | Identity | Generated | Collation | Comment                                                                                                                                                  |
| ----------------------------- | -------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `id`                          | `uuid`                     | no       | `uuidv7()`                   |          |           |           |                                                                                                                                                          |
| `hostname_id`                 | `uuid`                     | no       |                              |          |           |           | The hostname this crawler is configured for.                                                                                                             |
| `description`                 | `text`                     | no       | `''::text`                   |          |           |           | Human-readable description of what this crawler does.                                                                                                    |
| `crawler_type`                | `crawler_types`            | no       |                              |          |           |           | Method of crawling: fetch (HTTP) or automation (headless browser).                                                                                       |
| `priority`                    | `integer`                  | no       | `0`                          |          |           |           | Selection priority (descending). Higher priority crawlers are tried first.                                                                               |
| `created_at`                  | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                                                                                                                          |
| `created_by_id`               | `uuid`                     | yes      |                              |          |           |           |                                                                                                                                                          |
| `updated_at`                  | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP`          |          |           |           |                                                                                                                                                          |
| `updated_by_id`               | `uuid`                     | yes      |                              |          |           |           |                                                                                                                                                          |
| `deleted_at`                  | `timestamp with time zone` | yes      |                              |          |           |           |                                                                                                                                                          |
| `deleted_by_id`               | `uuid`                     | yes      |                              |          |           |           |                                                                                                                                                          |
| `css_selectors_to_remove`     | `text[]`                   | no       | `ARRAY[]::text[]`            |          |           |           | CSS selectors of elements to strip before parsing HTML content.                                                                                          |
| `link_text_content_to_remove` | `text[]`                   | no       | `ARRAY[]::text[]`            |          |           |           | Link text values to strip (exact match) before parsing.                                                                                                  |
| `link_hrefs_to_remove`        | `text[]`                   | no       | `ARRAY[]::text[]`            |          |           |           | Link href values to strip (exact match) before parsing.                                                                                                  |
| `content_selectors`           | `text[]`                   | no       | `ARRAY[]::text[]`            |          |           |           | CSS selectors that narrow HTML parsing to specific content areas.                                                                                        |
| `referral_program_id`         | `uuid`                     | yes      |                              |          |           |           | Optional FK linking this crawler to a referral program. When set, this crawler overrides the hostname default for that program's referral link crawling. |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**
_none_

**Check constraints:**
_none_

**Foreign keys:**

- `crawlers_created_by_id_fkey`: `FOREIGN KEY (created_by_id) REFERENCES users(id) ON DELETE CASCADE`
- `crawlers_deleted_by_id_fkey`: `FOREIGN KEY (deleted_by_id) REFERENCES users(id) ON DELETE SET NULL`
- `crawlers_hostname_id_fkey`: `FOREIGN KEY (hostname_id) REFERENCES url_hostnames(id) ON DELETE CASCADE`
- `crawlers_updated_by_id_fkey`: `FOREIGN KEY (updated_by_id) REFERENCES users(id) ON DELETE SET NULL`
- `fk_crawlers_referral_program_id`: `FOREIGN KEY (referral_program_id) REFERENCES topics__referral_programs(topic_id) ON DELETE SET NULL`

**Indexes:**

- `crawlers_pkey`: `CREATE UNIQUE INDEX crawlers_pkey ON public.crawlers USING btree (id)`
- `idx_crawlers__created_by_id`: `CREATE INDEX idx_crawlers__created_by_id ON public.crawlers USING btree (created_by_id) WHERE (created_by_id IS NOT NULL)`
- `idx_crawlers__hostname_id`: `CREATE INDEX idx_crawlers__hostname_id ON public.crawlers USING btree (hostname_id)`
- `idx_crawlers__referral_program_id`: `CREATE INDEX idx_crawlers__referral_program_id ON public.crawlers USING btree (referral_program_id) WHERE (referral_program_id IS NOT NULL)`

**Triggers:**

- `trigger_crawlers_updated_at`: `CREATE TRIGGER trigger_crawlers_updated_at BEFORE UPDATE ON public.crawlers FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
