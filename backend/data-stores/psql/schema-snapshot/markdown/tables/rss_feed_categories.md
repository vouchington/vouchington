# Table `rss_feed_categories`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Maps feed-level Apple itunes:category values to internal topics. One row per (feed, category_text) pair.

Not partitioned — growth: unbounded.

| Column          | Type                       | Nullable | Default             | Identity | Generated | Collation | Comment                                                                    |
| --------------- | -------------------------- | -------- | ------------------- | -------- | --------- | --------- | -------------------------------------------------------------------------- |
| `rss_feed_id`   | `uuid`                     | no       |                     |          |           |           | The RSS feed (podcast show) these categories belong to.                    |
| `category_text` | `text`                     | no       |                     |          |           |           | Normalized (trimmed, lowercased) Apple itunes:category text from the feed. |
| `topic_id`      | `uuid`                     | yes      |                     |          |           |           | The mapped internal topic. NULL if not yet mapped by the autotagger.       |
| `created_at`    | `timestamp with time zone` | yes      | `CURRENT_TIMESTAMP` |          |           |           |                                                                            |
| `updated_at`    | `timestamp with time zone` | yes      | `CURRENT_TIMESTAMP` |          |           |           |                                                                            |

**Primary key:** `PRIMARY KEY (rss_feed_id, category_text)`

**Unique constraints:**
_none_

**Check constraints:**
_none_

**Foreign keys:**

- `rss_feed_categories_rss_feed_id_fkey`: `FOREIGN KEY (rss_feed_id) REFERENCES rss_feeds(id) ON DELETE CASCADE`
- `rss_feed_categories_topic_id_fkey`: `FOREIGN KEY (topic_id) REFERENCES topics(id) ON DELETE SET NULL`

**Indexes:**

- `idx_rss_feed_categories__category_text`: `CREATE INDEX idx_rss_feed_categories__category_text ON public.rss_feed_categories USING btree (category_text)`
- `idx_rss_feed_categories__topic_id`: `CREATE INDEX idx_rss_feed_categories__topic_id ON public.rss_feed_categories USING btree (topic_id, rss_feed_id DESC) WHERE (topic_id IS NOT NULL)`
- `rss_feed_categories_pkey`: `CREATE UNIQUE INDEX rss_feed_categories_pkey ON public.rss_feed_categories USING btree (rss_feed_id, category_text)`

**Triggers:**

- `trigger_rss_feed_categories_updated_at`: `CREATE TRIGGER trigger_rss_feed_categories_updated_at BEFORE UPDATE ON public.rss_feed_categories FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
