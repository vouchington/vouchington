# Table `rss_feed_item_ids`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

RSS feed item identity lookup. Unique per (url_hostname_id, guid): same GUID on same feed domain = same item, regardless of which feed URL discovered it.

Not partitioned — growth: unbounded.

| Column            | Type   | Nullable | Default    | Identity | Generated | Collation | Comment                                                                                                    |
| ----------------- | ------ | -------- | ---------- | -------- | --------- | --------- | ---------------------------------------------------------------------------------------------------------- |
| `id`              | `uuid` | no       | `uuidv7()` |          |           |           |                                                                                                            |
| `guid`            | `text` | no       |            |          |           |           | The GUID/ID as defined by the RSS feed, for deduplication within the same hostname.                        |
| `url_hostname_id` | `uuid` | no       |            |          |           |           | The hostname of the RSS feed URL. Combined with guid for dedup: GUIDs are unique per domain, not globally. |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**

- `rss_feed_item_ids_url_hostname_id_guid_id_key`: `UNIQUE (url_hostname_id, guid) INCLUDE (id)`

**Check constraints:**

- `rss_feed_item_ids_guid_check`: `CHECK ((guid = TRIM(BOTH FROM guid)))`

**Foreign keys:**

- `rss_feed_item_ids_url_hostname_id_fkey`: `FOREIGN KEY (url_hostname_id) REFERENCES url_hostnames(id) ON DELETE CASCADE`

**Indexes:**

- `rss_feed_item_ids_pkey`: `CREATE UNIQUE INDEX rss_feed_item_ids_pkey ON public.rss_feed_item_ids USING btree (id)`
- `rss_feed_item_ids_url_hostname_id_guid_id_key`: `CREATE UNIQUE INDEX rss_feed_item_ids_url_hostname_id_guid_id_key ON public.rss_feed_item_ids USING btree (url_hostname_id, guid) INCLUDE (id)`

**Triggers:**
_none_
