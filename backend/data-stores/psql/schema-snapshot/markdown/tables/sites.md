# Table `sites`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Top-level content sites/verticals. Each site groups topics and content under a slug.

Not partitioned — growth: unbounded.

| Column       | Type                       | Nullable | Default             | Identity | Generated | Collation | Comment                                                          |
| ------------ | -------------------------- | -------- | ------------------- | -------- | --------- | --------- | ---------------------------------------------------------------- |
| `slug`       | `text`                     | no       |                     |          |           |           | URL-safe lowercase identifier for the site (e.g. rewards, cars). |
| `created_at` | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                                  |
| `updated_at` | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                                  |

**Primary key:** `PRIMARY KEY (slug)`

**Unique constraints:**
_none_

**Check constraints:**

- `sites_slug_check`: `CHECK ((char_length(slug) <= 255))`
- `sites_slug_check1`: `CHECK ((slug = lower(slug)))`
- `sites_slug_check2`: `CHECK ((slug = TRIM(BOTH FROM slug)))`

**Foreign keys:**
_none_

**Indexes:**

- `sites_pkey`: `CREATE UNIQUE INDEX sites_pkey ON public.sites USING btree (slug)`

**Triggers:**

- `trigger_sites_updated_at`: `CREATE TRIGGER trigger_sites_updated_at BEFORE UPDATE ON public.sites FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
