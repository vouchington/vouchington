# Table `images`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Uploaded images with S3 storage, metadata, and moderation results.

Not partitioned — growth: unbounded.

| Column                                            | Type                       | Nullable | Default                      | Identity | Generated | Collation | Comment                                                                                                                          |
| ------------------------------------------------- | -------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | -------------------------------------------------------------------------------------------------------------------------------- |
| `id`                                              | `uuid`                     | no       | `uuidv7()`                   |          |           |           |                                                                                                                                  |
| `created_by_id`                                   | `uuid`                     | no       |                              |          |           |           |                                                                                                                                  |
| `created_at`                                      | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                                                                                                  |
| `updated_at`                                      | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP`          |          |           |           |                                                                                                                                  |
| `deleted_at`                                      | `timestamp with time zone` | yes      |                              |          |           |           |                                                                                                                                  |
| `deleted_by_id`                                   | `uuid`                     | yes      |                              |          |           |           |                                                                                                                                  |
| `data`                                            | `jsonb`                    | no       |                              |          |           |           | Raw sharp (image processing library) metadata as JSONB.                                                                          |
| `sha_256`                                         | `bytea`                    | yes      |                              |          |           |           | SHA-256 hash of the original image file bytes. NULL until completion freezes and hashes the staged upload.                       |
| `s3_key`                                          | `text`                     | no       |                              |          |           |           | S3 object key where the image is stored.                                                                                         |
| `upload_started_at`                               | `timestamp with time zone` | yes      |                              |          |           |           | When upload processing began (S3 object received and hash calculation started). NULL = pending.                                  |
| `upload_completed_at`                             | `timestamp with time zone` | yes      |                              |          |           |           | When upload processing finished successfully. NULL until complete.                                                               |
| `upload_failed_at`                                | `timestamp with time zone` | yes      |                              |          |           |           | When upload processing failed terminally. NULL until failed.                                                                     |
| `upload_error`                                    | `text`                     | yes      |                              |          |           |           | Error message if upload processing failed.                                                                                       |
| `openai_omni_moderation_results`                  | `jsonb`                    | yes      |                              |          |           |           | Raw JSONB results from OpenAI omni moderation API.                                                                               |
| `openai_omni_moderation_flagged`                  | `boolean`                  | yes      |                              |          |           |           | Whether OpenAI moderation flagged this image.                                                                                    |
| `openai_omni_moderation_created_at`               | `timestamp with time zone` | yes      |                              |          |           |           | When the moderation check was performed.                                                                                         |
| `quarantine_pending_at`                           | `timestamp with time zone` | yes      |                              |          |           |           | When CSAM quarantine began. Pending images are unavailable until permanent copy and deletion complete.                           |
| `quarantined_at`                                  | `timestamp with time zone` | yes      |                              |          |           |           | When the image was copied to the CSAM quarantine bucket. NULL until the permanent copy succeeds.                                 |
| `quarantine_s3_key`                               | `text`                     | yes      |                              |          |           |           | S3 key in the permanent quarantine bucket. NULL until the copy succeeds.                                                         |
| `bedrock_nova_multimodal_v1_embedding`            | `vector(1024)`             | yes      |                              |          |           |           | 1024-dimensional vector from Amazon Nova 2 Multimodal Embeddings V1 for the image.                                               |
| `bedrock_nova_multimodal_v1_embedding_created_at` | `timestamp with time zone` | yes      |                              |          |           |           | When the image embedding was generated.                                                                                          |
| `upload_staged_at`                                | `timestamp with time zone` | yes      |                              |          |           |           | When the private browser upload staging source was created.                                                                      |
| `upload_source_deleted_at`                        | `timestamp with time zone` | yes      |                              |          |           |           | Durable evidence that the browser-writable source object was deleted. NULL keeps the source eligible for bounded reconciliation. |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**

- `images_sha_256_key`: `UNIQUE (sha_256)`

**Check constraints:**

- `chk_images__completed_requires_sha_256`: `CHECK (((upload_completed_at IS NULL) OR (sha_256 IS NOT NULL)))`
- `chk_images__upload_lifecycle`: `CHECK ((((upload_started_at IS NOT NULL) OR ((upload_completed_at IS NULL) AND (upload_failed_at IS NULL))) AND ((upload_completed_at IS NULL) OR (upload_failed_at IS NULL)) AND ((upload_completed_at IS NULL) OR (upload_error IS NULL)) AND ((upload_failed_at IS NULL) OR (upload_error IS NOT NULL))))`
- `images_sha_256_check`: `CHECK ((octet_length(sha_256) = 32))`

**Foreign keys:**

- `images_created_by_id_fkey`: `FOREIGN KEY (created_by_id) REFERENCES users(id) ON DELETE CASCADE`
- `images_deleted_by_id_fkey`: `FOREIGN KEY (deleted_by_id) REFERENCES users(id) ON DELETE SET NULL`

**Indexes:**

- `idx_images__bedrock_nova_multimodal_v1_embedding`: `CREATE INDEX idx_images__bedrock_nova_multimodal_v1_embedding ON public.images USING hnsw (bedrock_nova_multimodal_v1_embedding vector_cosine_ops) WHERE (bedrock_nova_multimodal_v1_embedding IS NOT NULL)`
- `idx_images__created_by_id`: `CREATE INDEX idx_images__created_by_id ON public.images USING btree (created_by_id) WHERE (created_by_id IS NOT NULL)`
- `idx_images__quarantine_pending`: `CREATE INDEX idx_images__quarantine_pending ON public.images USING btree (quarantine_pending_at, id) WHERE ((quarantine_pending_at IS NOT NULL) AND (deleted_at IS NULL))`
- `idx_images__staged_source_cleanup`: `CREATE INDEX idx_images__staged_source_cleanup ON public.images USING btree (id) WHERE ((upload_staged_at IS NOT NULL) AND (upload_source_deleted_at IS NULL))`
- `idx_images__updated_at_id_active`: `CREATE INDEX idx_images__updated_at_id_active ON public.images USING btree (updated_at, id) WHERE ((deleted_at IS NULL) AND (upload_completed_at IS NOT NULL))`
- `idx_images__upload_in_flight`: `CREATE INDEX idx_images__upload_in_flight ON public.images USING btree (id) WHERE ((upload_completed_at IS NULL) AND (upload_failed_at IS NULL))`
- `images_pkey`: `CREATE UNIQUE INDEX images_pkey ON public.images USING btree (id)`
- `images_sha_256_key`: `CREATE UNIQUE INDEX images_sha_256_key ON public.images USING btree (sha_256)`

**Triggers:**

- `trigger_images_guard_terminal_lifecycle`: `CREATE TRIGGER trigger_images_guard_terminal_lifecycle BEFORE UPDATE ON public.images FOR EACH ROW EXECUTE FUNCTION fn_guard_terminal_lifecycle('upload_completed_at', 'upload_failed_at')`
- `trigger_images_updated_at`: `CREATE TRIGGER trigger_images_updated_at BEFORE UPDATE ON public.images FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
