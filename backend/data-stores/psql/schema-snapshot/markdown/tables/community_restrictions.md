# Table `community_restrictions`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Tracks temporary community-wide moderation restrictions for Raid Mode. NULL lifted_at means the restriction was not manually lifted; expires_at may still make it inactive.

Not partitioned — growth: unbounded.

| Column             | Type                          | Nullable | Default                      | Identity | Generated | Collation | Comment                                                                                 |
| ------------------ | ----------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | --------------------------------------------------------------------------------------- |
| `id`               | `uuid`                        | no       | `uuidv7()`                   |          |           |           |                                                                                         |
| `community_id`     | `uuid`                        | no       |                              |          |           |           | Community this restriction applies to.                                                  |
| `restriction_type` | `community_restriction_types` | no       |                              |          |           |           | Restriction behavior enforced while active.                                             |
| `activated_by_id`  | `uuid`                        | yes      |                              |          |           |           | Moderator, owner, or admin who activated the restriction.                               |
| `activated_at`     | `timestamp with time zone`    | no       | `CURRENT_TIMESTAMP`          |          |           |           | When the restriction was activated.                                                     |
| `expires_at`       | `timestamp with time zone`    | yes      |                              |          |           |           | When the restriction expires. NULL means manual lift only.                              |
| `created_at`       | `timestamp with time zone`    | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                                                         |
| `updated_at`       | `timestamp with time zone`    | yes      | `CURRENT_TIMESTAMP`          |          |           |           | When the restriction row was last updated.                                              |
| `lifted_at`        | `timestamp with time zone`    | yes      |                              |          |           |           | When a moderator manually lifted the restriction early. NULL means not manually lifted. |
| `lifted_by_id`     | `uuid`                        | yes      |                              |          |           |           | Who lifted the restriction early.                                                       |
| `reason`           | `text`                        | yes      |                              |          |           |           | Optional reason recorded for moderation history.                                        |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**
_none_

**Check constraints:**

- `community_restrictions_reason_check`: `CHECK (((reason IS NULL) OR (char_length(reason) <= 1000)))`
- `community_restrictions_reason_check1`: `CHECK (((reason IS NULL) OR (reason = TRIM(BOTH FROM reason))))`

**Foreign keys:**

- `community_restrictions_activated_by_id_fkey`: `FOREIGN KEY (activated_by_id) REFERENCES users(id) ON DELETE SET NULL`
- `community_restrictions_community_id_fkey`: `FOREIGN KEY (community_id) REFERENCES communities(id) ON DELETE CASCADE`
- `community_restrictions_lifted_by_id_fkey`: `FOREIGN KEY (lifted_by_id) REFERENCES users(id) ON DELETE SET NULL`

**Indexes:**

- `community_restrictions_pkey`: `CREATE UNIQUE INDEX community_restrictions_pkey ON public.community_restrictions USING btree (id)`
- `idx_community_restrictions__active_lookup`: `CREATE INDEX idx_community_restrictions__active_lookup ON public.community_restrictions USING btree (community_id, restriction_type) WHERE (lifted_at IS NULL)`
- `idx_community_restrictions__community__id`: `CREATE INDEX idx_community_restrictions__community__id ON public.community_restrictions USING btree (community_id, id DESC)`

**Triggers:**

- `trigger_community_restrictions_updated_at`: `CREATE TRIGGER trigger_community_restrictions_updated_at BEFORE UPDATE ON public.community_restrictions FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
