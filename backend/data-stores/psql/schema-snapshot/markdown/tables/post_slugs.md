# Table `post_slugs`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

URL slugs for posts. The most recently created slug is canonical; older slugs redirect.

Not partitioned — growth: unbounded.

| Column       | Type                       | Nullable | Default             | Identity | Generated | Collation | Comment                                                         |
| ------------ | -------------------------- | -------- | ------------------- | -------- | --------- | --------- | --------------------------------------------------------------- |
| `post_id`    | `uuid`                     | no       |                     |          |           |           | The post this slug belongs to.                                  |
| `slug`       | `text`                     | no       |                     |          |           |           | URL-safe lowercase slug (primary key). Unique across all posts. |
| `created_at` | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                                 |
| `updated_at` | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                                 |

**Primary key:** `PRIMARY KEY (slug)`

**Unique constraints:**
_none_

**Check constraints:**

- `post_slugs_slug_check`: `CHECK ((char_length(slug) <= 255))`
- `post_slugs_slug_check1`: `CHECK ((slug = lower(slug)))`
- `post_slugs_slug_check2`: `CHECK ((slug = TRIM(BOTH FROM slug)))`

**Foreign keys:**

- `post_slugs_post_id_fkey`: `FOREIGN KEY (post_id) REFERENCES posts(id) ON DELETE CASCADE`

**Indexes:**

- `idx_post_slugs__post_id__created_at_desc`: `CREATE INDEX idx_post_slugs__post_id__created_at_desc ON public.post_slugs USING btree (post_id, created_at DESC) INCLUDE (slug)`
- `post_slugs_pkey`: `CREATE UNIQUE INDEX post_slugs_pkey ON public.post_slugs USING btree (slug)`

**Triggers:**

- `trigger_post_slugs_updated_at`: `CREATE TRIGGER trigger_post_slugs_updated_at BEFORE UPDATE ON public.post_slugs FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
