# Table `web_push_endpoint_owners`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Global exact browser endpoint owner; SHA-256 digest serializes endpoint claims while exact endpoint comparison detects digest collisions.

Not partitioned — growth: unbounded.

| Column            | Type                       | Nullable | Default             | Identity | Generated | Collation | Comment                                                                       |
| ----------------- | -------------------------- | -------- | ------------------- | -------- | --------- | --------- | ----------------------------------------------------------------------------- |
| `endpoint_digest` | `bytea`                    | no       |                     |          |           |           | SHA-256 digest of endpoint, used as the global registry primary key.          |
| `endpoint`        | `text`                     | no       |                     |          |           |           | Canonical HTTPS browser push endpoint whose ownership is globally serialized. |
| `user_id`         | `uuid`                     | no       |                     |          |           |           | Authenticated user who owns the current endpoint activation generation.       |
| `subscription_id` | `uuid`                     | no       |                     |          |           |           | Current web push activation generation for this endpoint.                     |
| `created_at`      | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                                               |
| `updated_at`      | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                                               |

**Primary key:** `PRIMARY KEY (endpoint_digest)`

**Unique constraints:**

- `web_push_endpoint_owners_endpoint_key`: `UNIQUE (endpoint)`
- `web_push_endpoint_owners_user_id_subscription_id_key`: `UNIQUE (user_id, subscription_id)`

**Check constraints:**

- `web_push_endpoint_owners_check`: `CHECK ((endpoint_digest = digest(endpoint, 'sha256'::text)))`
- `web_push_endpoint_owners_endpoint_check`: `CHECK ((endpoint ~~ 'https://%'::text))`

**Foreign keys:**

- `web_push_endpoint_owners_subscription_fkey`: `FOREIGN KEY (user_id, subscription_id) REFERENCES web_push_subscriptions(user_id, id) ON UPDATE CASCADE ON DELETE CASCADE`

**Indexes:**

- `web_push_endpoint_owners_endpoint_key`: `CREATE UNIQUE INDEX web_push_endpoint_owners_endpoint_key ON public.web_push_endpoint_owners USING btree (endpoint)`
- `web_push_endpoint_owners_pkey`: `CREATE UNIQUE INDEX web_push_endpoint_owners_pkey ON public.web_push_endpoint_owners USING btree (endpoint_digest)`
- `web_push_endpoint_owners_user_id_subscription_id_key`: `CREATE UNIQUE INDEX web_push_endpoint_owners_user_id_subscription_id_key ON public.web_push_endpoint_owners USING btree (user_id, subscription_id)`

**Triggers:**

- `trigger_assert_web_push_owner_subscription`: `CREATE CONSTRAINT TRIGGER trigger_assert_web_push_owner_subscription AFTER INSERT OR DELETE OR UPDATE ON public.web_push_endpoint_owners DEFERRABLE INITIALLY DEFERRED FOR EACH ROW EXECUTE FUNCTION fn_assert_web_push_endpoint_owner_subscription()`
- `trigger_web_push_endpoint_owners_updated_at`: `CREATE TRIGGER trigger_web_push_endpoint_owners_updated_at BEFORE UPDATE ON public.web_push_endpoint_owners FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
