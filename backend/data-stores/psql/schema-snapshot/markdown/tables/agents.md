# Table `agents`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

LLM-powered agents (moderators, autotaggers, etc.) that operate as system users.

Not partitioned — growth: unbounded.

| Column           | Type                       | Nullable | Default                      | Identity | Generated | Collation | Comment                                                      |
| ---------------- | -------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | ------------------------------------------------------------ |
| `id`             | `uuid`                     | no       | `uuidv7()`                   |          |           |           |                                                              |
| `system_user_id` | `uuid`                     | no       |                              |          |           |           | The user account this agent operates as. One-to-one.         |
| `agent_type`     | `agent_types`              | no       |                              |          |           |           | Type of agent: moderator or autotagger.                      |
| `activated_at`   | `timestamp with time zone` | yes      |                              |          |           |           | When the agent was activated. NULL if currently deactivated. |
| `deactivated_at` | `timestamp with time zone` | yes      |                              |          |           |           | When the agent was deactivated. NULL if currently active.    |
| `created_at`     | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                              |
| `created_by_id`  | `uuid`                     | yes      |                              |          |           |           |                                                              |
| `updated_at`     | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP`          |          |           |           |                                                              |
| `updated_by_id`  | `uuid`                     | yes      |                              |          |           |           |                                                              |
| `deleted_at`     | `timestamp with time zone` | yes      |                              |          |           |           |                                                              |
| `deleted_by_id`  | `uuid`                     | yes      |                              |          |           |           |                                                              |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**

- `agents_system_user_id_key`: `UNIQUE (system_user_id)`

**Check constraints:**

- `agents_check`: `CHECK ((NOT ((activated_at IS NOT NULL) AND (deactivated_at IS NOT NULL))))`

**Foreign keys:**

- `agents_created_by_id_fkey`: `FOREIGN KEY (created_by_id) REFERENCES users(id) ON DELETE SET NULL`
- `agents_deleted_by_id_fkey`: `FOREIGN KEY (deleted_by_id) REFERENCES users(id) ON DELETE SET NULL`
- `agents_system_user_id_fkey`: `FOREIGN KEY (system_user_id) REFERENCES users(id) ON DELETE CASCADE`
- `agents_updated_by_id_fkey`: `FOREIGN KEY (updated_by_id) REFERENCES users(id) ON DELETE SET NULL`

**Indexes:**

- `agents_pkey`: `CREATE UNIQUE INDEX agents_pkey ON public.agents USING btree (id)`
- `agents_system_user_id_key`: `CREATE UNIQUE INDEX agents_system_user_id_key ON public.agents USING btree (system_user_id)`
- `idx_agents__active_by_type`: `CREATE INDEX idx_agents__active_by_type ON public.agents USING btree (agent_type, activated_at DESC) WHERE ((activated_at IS NOT NULL) AND (deactivated_at IS NULL) AND (deleted_at IS NULL))`
- `idx_agents__system_user_id`: `CREATE INDEX idx_agents__system_user_id ON public.agents USING btree (system_user_id) WHERE (deleted_at IS NULL)`

**Triggers:**

- `moderation_transparency_agents_lock`: `CREATE TRIGGER moderation_transparency_agents_lock BEFORE UPDATE OF deleted_at ON public.agents FOR EACH ROW EXECUTE FUNCTION fn_lock_agent_moderation_transparency_agent()`
- `trigger_agents_updated_at`: `CREATE TRIGGER trigger_agents_updated_at BEFORE UPDATE ON public.agents FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
