# Table `ses_bounce_events`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Records email bounce, complaint, and delivery notifications received from AWS SES.

Not partitioned — growth: unbounded.

| Column              | Type                       | Nullable | Default                      | Identity | Generated | Collation | Comment                                                                                                                                                                                                                                                                                                            |
| ------------------- | -------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `id`                | `uuid`                     | no       | `uuidv7()`                   |          |           |           |                                                                                                                                                                                                                                                                                                                    |
| `created_at`        | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                                                                                                                                                                                                                                                                                    |
| `notification_type` | `ses_notification_types`   | no       |                              |          |           |           | SES notification type: bounce, complaint, or delivery.                                                                                                                                                                                                                                                             |
| `bounce_type`       | `ses_bounce_types`         | yes      |                              |          |           |           | Bounce classification: permanent, transient, or undetermined.                                                                                                                                                                                                                                                      |
| `bounce_sub_type`   | `text`                     | yes      |                              |          |           |           | Detailed bounce sub-type from SES (e.g. General, NoEmail).                                                                                                                                                                                                                                                         |
| `recipients`        | `jsonb`                    | no       | `'[]'::jsonb`                |          |           |           | JSONB array of recipient email addresses affected by this event.                                                                                                                                                                                                                                                   |
| `ses_message_id`    | `text`                     | yes      |                              |          |           |           | SES message ID for correlating with sent emails.                                                                                                                                                                                                                                                                   |
| `ses_feedback_id`   | `text`                     | yes      |                              |          |           |           | SES feedback ID for the notification.                                                                                                                                                                                                                                                                              |
| `ses_timestamp`     | `timestamp with time zone` | yes      |                              |          |           |           | Timestamp from the SES notification payload.                                                                                                                                                                                                                                                                       |
| `raw_message`       | `jsonb`                    | no       |                              |          |           |           | Full raw SES notification payload for debugging.                                                                                                                                                                                                                                                                   |
| `diagnostic_code`   | `text`                     | yes      |                              |          |           |           | SMTP diagnostic code from the bounce (e.g. 550 5.1.1).                                                                                                                                                                                                                                                             |
| `reporting_mta`     | `text`                     | yes      |                              |          |           |           | The MTA that reported the bounce.                                                                                                                                                                                                                                                                                  |
| `dedup_key`         | `text`                     | yes      |                              |          |           |           | SHA-256 hex digest of ses_message_id, notification_type, ses_timestamp, and the sorted normalized recipients; NULL when ses_message_id or ses_timestamp is missing. Absorbs at-least-once redelivery duplicates without collapsing distinct per-recipient notifications that share a mail.messageId and timestamp. |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**
_none_

**Check constraints:**

- `ses_bounce_events_bounce_sub_type_check`: `CHECK (((bounce_sub_type IS NULL) OR ((char_length(bounce_sub_type) <= 255) AND (TRIM(BOTH FROM bounce_sub_type) = bounce_sub_type))))`
- `ses_bounce_events_dedup_key_check`: `CHECK (((dedup_key IS NULL) OR (char_length(dedup_key) = 64)))`
- `ses_bounce_events_diagnostic_code_check`: `CHECK (((diagnostic_code IS NULL) OR ((char_length(diagnostic_code) <= 1024) AND (TRIM(BOTH FROM diagnostic_code) = diagnostic_code))))`
- `ses_bounce_events_reporting_mta_check`: `CHECK (((reporting_mta IS NULL) OR ((char_length(reporting_mta) <= 255) AND (TRIM(BOTH FROM reporting_mta) = reporting_mta))))`
- `ses_bounce_events_ses_feedback_id_check`: `CHECK (((ses_feedback_id IS NULL) OR ((char_length(ses_feedback_id) <= 1024) AND (TRIM(BOTH FROM ses_feedback_id) = ses_feedback_id))))`
- `ses_bounce_events_ses_message_id_check`: `CHECK (((ses_message_id IS NULL) OR ((char_length(ses_message_id) <= 1024) AND (TRIM(BOTH FROM ses_message_id) = ses_message_id))))`

**Foreign keys:**
_none_

**Indexes:**

- `idx_ses_bounce_events__dedup_key`: `CREATE UNIQUE INDEX idx_ses_bounce_events__dedup_key ON public.ses_bounce_events USING btree (dedup_key) WHERE (dedup_key IS NOT NULL)`
- `idx_ses_bounce_events__notification_type`: `CREATE INDEX idx_ses_bounce_events__notification_type ON public.ses_bounce_events USING btree (notification_type, id DESC)`
- `idx_ses_bounce_events__recipients`: `CREATE INDEX idx_ses_bounce_events__recipients ON public.ses_bounce_events USING gin (recipients)`
- `idx_ses_bounce_events__ses_message_id`: `CREATE INDEX idx_ses_bounce_events__ses_message_id ON public.ses_bounce_events USING btree (ses_message_id) WHERE (ses_message_id IS NOT NULL)`
- `ses_bounce_events_pkey`: `CREATE UNIQUE INDEX ses_bounce_events_pkey ON public.ses_bounce_events USING btree (id)`

**Triggers:**
_none_
