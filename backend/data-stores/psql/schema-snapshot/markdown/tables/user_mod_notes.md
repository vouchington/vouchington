# Table `user_mod_notes`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Private moderator notes on a user. community_id NULL = global/site-level note.

Not partitioned — growth: unbounded.

| Column           | Type                       | Nullable | Default                      | Identity | Generated | Collation | Comment                                                                                     |
| ---------------- | -------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | ------------------------------------------------------------------------------------------- |
| `id`             | `uuid`                     | no       | `uuidv7()`                   |          |           |           |                                                                                             |
| `target_user_id` | `uuid`                     | no       |                              |          |           |           | The user the note is about.                                                                 |
| `author_user_id` | `uuid`                     | no       |                              |          |           |           | Moderator who wrote the note.                                                               |
| `community_id`   | `uuid`                     | yes      |                              |          |           |           | Community scope; NULL means a global/site-level note visible only to site moderation staff. |
| `body`           | `text`                     | no       |                              |          |           |           | Note text, max 2000 chars.                                                                  |
| `created_at`     | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                                                             |
| `deleted_at`     | `timestamp with time zone` | yes      |                              |          |           |           | When set, the note is soft-deleted.                                                         |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**
_none_

**Check constraints:**

- `user_mod_notes_body_check`: `CHECK (((char_length(body) > 0) AND (char_length(body) <= 2000)))`

**Foreign keys:**

- `user_mod_notes_author_user_id_fkey`: `FOREIGN KEY (author_user_id) REFERENCES users(id) ON DELETE CASCADE`
- `user_mod_notes_community_id_fkey`: `FOREIGN KEY (community_id) REFERENCES communities(id) ON DELETE CASCADE`
- `user_mod_notes_target_user_id_fkey`: `FOREIGN KEY (target_user_id) REFERENCES users(id) ON DELETE CASCADE`

**Indexes:**

- `idx_user_mod_notes__author_user_id`: `CREATE INDEX idx_user_mod_notes__author_user_id ON public.user_mod_notes USING btree (author_user_id)`
- `idx_user_mod_notes__community_id`: `CREATE INDEX idx_user_mod_notes__community_id ON public.user_mod_notes USING btree (community_id)`
- `idx_user_mod_notes__target__community`: `CREATE INDEX idx_user_mod_notes__target__community ON public.user_mod_notes USING btree (target_user_id, community_id) WHERE (deleted_at IS NULL)`
- `idx_user_mod_notes__target__id`: `CREATE INDEX idx_user_mod_notes__target__id ON public.user_mod_notes USING btree (target_user_id, id DESC) WHERE (deleted_at IS NULL)`
- `idx_user_mod_notes__target_user_id`: `CREATE INDEX idx_user_mod_notes__target_user_id ON public.user_mod_notes USING btree (target_user_id) WHERE (target_user_id IS NOT NULL)`
- `user_mod_notes_pkey`: `CREATE UNIQUE INDEX user_mod_notes_pkey ON public.user_mod_notes USING btree (id)`

**Triggers:**
_none_
