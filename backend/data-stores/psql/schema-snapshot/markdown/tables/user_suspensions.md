# Table `user_suspensions`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Append-only history of user suspensions. A user is suspended as long as any active row (lifted_at IS NULL) exists. NULL lifted_at means active. Canonical insert-then-cancel pattern; never UPDATE timestamps in place. See community_bans for reference.

Not partitioned — growth: unbounded.

| Column            | Type                       | Nullable | Default                      | Identity | Generated | Collation | Comment                                                                |
| ----------------- | -------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | ---------------------------------------------------------------------- |
| `id`              | `uuid`                     | no       | `uuidv7()`                   |          |           |           |                                                                        |
| `user_id`         | `uuid`                     | no       |                              |          |           |           | The suspended user.                                                    |
| `suspended_by_id` | `uuid`                     | yes      |                              |          |           |           | Admin who issued the suspension.                                       |
| `reason`          | `text`                     | yes      |                              |          |           |           | Optional reason shown to the user and recorded for moderation history. |
| `created_at`      | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                                        |
| `updated_at`      | `timestamp with time zone` | yes      | `CURRENT_TIMESTAMP`          |          |           |           |                                                                        |
| `lifted_at`       | `timestamp with time zone` | yes      |                              |          |           |           | When the suspension was manually lifted. NULL means still active.      |
| `lifted_by_id`    | `uuid`                     | yes      |                              |          |           |           | Who lifted the suspension.                                             |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**
_none_

**Check constraints:**
_none_

**Foreign keys:**

- `user_suspensions_lifted_by_id_fkey`: `FOREIGN KEY (lifted_by_id) REFERENCES users(id) ON DELETE SET NULL`
- `user_suspensions_suspended_by_id_fkey`: `FOREIGN KEY (suspended_by_id) REFERENCES users(id) ON DELETE SET NULL`
- `user_suspensions_user_id_fkey`: `FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE`

**Indexes:**

- `idx_user_suspensions__user`: `CREATE INDEX idx_user_suspensions__user ON public.user_suspensions USING btree (user_id) WHERE (lifted_at IS NULL)`
- `idx_user_suspensions__user_id`: `CREATE INDEX idx_user_suspensions__user_id ON public.user_suspensions USING btree (user_id) WHERE (user_id IS NOT NULL)`
- `user_suspensions_pkey`: `CREATE UNIQUE INDEX user_suspensions_pkey ON public.user_suspensions USING btree (id)`

**Triggers:**

- `trigger_user_suspensions_updated_at`: `CREATE TRIGGER trigger_user_suspensions_updated_at BEFORE UPDATE ON public.user_suspensions FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
