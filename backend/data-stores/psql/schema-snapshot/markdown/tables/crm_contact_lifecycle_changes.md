# Table `crm_contact_lifecycle_changes`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Append-only audit log of CRM contact lifecycle transitions. Current lifecycle timestamps on crm_contacts are denormalized from the latest change.

Not partitioned — growth: unbounded.

| Column           | Type                                 | Nullable | Default                      | Identity | Generated | Collation | Comment                                                               |
| ---------------- | ------------------------------------ | -------- | ---------------------------- | -------- | --------- | --------- | --------------------------------------------------------------------- |
| `id`             | `uuid`                               | no       | `uuidv7()`                   |          |           |           |                                                                       |
| `crm_contact_id` | `uuid`                               | no       |                              |          |           |           | The CRM contact whose lifecycle changed.                              |
| `change_type`    | `crm_contact_lifecycle_change_types` | no       |                              |          |           |           | Type of lifecycle transition.                                         |
| `changed_by_id`  | `uuid`                               | yes      |                              |          |           |           | User or admin who initiated the change (no FK for audit persistence). |
| `note`           | `text`                               | yes      |                              |          |           |           | Optional free-text note about the lifecycle transition.               |
| `metadata`       | `jsonb`                              | no       | `'{}'::jsonb`                |          |           |           | Structured metadata about the lifecycle transition.                   |
| `contacted_at`   | `timestamp with time zone`           | yes      |                              |          |           |           | Snapshot of contacted_at after this lifecycle transition.             |
| `responded_at`   | `timestamp with time zone`           | yes      |                              |          |           |           | Snapshot of responded_at after this lifecycle transition.             |
| `converted_at`   | `timestamp with time zone`           | yes      |                              |          |           |           | Snapshot of converted_at after this lifecycle transition.             |
| `opted_out_at`   | `timestamp with time zone`           | yes      |                              |          |           |           | Snapshot of opted_out_at after this lifecycle transition.             |
| `archived_at`    | `timestamp with time zone`           | yes      |                              |          |           |           | Snapshot of archived_at after this lifecycle transition.              |
| `created_at`     | `timestamp with time zone`           | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                                       |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**

- `crm_contact_lifecycle_changes_crm_contact_id_id_key`: `UNIQUE (crm_contact_id, id)`

**Check constraints:**

- `crm_contact_lifecycle_changes_metadata_check`: `CHECK ((jsonb_typeof(metadata) = 'object'::text))`
- `crm_contact_lifecycle_changes_note_check`: `CHECK (((note IS NULL) OR (char_length(note) <= 1000)))`

**Foreign keys:**

- `crm_contact_lifecycle_changes_crm_contact_id_fkey`: `FOREIGN KEY (crm_contact_id) REFERENCES crm_contacts(id) ON DELETE CASCADE`

**Indexes:**

- `crm_contact_lifecycle_changes_crm_contact_id_id_key`: `CREATE UNIQUE INDEX crm_contact_lifecycle_changes_crm_contact_id_id_key ON public.crm_contact_lifecycle_changes USING btree (crm_contact_id, id)`
- `crm_contact_lifecycle_changes_pkey`: `CREATE UNIQUE INDEX crm_contact_lifecycle_changes_pkey ON public.crm_contact_lifecycle_changes USING btree (id)`
- `idx_crm_contact_lifecycle_changes__contact_id__id`: `CREATE INDEX idx_crm_contact_lifecycle_changes__contact_id__id ON public.crm_contact_lifecycle_changes USING btree (crm_contact_id, id DESC)`

**Triggers:**
_none_
