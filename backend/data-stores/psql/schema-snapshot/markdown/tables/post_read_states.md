# Table `post_read_states`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Tracks which posts a user has marked as read.

RANGE partitioned on `user_id` (children: default, no retention owner, access class: target-scoped, growth: unbounded).

| Column    | Type                       | Nullable | Default | Identity | Generated | Collation | Comment                                     |
| --------- | -------------------------- | -------- | ------- | -------- | --------- | --------- | ------------------------------------------- |
| `user_id` | `uuid`                     | no       |         |          |           |           | The user who marked this post as read.      |
| `post_id` | `uuid`                     | no       |         |          |           |           | The post marked as read.                    |
| `read_at` | `timestamp with time zone` | no       | `now()` |          |           |           | Timestamp when the post was marked as read. |

**Primary key:** `PRIMARY KEY (user_id, post_id)`

**Unique constraints:**
_none_

**Check constraints:**
_none_

**Foreign keys:**

- `post_read_states_post_id_fkey`: `FOREIGN KEY (post_id) REFERENCES posts(id) ON DELETE CASCADE`
- `post_read_states_user_id_fkey`: `FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE`

**Indexes:**

- `post_read_states__post_id`: `CREATE INDEX post_read_states__post_id ON ONLY public.post_read_states USING btree (post_id)`
- `post_read_states_pkey`: `CREATE UNIQUE INDEX post_read_states_pkey ON ONLY public.post_read_states USING btree (user_id, post_id)`

**Triggers:**
_none_
