# Table `copyright_notice_lifecycle_events`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Append-only legal workflow audit trail.

Not partitioned — growth: unbounded.

| Column                | Type                       | Nullable | Default                      | Identity | Generated | Collation | Comment                                                                                   |
| --------------------- | -------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | ----------------------------------------------------------------------------------------- |
| `id`                  | `uuid`                     | no       | `uuidv7()`                   |          |           |           |                                                                                           |
| `copyright_notice_id` | `uuid`                     | no       |                              |          |           |           | Legal case whose transition or correspondence event was recorded.                         |
| `event_type`          | `text`                     | no       |                              |          |           |           | Versioned legal workflow event name.                                                      |
| `actor_user_id`       | `uuid`                     | yes      |                              |          |           |           | User or staff actor for the event; NULL for system activity or after account deletion.    |
| `metadata`            | `jsonb`                    | no       | `'{}'::jsonb`                |          |           |           | Private structured event snapshot; member responses never serialize this object directly. |
| `created_at`          | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                                                           |
| `updated_at`          | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP`          |          |           |           |                                                                                           |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**
_none_

**Check constraints:**

- `copyright_notice_lifecycle_events_event_type_check`: `CHECK (((char_length(event_type) >= 1) AND (char_length(event_type) <= 100)))`
- `copyright_notice_lifecycle_events_metadata_check`: `CHECK ((jsonb_typeof(metadata) = 'object'::text))`

**Foreign keys:**

- `copyright_notice_lifecycle_events_actor_user_id_fkey`: `FOREIGN KEY (actor_user_id) REFERENCES users(id) ON DELETE SET NULL`
- `copyright_notice_lifecycle_events_copyright_notice_id_fkey`: `FOREIGN KEY (copyright_notice_id) REFERENCES copyright_notices(id) ON DELETE CASCADE`

**Indexes:**

- `copyright_notice_lifecycle_events_pkey`: `CREATE UNIQUE INDEX copyright_notice_lifecycle_events_pkey ON public.copyright_notice_lifecycle_events USING btree (id)`
- `idx_copyright_notice_events__actor`: `CREATE INDEX idx_copyright_notice_events__actor ON public.copyright_notice_lifecycle_events USING btree (actor_user_id) WHERE (actor_user_id IS NOT NULL)`
- `idx_copyright_notice_events__notice`: `CREATE INDEX idx_copyright_notice_events__notice ON public.copyright_notice_lifecycle_events USING btree (copyright_notice_id, id DESC)`

**Triggers:**

- `trigger_copyright_notice_events_immutable`: `CREATE TRIGGER trigger_copyright_notice_events_immutable BEFORE DELETE OR UPDATE ON public.copyright_notice_lifecycle_events FOR EACH ROW EXECUTE FUNCTION fn_guard_copyright_immutable_with_actor_erasure('actor_user_id')`
