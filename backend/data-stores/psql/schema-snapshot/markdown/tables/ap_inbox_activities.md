# Table `ap_inbox_activities`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Replay-dedup ledger for the ActivityPub inbox receiver. The marker commits atomically with the activity core database effect; a second delivery of the same activity id is rejected before it reaches any write-path.

Not partitioned — growth: unbounded.

| Column          | Type                       | Nullable | Default                      | Identity | Generated | Collation | Comment                                                                                                        |
| --------------- | -------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | -------------------------------------------------------------------------------------------------------------- |
| `id`            | `uuid`                     | no       | `uuidv7()`                   |          |           |           |                                                                                                                |
| `activity_id`   | `text`                     | no       |                              |          |           |           | The ActivityPub activity's `id` field (a remote URI). Unique — the dedup key.                                  |
| `activity_type` | `text`                     | no       |                              |          |           |           | The activity's `type` field (e.g. Follow, Undo, Like), recorded for operational visibility.                    |
| `actor_uri`     | `text`                     | no       |                              |          |           |           | The activity's `actor` field (the sending remote actor's ActivityPub id), recorded for operational visibility. |
| `received_at`   | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP`          |          |           |           | When this activity was accepted by the inbox receiver.                                                         |
| `created_at`    | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                                                                                |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**
_none_

**Check constraints:**
_none_

**Foreign keys:**
_none_

**Indexes:**

- `ap_inbox_activities_pkey`: `CREATE UNIQUE INDEX ap_inbox_activities_pkey ON public.ap_inbox_activities USING btree (id)`
- `idx_ap_inbox_activities__activity_id`: `CREATE UNIQUE INDEX idx_ap_inbox_activities__activity_id ON public.ap_inbox_activities USING btree (activity_id)`

**Triggers:**
_none_
