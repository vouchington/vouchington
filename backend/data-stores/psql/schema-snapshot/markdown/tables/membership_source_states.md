# Table `membership_source_states`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Current mutable state projected from one membership source.

Not partitioned — growth: unbounded.

| Column                               | Type                       | Nullable | Default             | Identity | Generated | Collation | Comment                                                                          |
| ------------------------------------ | -------------------------- | -------- | ------------------- | -------- | --------- | --------- | -------------------------------------------------------------------------------- |
| `membership_source_id`               | `uuid`                     | no       |                     |          |           |           | Source whose current entitlement state is projected.                             |
| `source_kind`                        | `membership_source_kinds`  | no       |                     |          |           |           | Copied source kind, constrained to the source row.                               |
| `membership_provider_lineage_id`     | `uuid`                     | yes      |                     |          |           |           | Copied provider lineage, constrained to the source row.                          |
| `membership_provider_observation_id` | `uuid`                     | yes      |                     |          |           |           | Latest normalized provider observation, when the source is provider-backed.      |
| `membership_product_id`              | `uuid`                     | no       |                     |          |           |           | Canonical product currently provided by this source.                             |
| `effective_at`                       | `timestamp with time zone` | no       |                     |          |           |           | When this projected source state became effective.                               |
| `expires_at`                         | `timestamp with time zone` | yes      |                     |          |           |           | When this projected source state expires, if finite.                             |
| `cancelled_at`                       | `timestamp with time zone` | yes      |                     |          |           |           | Projected cancellation time.                                                     |
| `expired_at`                         | `timestamp with time zone` | yes      |                     |          |           |           | When this projected source state reached terminal expiration.                    |
| `past_due_at`                        | `timestamp with time zone` | yes      |                     |          |           |           | Projected past-due entry time.                                                   |
| `paused_at`                          | `timestamp with time zone` | yes      |                     |          |           |           | When this projected source state was paused for a higher-precedence entitlement. |
| `auto_renews`                        | `boolean`                  | no       | `false`             |          |           |           | Projected provider automatic-renewal setting.                                    |
| `updated_at`                         | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                                                  |

**Primary key:** `PRIMARY KEY (membership_source_id)`

**Unique constraints:**
_none_

**Check constraints:**

- `membership_source_states_check`: `CHECK (((source_kind = 'admin_grant'::membership_source_kinds) = (membership_provider_lineage_id IS NULL)))`
- `membership_source_states_check1`: `CHECK (((membership_provider_observation_id IS NULL) OR (membership_provider_lineage_id IS NOT NULL)))`
- `membership_source_states_check2`: `CHECK (((expires_at IS NULL) OR (expires_at >= effective_at)))`
- `membership_source_states_check3`: `CHECK (((cancelled_at IS NULL) OR (cancelled_at >= effective_at)))`
- `membership_source_states_check4`: `CHECK (((expired_at IS NULL) OR (expired_at >= effective_at)))`
- `membership_source_states_check5`: `CHECK (((past_due_at IS NULL) OR (past_due_at >= effective_at)))`
- `membership_source_states_check6`: `CHECK (((paused_at IS NULL) OR (paused_at >= effective_at)))`
- `membership_source_states_check7`: `CHECK ((num_nonnulls(cancelled_at, expired_at, past_due_at, paused_at) <= 1))`

**Foreign keys:**

- `fk_membership_source_states__observation_lineage_product_kind`: `FOREIGN KEY (membership_provider_observation_id, membership_provider_lineage_id, membership_product_id, source_kind) REFERENCES membership_provider_observations(id, membership_provider_lineage_id, membership_product_id, source_kind) ON DELETE RESTRICT`
- `fk_membership_source_states__source_kind`: `FOREIGN KEY (membership_source_id, source_kind) REFERENCES membership_sources(id, source_kind) ON DELETE CASCADE`
- `fk_membership_source_states__source_lineage`: `FOREIGN KEY (membership_source_id, membership_provider_lineage_id) REFERENCES membership_sources(id, membership_provider_lineage_id) ON DELETE CASCADE`
- `membership_source_states_membership_product_id_fkey`: `FOREIGN KEY (membership_product_id) REFERENCES membership_products(id) ON DELETE RESTRICT`
- `membership_source_states_membership_source_id_fkey`: `FOREIGN KEY (membership_source_id) REFERENCES membership_sources(id) ON DELETE CASCADE`

**Indexes:**

- `idx_membership_source_states__lineage_id`: `CREATE INDEX idx_membership_source_states__lineage_id ON public.membership_source_states USING btree (membership_provider_lineage_id) WHERE (membership_provider_lineage_id IS NOT NULL)`
- `idx_membership_source_states__observation_id`: `CREATE INDEX idx_membership_source_states__observation_id ON public.membership_source_states USING btree (membership_provider_observation_id) WHERE (membership_provider_observation_id IS NOT NULL)`
- `idx_membership_source_states__product_id`: `CREATE INDEX idx_membership_source_states__product_id ON public.membership_source_states USING btree (membership_product_id)`
- `membership_source_states_pkey`: `CREATE UNIQUE INDEX membership_source_states_pkey ON public.membership_source_states USING btree (membership_source_id)`

**Triggers:**
_none_
