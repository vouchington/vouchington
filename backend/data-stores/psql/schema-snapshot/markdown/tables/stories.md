# Table `stories`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Groups of related RSS feed items covering the same news event.

Not partitioned — growth: unbounded.

| Column                      | Type                       | Nullable | Default                      | Identity | Generated | Collation | Comment                                                                            |
| --------------------------- | -------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | ---------------------------------------------------------------------------------- |
| `id`                        | `uuid`                     | no       | `uuidv7()`                   |          |           |           |                                                                                    |
| `title`                     | `text`                     | yes      |                              |          |           |           | LLM-generated summary title for the story cluster.                                 |
| `published_at`              | `timestamp with time zone` | yes      |                              |          |           |           | When the event actually occurred (agent-determined, not row creation time).        |
| `cluster_reason`            | `text`                     | yes      |                              |          |           |           | LLM-generated explanation of why the articles in this story were grouped together. |
| `official_rss_feed_item_id` | `uuid`                     | yes      |                              |          |           |           | The canonical/official item for this story (e.g. original press release).          |
| `official_locked_at`        | `timestamp with time zone` | yes      |                              |          |           |           | When set by admin, prevents agents from overriding the official item selection.    |
| `created_at`                | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                                                    |
| `updated_at`                | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP`          |          |           |           |                                                                                    |
| `deleted_at`                | `timestamp with time zone` | yes      |                              |          |           |           |                                                                                    |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**
_none_

**Check constraints:**

- `stories_title_check`: `CHECK (((title IS NULL) OR ((char_length(title) >= 1) AND (char_length(title) <= 500))))`

**Foreign keys:**

- `stories_official_rss_feed_item_id_fkey`: `FOREIGN KEY (official_rss_feed_item_id) REFERENCES rss_feed_items(id) ON DELETE SET NULL`

**Indexes:**

- `idx_stories__id_desc`: `CREATE INDEX idx_stories__id_desc ON public.stories USING btree (id DESC) WHERE (deleted_at IS NULL)`
- `idx_stories__official_rss_feed_item_id`: `CREATE INDEX idx_stories__official_rss_feed_item_id ON public.stories USING btree (official_rss_feed_item_id) WHERE (official_rss_feed_item_id IS NOT NULL)`
- `stories_pkey`: `CREATE UNIQUE INDEX stories_pkey ON public.stories USING btree (id)`

**Triggers:**

- `trigger_stories_updated_at`: `CREATE TRIGGER trigger_stories_updated_at BEFORE UPDATE ON public.stories FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
