# Table `community_list_items__topics`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Topics curated into a community list by community leads.

Not partitioned — growth: unbounded.

| Column          | Type                       | Nullable | Default                      | Identity | Generated | Collation | Comment                                            |
| --------------- | -------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | -------------------------------------------------- |
| `id`            | `uuid`                     | no       | `uuidv7()`                   |          |           |           |                                                    |
| `community_id`  | `uuid`                     | no       |                              |          |           |           | The community this list item belongs to.           |
| `topic_id`      | `uuid`                     | no       |                              |          |           |           | The topic added to the list.                       |
| `order_index`   | `integer`                  | no       | `0`                          |          |           |           | Manual sort order within the list (lower = first). |
| `added_by_id`   | `uuid`                     | yes      |                              |          |           |           | User who added this item to the list.              |
| `created_at`    | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                    |
| `removed_at`    | `timestamp with time zone` | yes      |                              |          |           |           | When set, the item has been removed from the list. |
| `removed_by_id` | `uuid`                     | yes      |                              |          |           |           | User who removed this item from the list.          |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**
_none_

**Check constraints:**
_none_

**Foreign keys:**

- `community_list_items__topics_added_by_id_fkey`: `FOREIGN KEY (added_by_id) REFERENCES users(id) ON DELETE SET NULL`
- `community_list_items__topics_community_id_fkey`: `FOREIGN KEY (community_id) REFERENCES communities(id) ON DELETE CASCADE`
- `community_list_items__topics_removed_by_id_fkey`: `FOREIGN KEY (removed_by_id) REFERENCES users(id) ON DELETE SET NULL`
- `community_list_items__topics_topic_id_fkey`: `FOREIGN KEY (topic_id) REFERENCES topics(id) ON DELETE CASCADE`

**Indexes:**

- `community_list_items__topics_pkey`: `CREATE UNIQUE INDEX community_list_items__topics_pkey ON public.community_list_items__topics USING btree (id)`
- `idx_cli_topics__community`: `CREATE INDEX idx_cli_topics__community ON public.community_list_items__topics USING btree (community_id)`
- `idx_cli_topics__topic`: `CREATE INDEX idx_cli_topics__topic ON public.community_list_items__topics USING btree (topic_id)`
- `idx_cli_topics__uniq`: `CREATE UNIQUE INDEX idx_cli_topics__uniq ON public.community_list_items__topics USING btree (community_id, topic_id) WHERE (removed_at IS NULL)`

**Triggers:**
_none_
