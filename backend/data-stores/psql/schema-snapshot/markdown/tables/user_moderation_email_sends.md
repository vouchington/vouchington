# Table `user_moderation_email_sends`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

One row per user and moderation email send key, used to prevent duplicate recurring summary sends.

Not partitioned — growth: unbounded.

| Column       | Type                       | Nullable | Default             | Identity | Generated | Collation | Comment                                                  |
| ------------ | -------------------------- | -------- | ------------------- | -------- | --------- | --------- | -------------------------------------------------------- |
| `user_id`    | `uuid`                     | no       |                     |          |           |           | The recipient user.                                      |
| `send_key`   | `text`                     | no       |                     |          |           |           | Recurring cadence key for the moderation summary window. |
| `claimed_at` | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           | When the send was first claimed by the dispatcher.       |
| `sent_at`    | `timestamp with time zone` | yes      |                     |          |           |           | When the email processor marked the send as delivered.   |
| `updated_at` | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                          |

**Primary key:** `PRIMARY KEY (user_id, send_key)`

**Unique constraints:**
_none_

**Check constraints:**
_none_

**Foreign keys:**

- `user_moderation_email_sends_user_id_fkey`: `FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE`

**Indexes:**

- `user_moderation_email_sends_pkey`: `CREATE UNIQUE INDEX user_moderation_email_sends_pkey ON public.user_moderation_email_sends USING btree (user_id, send_key)`

**Triggers:**

- `trigger_user_moderation_email_sends_updated_at`: `CREATE TRIGGER trigger_user_moderation_email_sends_updated_at BEFORE UPDATE ON public.user_moderation_email_sends FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
