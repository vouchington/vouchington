# Table `post_images`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Associates images with posts in a specified display order with optional captions.

Not partitioned — growth: unbounded.

| Column        | Type      | Nullable | Default    | Identity | Generated | Collation | Comment                                                |
| ------------- | --------- | -------- | ---------- | -------- | --------- | --------- | ------------------------------------------------------ |
| `post_id`     | `uuid`    | no       |            |          |           |           | The post this image is attached to.                    |
| `image_id`    | `uuid`    | no       |            |          |           |           | The image entity.                                      |
| `order_index` | `integer` | no       | `0`        |          |           |           | Zero-based display order of the image within the post. |
| `caption`     | `text`    | no       | `''::text` |          |           |           | Optional caption text for the image.                   |

**Primary key:** `PRIMARY KEY (post_id, image_id)`

**Unique constraints:**
_none_

**Check constraints:**

- `post_images_caption_check`: `CHECK ((char_length(caption) <= 1000))`
- `post_images_caption_check1`: `CHECK ((caption = TRIM(BOTH FROM caption)))`
- `post_images_order_index_check`: `CHECK ((order_index >= 0))`

**Foreign keys:**

- `post_images_image_id_fkey`: `FOREIGN KEY (image_id) REFERENCES images(id) ON DELETE CASCADE`
- `post_images_post_id_fkey`: `FOREIGN KEY (post_id) REFERENCES posts(id) ON DELETE CASCADE`

**Indexes:**

- `idx_post_images__image_id`: `CREATE INDEX idx_post_images__image_id ON public.post_images USING btree (image_id)`
- `idx_post_images__post_id__order_index`: `CREATE INDEX idx_post_images__post_id__order_index ON public.post_images USING btree (post_id, order_index)`
- `post_images_pkey`: `CREATE UNIQUE INDEX post_images_pkey ON public.post_images USING btree (post_id, image_id)`

**Triggers:**
_none_
