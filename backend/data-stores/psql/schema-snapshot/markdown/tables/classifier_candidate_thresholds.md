# Table `classifier_candidate_thresholds`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Immutable per-candidate threshold revisions with an explicit deactivation lifecycle.

Not partitioned — growth: unbounded.

| Column                     | Type                       | Nullable | Default                      | Identity | Generated | Collation | Comment                                                                            |
| -------------------------- | -------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | ---------------------------------------------------------------------------------- |
| `id`                       | `uuid`                     | no       | `uuidv7()`                   |          |           |           |                                                                                    |
| `classifier_id`            | `uuid`                     | no       |                              |          |           |           | Classifier copied from candidate and prompt for relational alignment.              |
| `candidate_id`             | `uuid`                     | no       |                              |          |           |           | Stored candidate receiving prompt-revision-specific bounds.                        |
| `prompt_version_id`        | `uuid`                     | no       |                              |          |           |           | Prompt revision whose defaults the overrides inherit.                              |
| `lower_threshold_override` | `numeric(5,4)`             | yes      |                              |          |           |           | Candidate-specific lower boundary, or NULL to inherit the prompt revision default. |
| `upper_threshold_override` | `numeric(5,4)`             | yes      |                              |          |           |           | Candidate-specific upper boundary, or NULL to inherit the prompt revision default. |
| `activated_at`             | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP`          |          |           |           | Time this immutable threshold revision became active.                              |
| `deactivated_at`           | `timestamp with time zone` | yes      |                              |          |           |           | Time this threshold revision was superseded, or NULL while active.                 |
| `created_at`               | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                                                    |
| `created_by_id`            | `uuid`                     | yes      |                              |          |           |           |                                                                                    |
| `updated_at`               | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP`          |          |           |           |                                                                                    |
| `deactivated_by_id`        | `uuid`                     | yes      |                              |          |           |           | User who superseded this threshold revision, retained only while the user exists.  |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**
_none_

**Check constraints:**

- `chk_classifier_candidate_thresholds__lifecycle`: `CHECK ((((deactivated_at IS NULL) AND (deactivated_by_id IS NULL)) OR ((deactivated_at IS NOT NULL) AND (deactivated_at >= activated_at))))`
- `chk_classifier_candidate_thresholds__override_bounds`: `CHECK ((((lower_threshold_override IS NULL) OR (lower_threshold_override >= (0)::numeric)) AND ((upper_threshold_override IS NULL) OR (upper_threshold_override <= (1)::numeric))))`

**Foreign keys:**

- `classifier_candidate_thresholds_created_by_id_fkey`: `FOREIGN KEY (created_by_id) REFERENCES users(id) ON DELETE SET NULL`
- `classifier_candidate_thresholds_deactivated_by_id_fkey`: `FOREIGN KEY (deactivated_by_id) REFERENCES users(id) ON DELETE SET NULL`
- `fk_classifier_candidate_thresholds__candidate_classifier`: `FOREIGN KEY (candidate_id, classifier_id) REFERENCES classifier_candidates(id, classifier_id) ON DELETE CASCADE`
- `fk_classifier_candidate_thresholds__prompt_classifier`: `FOREIGN KEY (prompt_version_id, classifier_id) REFERENCES classifier_prompt_versions(id, classifier_id) ON DELETE CASCADE`

**Indexes:**

- `classifier_candidate_thresholds_pkey`: `CREATE UNIQUE INDEX classifier_candidate_thresholds_pkey ON public.classifier_candidate_thresholds USING btree (id)`
- `idx_classifier_candidate_thresholds__active_candidate_prompt`: `CREATE UNIQUE INDEX idx_classifier_candidate_thresholds__active_candidate_prompt ON public.classifier_candidate_thresholds USING btree (candidate_id, prompt_version_id) WHERE (deactivated_at IS NULL)`
- `idx_classifier_candidate_thresholds__candidate`: `CREATE INDEX idx_classifier_candidate_thresholds__candidate ON public.classifier_candidate_thresholds USING btree (candidate_id)`
- `idx_classifier_candidate_thresholds__classifier`: `CREATE INDEX idx_classifier_candidate_thresholds__classifier ON public.classifier_candidate_thresholds USING btree (classifier_id)`
- `idx_classifier_candidate_thresholds__created_by`: `CREATE INDEX idx_classifier_candidate_thresholds__created_by ON public.classifier_candidate_thresholds USING btree (created_by_id)`
- `idx_classifier_candidate_thresholds__deactivated_by`: `CREATE INDEX idx_classifier_candidate_thresholds__deactivated_by ON public.classifier_candidate_thresholds USING btree (deactivated_by_id)`
- `idx_classifier_candidate_thresholds__prompt_classifier`: `CREATE INDEX idx_classifier_candidate_thresholds__prompt_classifier ON public.classifier_candidate_thresholds USING btree (prompt_version_id, classifier_id)`

**Triggers:**

- `trigger_classifier_candidate_thresholds_effective`: `CREATE TRIGGER trigger_classifier_candidate_thresholds_effective BEFORE INSERT OR UPDATE OF prompt_version_id, lower_threshold_override, upper_threshold_override ON public.classifier_candidate_thresholds FOR EACH ROW EXECUTE FUNCTION fn_require_classifier_candidate_effective_thresholds()`
- `trigger_classifier_candidate_thresholds_lifecycle`: `CREATE TRIGGER trigger_classifier_candidate_thresholds_lifecycle BEFORE UPDATE ON public.classifier_candidate_thresholds FOR EACH ROW EXECUTE FUNCTION fn_require_classifier_candidate_threshold_lifecycle()`
- `trigger_classifier_candidate_thresholds_updated_at`: `CREATE TRIGGER trigger_classifier_candidate_thresholds_updated_at BEFORE UPDATE ON public.classifier_candidate_thresholds FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
