# Table `community_bans`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Tracks community-scoped bans. A ban prevents the user from joining, posting, or commenting in the community. NULL lifted_at means the ban is active; NULL expires_at means permanent.

Not partitioned — growth: unbounded.

| Column         | Type                       | Nullable | Default                      | Identity | Generated | Collation | Comment                                                                                                                                                              |
| -------------- | -------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `id`           | `uuid`                     | no       | `uuidv7()`                   |          |           |           |                                                                                                                                                                      |
| `community_id` | `uuid`                     | no       |                              |          |           |           | The community this ban applies to.                                                                                                                                   |
| `user_id`      | `uuid`                     | no       |                              |          |           |           | The banned user.                                                                                                                                                     |
| `banned_by_id` | `uuid`                     | yes      |                              |          |           |           | Moderator or owner who issued the ban.                                                                                                                               |
| `reason`       | `text`                     | yes      |                              |          |           |           | Optional reason shown to the banned user and recorded for moderation history.                                                                                        |
| `expires_at`   | `timestamp with time zone` | yes      |                              |          |           |           | When the ban expires. NULL means permanent.                                                                                                                          |
| `created_at`   | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                                                                                                                                      |
| `updated_at`   | `timestamp with time zone` | yes      | `CURRENT_TIMESTAMP`          |          |           |           |                                                                                                                                                                      |
| `lifted_at`    | `timestamp with time zone` | yes      |                              |          |           |           | When a moderator manually lifted the ban early. NULL means the ban was never manually lifted (it may still be active, or may have expired naturally via expires_at). |
| `lifted_by_id` | `uuid`                     | yes      |                              |          |           |           | Who lifted the ban early.                                                                                                                                            |
| `case_id`      | `uuid`                     | no       |                              |          |           |           | The moderation case this ban belongs to.                                                                                                                             |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**
_none_

**Check constraints:**
_none_

**Foreign keys:**

- `community_bans_banned_by_id_fkey`: `FOREIGN KEY (banned_by_id) REFERENCES users(id) ON DELETE SET NULL`
- `community_bans_case_id_fkey`: `FOREIGN KEY (case_id) REFERENCES moderation_cases(id) ON DELETE CASCADE`
- `community_bans_community_id_fkey`: `FOREIGN KEY (community_id) REFERENCES communities(id) ON DELETE CASCADE`
- `community_bans_lifted_by_id_fkey`: `FOREIGN KEY (lifted_by_id) REFERENCES users(id) ON DELETE SET NULL`
- `community_bans_user_id_fkey`: `FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE`

**Indexes:**

- `community_bans_pkey`: `CREATE UNIQUE INDEX community_bans_pkey ON public.community_bans USING btree (id)`
- `idx_community_bans__case_id`: `CREATE INDEX idx_community_bans__case_id ON public.community_bans USING btree (case_id)`
- `idx_community_bans__community_id`: `CREATE INDEX idx_community_bans__community_id ON public.community_bans USING btree (community_id) WHERE (community_id IS NOT NULL)`
- `idx_community_bans__lookup`: `CREATE INDEX idx_community_bans__lookup ON public.community_bans USING btree (community_id, user_id) WHERE (lifted_at IS NULL)`
- `idx_community_bans__user`: `CREATE INDEX idx_community_bans__user ON public.community_bans USING btree (user_id, id DESC) WHERE (lifted_at IS NULL)`
- `idx_community_bans__user_id`: `CREATE INDEX idx_community_bans__user_id ON public.community_bans USING btree (user_id) WHERE (user_id IS NOT NULL)`

**Triggers:**

- `trigger_community_bans_updated_at`: `CREATE TRIGGER trigger_community_bans_updated_at BEFORE UPDATE ON public.community_bans FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
