# Table `user_metrics`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Aggregated metrics for users: bookmark counts and follower counts.

Not partitioned — growth: unbounded.

| Column                            | Type                       | Nullable | Default                      | Identity | Generated | Collation | Comment                                                  |
| --------------------------------- | -------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | -------------------------------------------------------- |
| `id`                              | `uuid`                     | no       |                              |          |           |           |                                                          |
| `created_at`                      | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                          |
| `updated_at`                      | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP`          |          |           |           |                                                          |
| `bookmarks__follow__topics_count` | `integer`                  | yes      | `0`                          |          |           |           | Number of topics this user bookmarks/follows.            |
| `bookmarks__follow__posts_count`  | `integer`                  | yes      | `0`                          |          |           |           | Number of posts this user bookmarks/follows.             |
| `bookmarks__follow__users_count`  | `integer`                  | yes      | `0`                          |          |           |           | Number of users this user follows.                       |
| `bookmarkers__follow_count`       | `integer`                  | yes      | `0`                          |          |           |           | Number of users who follow this user.                    |
| `bookmarks__updated_at`           | `timestamp with time zone` | yes      | `CURRENT_TIMESTAMP`          |          |           |           | When the bookmark/follow metrics were last recalculated. |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**
_none_

**Check constraints:**
_none_

**Foreign keys:**

- `user_metrics_id_fkey`: `FOREIGN KEY (id) REFERENCES users(id) ON DELETE CASCADE`

**Indexes:**

- `user_metrics_pkey`: `CREATE UNIQUE INDEX user_metrics_pkey ON public.user_metrics USING btree (id)`

**Triggers:**

- `trigger_user_metrics_updated_at`: `CREATE TRIGGER trigger_user_metrics_updated_at BEFORE UPDATE ON public.user_metrics FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
