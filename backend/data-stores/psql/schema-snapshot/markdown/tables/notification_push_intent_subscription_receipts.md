# Table `notification_push_intent_subscription_receipts`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Generation-fenced delivery completion for a notification push intent.

Not partitioned — growth: unbounded.

| Column                  | Type                                | Nullable | Default                                        | Identity | Generated | Collation | Comment                                                                     |
| ----------------------- | ----------------------------------- | -------- | ---------------------------------------------- | -------- | --------- | --------- | --------------------------------------------------------------------------- |
| `user_id`               | `uuid`                              | no       |                                                |          |           |           | Notification owner and first half of the parent intent identity.            |
| `notification_id`       | `uuid`                              | no       |                                                |          |           |           | Notification identifier and second half of the parent intent identity.      |
| `subscription_id`       | `uuid`                              | no       |                                                |          |           |           | Exact subscription generation that received this outcome.                   |
| `endpoint`              | `text`                              | no       |                                                |          |           |           | Provider endpoint retained as delivery audit data for the exact generation. |
| `status`                | `notification_push_endpoint_status` | no       | `'pending'::notification_push_endpoint_status` |          |           |           | Delivery outcome for this subscription generation and notification.         |
| `delivered_at`          | `timestamp with time zone`          | yes      |                                                |          |           |           | Timestamp of successful delivery to this subscription generation.           |
| `permanently_failed_at` | `timestamp with time zone`          | yes      |                                                |          |           |           | Timestamp when this subscription generation became non-retryable.           |
| `updated_at`            | `timestamp with time zone`          | no       | `CURRENT_TIMESTAMP`                            |          |           |           |                                                                             |

**Primary key:** `PRIMARY KEY (user_id, notification_id, subscription_id)`

**Unique constraints:**
_none_

**Check constraints:**

- `notification_push_intent_subscription_receipts_check`: `CHECK (((status = 'delivered'::notification_push_endpoint_status) = (delivered_at IS NOT NULL)))`
- `notification_push_intent_subscription_receipts_check1`: `CHECK (((status = 'permanently_failed'::notification_push_endpoint_status) = (permanently_failed_at IS NOT NULL)))`

**Foreign keys:**

- `notification_push_receipts_intent_fkey`: `FOREIGN KEY (user_id, notification_id) REFERENCES notification_push_intents(user_id, notification_id) ON UPDATE CASCADE ON DELETE CASCADE`
- `notification_push_receipts_subscription_fkey`: `FOREIGN KEY (user_id, subscription_id) REFERENCES web_push_subscriptions(user_id, id) ON UPDATE CASCADE ON DELETE CASCADE`

**Indexes:**

- `idx_push_intent_subscription_receipts_user_subscription`: `CREATE INDEX idx_push_intent_subscription_receipts_user_subscription ON public.notification_push_intent_subscription_receipts USING btree (user_id, subscription_id)`
- `notification_push_intent_subscription_receipts_pkey`: `CREATE UNIQUE INDEX notification_push_intent_subscription_receipts_pkey ON public.notification_push_intent_subscription_receipts USING btree (user_id, notification_id, subscription_id)`

**Triggers:**

- `trigger_notification_push_receipts_updated_at`: `CREATE TRIGGER trigger_notification_push_receipts_updated_at BEFORE UPDATE ON public.notification_push_intent_subscription_receipts FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
