# Table `community_agent_prompt_changes`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Append-only audit log of community agent prompt changes made by moderators.

Not partitioned — growth: unbounded.

| Column            | Type                                   | Nullable | Default                      | Identity | Generated | Collation | Comment                                                                                                |
| ----------------- | -------------------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | ------------------------------------------------------------------------------------------------------ |
| `id`              | `uuid`                                 | no       | `uuidv7()`                   |          |           |           |                                                                                                        |
| `community_id`    | `uuid`                                 | no       |                              |          |           |           | Community that owns the changed prompt; rows cascade-deleted with the community.                       |
| `agent_prompt_id` | `uuid`                                 | no       |                              |          |           |           | ID of the community agent prompt; not a FK so history survives hard-deletes.                           |
| `changed_by_id`   | `uuid`                                 | yes      |                              |          |           |           | Moderator who made the change; SET NULL on user deletion.                                              |
| `action`          | `community_agent_prompt_change_action` | no       |                              |          |           |           | Type of change applied to the prompt (created, updated, deleted, allocated, deallocated, deactivated). |
| `previous_fields` | `jsonb`                                | no       |                              |          |           |           | Snapshot of the prompt fields before the change.                                                       |
| `next_fields`     | `jsonb`                                | no       |                              |          |           |           | Snapshot of the prompt fields after the change.                                                        |
| `created_at`      | `timestamp with time zone`             | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                                                                        |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**
_none_

**Check constraints:**
_none_

**Foreign keys:**

- `community_agent_prompt_changes_changed_by_id_fkey`: `FOREIGN KEY (changed_by_id) REFERENCES users(id) ON DELETE SET NULL`
- `community_agent_prompt_changes_community_id_fkey`: `FOREIGN KEY (community_id) REFERENCES communities(id) ON DELETE CASCADE`

**Indexes:**

- `community_agent_prompt_changes_agent_prompt_id_idx`: `CREATE INDEX community_agent_prompt_changes_agent_prompt_id_idx ON public.community_agent_prompt_changes USING btree (agent_prompt_id, id DESC)`
- `community_agent_prompt_changes_community_id_idx`: `CREATE INDEX community_agent_prompt_changes_community_id_idx ON public.community_agent_prompt_changes USING btree (community_id, id DESC)`
- `community_agent_prompt_changes_pkey`: `CREATE UNIQUE INDEX community_agent_prompt_changes_pkey ON public.community_agent_prompt_changes USING btree (id)`

**Triggers:**
_none_
