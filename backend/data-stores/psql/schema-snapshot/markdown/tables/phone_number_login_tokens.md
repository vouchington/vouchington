# Table `phone_number_login_tokens`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Short-lived tokens for phone-number-based login via SMS/voice.

Not partitioned — growth: unbounded.

| Column         | Type                       | Nullable | Default             | Identity | Generated | Collation | Comment                                                                                    |
| -------------- | -------------------------- | -------- | ------------------- | -------- | --------- | --------- | ------------------------------------------------------------------------------------------ |
| `phone_number` | `text`                     | no       |                     |          |           |           | The phone number this token was sent to.                                                   |
| `token`        | `text`                     | no       |                     |          |           |           | Purpose-bound HMAC-SHA256 hash (uppercase hex) of the verification token sent to the user. |
| `created_at`   | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                                                            |
| `updated_at`   | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                                                            |
| `logged_in_at` | `timestamp with time zone` | yes      |                     |          |           |           | When the token was used to log in. NULL means the token is still pending.                  |

**Primary key:** `PRIMARY KEY (phone_number, token)`

**Unique constraints:**

- `phone_number_login_tokens_token_key`: `UNIQUE (token)`

**Check constraints:**

- `phone_number_login_tokens_phone_number_check`: `CHECK ((char_length(phone_number) <= 255))`
- `phone_number_login_tokens_phone_number_check1`: `CHECK ((phone_number = lower(phone_number)))`
- `phone_number_login_tokens_phone_number_check2`: `CHECK ((phone_number = TRIM(BOTH FROM phone_number)))`
- `phone_number_login_tokens_token_check`: `CHECK ((char_length(token) <= 255))`
- `phone_number_login_tokens_token_check1`: `CHECK ((token = upper(token)))`
- `phone_number_login_tokens_token_check2`: `CHECK ((token = TRIM(BOTH FROM token)))`

**Foreign keys:**
_none_

**Indexes:**

- `idx_phone_number_login_tokens__created_at`: `CREATE INDEX idx_phone_number_login_tokens__created_at ON public.phone_number_login_tokens USING btree (created_at)`
- `idx_phone_number_login_tokens_active`: `CREATE UNIQUE INDEX idx_phone_number_login_tokens_active ON public.phone_number_login_tokens USING btree (phone_number, token) WHERE (logged_in_at IS NULL)`
- `phone_number_login_tokens_pkey`: `CREATE UNIQUE INDEX phone_number_login_tokens_pkey ON public.phone_number_login_tokens USING btree (phone_number, token)`
- `phone_number_login_tokens_token_key`: `CREATE UNIQUE INDEX phone_number_login_tokens_token_key ON public.phone_number_login_tokens USING btree (token)`

**Triggers:**

- `trigger_phone_number_login_tokens_updated_at`: `CREATE TRIGGER trigger_phone_number_login_tokens_updated_at BEFORE UPDATE ON public.phone_number_login_tokens FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
