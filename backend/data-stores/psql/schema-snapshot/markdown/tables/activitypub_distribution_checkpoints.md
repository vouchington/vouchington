# Table `activitypub_distribution_checkpoints`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Durable outbound ActivityPub fan-out cursor. A committed cursor means delivery jobs through that remote actor were accepted by Valkey; completed rows are retained until their source user is deleted.

Not partitioned — growth: unbounded.

| Column                 | Type                       | Nullable | Default                               | Identity | Generated | Collation | Comment                                                                                                                |
| ---------------------- | -------------------------- | -------- | ------------------------------------- | -------- | --------- | --------- | ---------------------------------------------------------------------------------------------------------------------- |
| `activity_id`          | `uuid`                     | no       |                                       |          |           |           | Stable UUID ActivityPub activity identity; one durable distribution cursor per activity.                               |
| `source_user_id`       | `uuid`                     | no       |                                       |          |           |           | Local actor that owns the outbound activity. Cascading deletion removes all of its durable delivery progress.          |
| `last_remote_actor_id` | `uuid`                     | yes      |                                       |          |           |           | Last remote follower whose page was committed. No foreign key: deleting that actor must not rewind a completed cursor. |
| `completed_at`         | `timestamp with time zone` | yes      |                                       |          |           |           | Set after the final follower page commits. Terminal checkpoints intentionally remain for replay protection.            |
| `created_at`           | `timestamp with time zone` | yes      | `uuid_extract_timestamp(activity_id)` |          | virtual   |           |                                                                                                                        |
| `updated_at`           | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP`                   |          |           |           |                                                                                                                        |

**Primary key:** `PRIMARY KEY (activity_id)`

**Unique constraints:**
_none_

**Check constraints:**
_none_

**Foreign keys:**

- `activitypub_distribution_checkpoints_source_user_id_fkey`: `FOREIGN KEY (source_user_id) REFERENCES users(id) ON DELETE CASCADE`

**Indexes:**

- `activitypub_distribution_checkpoints_pkey`: `CREATE UNIQUE INDEX activitypub_distribution_checkpoints_pkey ON public.activitypub_distribution_checkpoints USING btree (activity_id)`
- `idx_activitypub_distribution_checkpoints__source_user_id`: `CREATE INDEX idx_activitypub_distribution_checkpoints__source_user_id ON public.activitypub_distribution_checkpoints USING btree (source_user_id)`

**Triggers:**

- `trigger_activitypub_distribution_checkpoints_updated_at`: `CREATE TRIGGER trigger_activitypub_distribution_checkpoints_updated_at BEFORE UPDATE ON public.activitypub_distribution_checkpoints FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
