# Table `community_saved_replies`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Pre-written reply templates that moderators can insert when responding to modmail threads.

Not partitioned — growth: unbounded.

| Column          | Type                       | Nullable | Default                      | Identity | Generated | Collation | Comment                                                     |
| --------------- | -------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | ----------------------------------------------------------- |
| `id`            | `uuid`                     | no       | `uuidv7()`                   |          |           |           |                                                             |
| `community_id`  | `uuid`                     | no       |                              |          |           |           | The community this saved reply belongs to.                  |
| `title`         | `text`                     | no       | `''::text`                   |          |           |           | Short label for the saved reply, shown in the selection UI. |
| `body`          | `text`                     | no       |                              |          |           |           | Full reply text, up to 5000 characters.                     |
| `order_index`   | `integer`                  | no       | `0`                          |          |           |           | Display order of this reply within the community list.      |
| `created_at`    | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                             |
| `updated_at`    | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP`          |          |           |           |                                                             |
| `created_by_id` | `uuid`                     | yes      |                              |          |           |           | The moderator who created this saved reply.                 |
| `deleted_at`    | `timestamp with time zone` | yes      |                              |          |           |           | When set, this saved reply has been soft-deleted.           |
| `deleted_by_id` | `uuid`                     | yes      |                              |          |           |           | The moderator who deleted this saved reply.                 |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**
_none_

**Check constraints:**

- `chk_community_saved_replies__body`: `CHECK (((char_length(body) >= 1) AND (char_length(body) <= 5000)))`

**Foreign keys:**

- `community_saved_replies_community_id_fkey`: `FOREIGN KEY (community_id) REFERENCES communities(id) ON DELETE CASCADE`
- `community_saved_replies_created_by_id_fkey`: `FOREIGN KEY (created_by_id) REFERENCES users(id) ON DELETE SET NULL`
- `community_saved_replies_deleted_by_id_fkey`: `FOREIGN KEY (deleted_by_id) REFERENCES users(id) ON DELETE SET NULL`

**Indexes:**

- `community_saved_replies_pkey`: `CREATE UNIQUE INDEX community_saved_replies_pkey ON public.community_saved_replies USING btree (id)`
- `idx_community_saved_replies__community_id`: `CREATE INDEX idx_community_saved_replies__community_id ON public.community_saved_replies USING btree (community_id, id DESC) WHERE (deleted_at IS NULL)`
- `idx_community_saved_replies__community_id_bare`: `CREATE INDEX idx_community_saved_replies__community_id_bare ON public.community_saved_replies USING btree (community_id)`
- `idx_community_saved_replies__community_order`: `CREATE UNIQUE INDEX idx_community_saved_replies__community_order ON public.community_saved_replies USING btree (community_id, order_index) WHERE (deleted_at IS NULL)`

**Triggers:**

- `trigger_community_saved_replies_updated_at`: `CREATE TRIGGER trigger_community_saved_replies_updated_at BEFORE UPDATE ON public.community_saved_replies FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
