# Table `url_content_types`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Lookup table of MIME content types for URLs (e.g. text/html, application/pdf).

Not partitioned — growth: bounded.

| Column       | Type                       | Nullable | Default             | Identity | Generated | Collation | Comment                                              |
| ------------ | -------------------------- | -------- | ------------------- | -------- | --------- | --------- | ---------------------------------------------------- |
| `id`         | `bigint`                   | no       |                     | always   |           |           |                                                      |
| `created_at` | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                      |
| `updated_at` | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                      |
| `mime_type`  | `text`                     | no       |                     |          |           |           | Lowercase MIME type string (e.g. text/html). Unique. |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**

- `url_content_types_mime_type_key`: `UNIQUE (mime_type)`

**Check constraints:**

- `url_content_types_mime_type_check`: `CHECK ((mime_type = lower(mime_type)))`
- `url_content_types_mime_type_check1`: `CHECK ((mime_type = TRIM(BOTH FROM mime_type)))`
- `url_content_types_mime_type_check2`: `CHECK ((char_length(mime_type) <= 255))`
- `url_content_types_mime_type_check3`: `CHECK ((mime_type ~ '^\s*[\w.+-]+\/[\w.+-]+\s*$'::text))`

**Foreign keys:**
_none_

**Indexes:**

- `url_content_types_mime_type_key`: `CREATE UNIQUE INDEX url_content_types_mime_type_key ON public.url_content_types USING btree (mime_type)`
- `url_content_types_pkey`: `CREATE UNIQUE INDEX url_content_types_pkey ON public.url_content_types USING btree (id)`

**Triggers:**
_none_
