# Table `moderation_queue_claims`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Advisory claims on community moderation queue items. One live (un-released) claim per report or post-review; auto-expires after 15 minutes of inactivity (evaluated lazily at read time).

Not partitioned — growth: unbounded.

| Column          | Type                       | Nullable | Default                      | Identity | Generated | Collation | Comment                                                                                                                          |
| --------------- | -------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | -------------------------------------------------------------------------------------------------------------------------------- |
| `id`            | `uuid`                     | no       | `uuidv7()`                   |          |           |           |                                                                                                                                  |
| `created_at`    | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                                                                                                  |
| `community_id`  | `uuid`                     | no       |                              |          |           |           | The community whose moderation queue this claim belongs to.                                                                      |
| `report_id`     | `uuid`                     | yes      |                              |          |           |           | The moderation report being claimed. Mutually exclusive with post_id.                                                            |
| `post_id`       | `uuid`                     | yes      |                              |          |           |           | The community post review being claimed (keyed by post_id, the PK of community_post_reviews). Mutually exclusive with report_id. |
| `claimed_by_id` | `uuid`                     | no       |                              |          |           |           | The moderator who holds this claim.                                                                                              |
| `claimed_at`    | `timestamp with time zone` | no       | `now()`                      |          |           |           | When this claim was last set or renewed. Updated on re-claim or takeover of an expired claim.                                    |
| `released_at`   | `timestamp with time zone` | yes      |                              |          |           |           | When this claim was explicitly released. NULL means the claim is live (subject to the 15-minute inactivity window).              |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**
_none_

**Check constraints:**

- `moderation_queue_claims_check`: `CHECK ((num_nonnulls(report_id, post_id) = 1))`

**Foreign keys:**

- `moderation_queue_claims_claimed_by_id_fkey`: `FOREIGN KEY (claimed_by_id) REFERENCES users(id) ON DELETE CASCADE`
- `moderation_queue_claims_community_id_fkey`: `FOREIGN KEY (community_id) REFERENCES communities(id) ON DELETE CASCADE`
- `moderation_queue_claims_post_id_fkey`: `FOREIGN KEY (post_id) REFERENCES posts(id) ON DELETE CASCADE`
- `moderation_queue_claims_report_id_fkey`: `FOREIGN KEY (report_id) REFERENCES moderation_reports(id) ON DELETE CASCADE`

**Indexes:**

- `idx_moderation_queue_claims__claimed_by_id`: `CREATE INDEX idx_moderation_queue_claims__claimed_by_id ON public.moderation_queue_claims USING btree (claimed_by_id) WHERE (claimed_by_id IS NOT NULL)`
- `idx_moderation_queue_claims__post_id`: `CREATE INDEX idx_moderation_queue_claims__post_id ON public.moderation_queue_claims USING btree (post_id) WHERE (post_id IS NOT NULL)`
- `idx_moderation_queue_claims__report_id`: `CREATE INDEX idx_moderation_queue_claims__report_id ON public.moderation_queue_claims USING btree (report_id) WHERE (report_id IS NOT NULL)`
- `idx_modq_claims__community`: `CREATE INDEX idx_modq_claims__community ON public.moderation_queue_claims USING btree (community_id)`
- `moderation_queue_claims_pkey`: `CREATE UNIQUE INDEX moderation_queue_claims_pkey ON public.moderation_queue_claims USING btree (id)`
- `uq_modq_claims__active_post`: `CREATE UNIQUE INDEX uq_modq_claims__active_post ON public.moderation_queue_claims USING btree (post_id) WHERE ((post_id IS NOT NULL) AND (released_at IS NULL))`
- `uq_modq_claims__active_report`: `CREATE UNIQUE INDEX uq_modq_claims__active_report ON public.moderation_queue_claims USING btree (report_id) WHERE ((report_id IS NOT NULL) AND (released_at IS NULL))`

**Triggers:**
_none_
