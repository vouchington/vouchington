# Table `membership_provider_products`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Provider-specific product identities mapped to canonical membership products.

Not partitioned — growth: unbounded.

| Column                  | Type                               | Nullable | Default                      | Identity | Generated | Collation | Comment                                                                            |
| ----------------------- | ---------------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | ---------------------------------------------------------------------------------- |
| `id`                    | `uuid`                             | no       | `uuidv7()`                   |          |           |           |                                                                                    |
| `membership_product_id` | `uuid`                             | no       |                              |          |           |           | Canonical product delivered by this provider product identity.                     |
| `provider`              | `membership_provider_kinds`        | no       |                              |          |           |           | Store or payment provider that owns this product identity.                         |
| `environment`           | `membership_provider_environments` | no       |                              |          |           |           | Provider environment in which this product identity is valid.                      |
| `application_id`        | `text`                             | no       |                              |          |           |           | Provider application, bundle, or tenant identifier.                                |
| `provider_product_id`   | `text`                             | no       |                              |          |           |           | Provider product identifier.                                                       |
| `base_plan_id`          | `text`                             | yes      |                              |          |           |           | Optional provider base-plan identity.                                              |
| `offer_id`              | `text`                             | yes      |                              |          |           |           | Optional provider offer identity.                                                  |
| `sku_id`                | `text`                             | yes      |                              |          |           |           | Optional provider SKU identity.                                                    |
| `price_minor_units`     | `bigint`                           | yes      |                              |          |           |           | Displayed product price in the currency minor unit, when the provider exposes one. |
| `currency_code`         | `text`                             | yes      |                              |          |           |           | ISO currency code for the displayed provider price, when the provider exposes one. |
| `retired_at`            | `timestamp with time zone`         | yes      |                              |          |           |           | When this mapping stopped accepting new provider purchases.                        |
| `created_at`            | `timestamp with time zone`         | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                                                    |
| `updated_at`            | `timestamp with time zone`         | no       | `CURRENT_TIMESTAMP`          |          |           |           |                                                                                    |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**
_none_

**Check constraints:**

- `membership_provider_products_application_id_check`: `CHECK ((((char_length(application_id) >= 1) AND (char_length(application_id) <= 255)) AND (application_id = TRIM(BOTH FROM application_id))))`
- `membership_provider_products_base_plan_id_check`: `CHECK (((base_plan_id IS NULL) OR (((char_length(base_plan_id) >= 1) AND (char_length(base_plan_id) <= 255)) AND (base_plan_id = TRIM(BOTH FROM base_plan_id)))))`
- `membership_provider_products_check`: `CHECK ((num_nonnulls(price_minor_units, currency_code) = ANY (ARRAY[0, 2])))`
- `membership_provider_products_offer_id_check`: `CHECK (((offer_id IS NULL) OR (((char_length(offer_id) >= 1) AND (char_length(offer_id) <= 255)) AND (offer_id = TRIM(BOTH FROM offer_id)))))`
- `membership_provider_products_price_minor_units_check`: `CHECK (((price_minor_units >= 0) AND (price_minor_units <= '9007199254740991'::bigint)))`
- `membership_provider_products_provider_check`: `CHECK ((provider <> 'admin'::membership_provider_kinds))`
- `membership_provider_products_provider_product_id_check`: `CHECK ((((char_length(provider_product_id) >= 1) AND (char_length(provider_product_id) <= 255)) AND (provider_product_id = TRIM(BOTH FROM provider_product_id))))`
- `membership_provider_products_sku_id_check`: `CHECK (((sku_id IS NULL) OR (((char_length(sku_id) >= 1) AND (char_length(sku_id) <= 255)) AND (sku_id = TRIM(BOTH FROM sku_id)))))`

**Foreign keys:**

- `membership_provider_products_currency_code_fkey`: `FOREIGN KEY (currency_code) REFERENCES currencies(code) ON DELETE RESTRICT`
- `membership_provider_products_membership_product_id_fkey`: `FOREIGN KEY (membership_product_id) REFERENCES membership_products(id) ON DELETE RESTRICT`

**Indexes:**

- `idx_membership_provider_products__currency_code`: `CREATE INDEX idx_membership_provider_products__currency_code ON public.membership_provider_products USING btree (currency_code)`
- `idx_membership_provider_products__id_context`: `CREATE UNIQUE INDEX idx_membership_provider_products__id_context ON public.membership_provider_products USING btree (id, membership_product_id, provider, environment, application_id)`
- `idx_membership_provider_products__membership_product_id`: `CREATE INDEX idx_membership_provider_products__membership_product_id ON public.membership_provider_products USING btree (membership_product_id)`
- `idx_membership_provider_products__provider_identity`: `CREATE UNIQUE INDEX idx_membership_provider_products__provider_identity ON public.membership_provider_products USING btree (provider, environment, application_id, provider_product_id, base_plan_id, offer_id, sku_id) NULLS NOT DISTINCT`
- `membership_provider_products_pkey`: `CREATE UNIQUE INDEX membership_provider_products_pkey ON public.membership_provider_products USING btree (id)`

**Triggers:**

- `trigger_membership_provider_products_updated_at`: `CREATE TRIGGER trigger_membership_provider_products_updated_at BEFORE UPDATE ON public.membership_provider_products FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
