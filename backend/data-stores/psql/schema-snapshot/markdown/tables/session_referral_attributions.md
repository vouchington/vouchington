# Table `session_referral_attributions`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Tracks which user referred a session, linking anonymous sessions to referrers for attribution. RANGE-partitioned by id with a default partition only; split later by adding explicit range partitions.

RANGE partitioned on `id` (children: default, no retention owner, access class: target-scoped, growth: unbounded).

| Column         | Type                       | Nullable | Default                      | Identity | Generated | Collation | Comment                                                                                  |
| -------------- | -------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | ---------------------------------------------------------------------------------------- |
| `id`           | `uuid`                     | no       | `uuidv7()`                   |          |           |           |                                                                                          |
| `session_id`   | `uuid`                     | no       |                              |          |           |           | The anonymous session that was referred. Not a FK — sessions live outside PostgreSQL.    |
| `referrer_id`  | `uuid`                     | yes      |                              |          |           |           | The user who referred this session. NULL if the referrer deleted their account.          |
| `user_id`      | `uuid`                     | yes      |                              |          |           |           | The user who signed up from this referral. NULL until signup or if user deletes account. |
| `landing_url`  | `text`                     | no       |                              |          |           |           | The URL the referred session landed on (max 2048 chars).                                 |
| `utm_source`   | `text`                     | yes      |                              |          |           |           | UTM source parameter from the referral URL (e.g. google, newsletter).                    |
| `utm_medium`   | `text`                     | yes      |                              |          |           |           | UTM medium parameter from the referral URL (e.g. cpc, email).                            |
| `utm_campaign` | `text`                     | yes      |                              |          |           |           | UTM campaign name from the referral URL.                                                 |
| `utm_content`  | `text`                     | yes      |                              |          |           |           | UTM content parameter, used to differentiate links within the same campaign.             |
| `signed_up_at` | `timestamp with time zone` | yes      |                              |          |           |           | When the referred session converted to a signup. NULL until the user registers.          |
| `created_at`   | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                                                          |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**
_none_

**Check constraints:**

- `chk_sra_utm_campaign`: `CHECK (((utm_campaign IS NULL) OR ((char_length(utm_campaign) <= 255) AND (utm_campaign = lower(TRIM(BOTH FROM utm_campaign))))))`
- `chk_sra_utm_content`: `CHECK (((utm_content IS NULL) OR ((char_length(utm_content) <= 255) AND (utm_content = lower(TRIM(BOTH FROM utm_content))))))`
- `chk_sra_utm_medium`: `CHECK (((utm_medium IS NULL) OR ((char_length(utm_medium) <= 255) AND (utm_medium = lower(TRIM(BOTH FROM utm_medium))))))`
- `chk_sra_utm_source`: `CHECK (((utm_source IS NULL) OR ((char_length(utm_source) <= 255) AND (utm_source = lower(TRIM(BOTH FROM utm_source))))))`
- `session_referral_attributions_check`: `CHECK ((referrer_id <> user_id))`
- `session_referral_attributions_landing_url_check`: `CHECK ((char_length(landing_url) <= 2048))`

**Foreign keys:**

- `session_referral_attributions_referrer_id_fkey`: `FOREIGN KEY (referrer_id) REFERENCES users(id) ON DELETE SET NULL`
- `session_referral_attributions_user_id_fkey`: `FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE SET NULL`

**Indexes:**

- `idx_session_referral_attributions__referrer_id`: `CREATE INDEX idx_session_referral_attributions__referrer_id ON ONLY public.session_referral_attributions USING btree (referrer_id) WHERE (referrer_id IS NOT NULL)`
- `idx_session_referral_attributions__session_id_id`: `CREATE INDEX idx_session_referral_attributions__session_id_id ON ONLY public.session_referral_attributions USING btree (session_id, id)`
- `idx_session_referral_attributions__user_id`: `CREATE INDEX idx_session_referral_attributions__user_id ON ONLY public.session_referral_attributions USING btree (user_id) WHERE (user_id IS NOT NULL)`
- `session_referral_attributions_pkey`: `CREATE UNIQUE INDEX session_referral_attributions_pkey ON ONLY public.session_referral_attributions USING btree (id)`

**Triggers:**
_none_
