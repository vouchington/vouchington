# Table `user_email_addresses`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Email addresses associated with user accounts. Composite PK on (user_id, email_address).

Not partitioned — growth: unbounded.

| Column          | Type                       | Nullable | Default             | Identity | Generated | Collation | Comment                                           |
| --------------- | -------------------------- | -------- | ------------------- | -------- | --------- | --------- | ------------------------------------------------- |
| `user_id`       | `uuid`                     | no       |                     |          |           |           | The user who owns this email address.             |
| `email_address` | `text`                     | no       |                     |          |           |           | Normalized (lowercase, trimmed) email address.    |
| `created_at`    | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                   |
| `updated_at`    | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                   |
| `is_primary`    | `boolean`                  | yes      | `true`              |          |           |           | Whether this is the user's primary email address. |

**Primary key:** `PRIMARY KEY (user_id, email_address)`

**Unique constraints:**
_none_

**Check constraints:**

- `user_email_addresses_email_address_check`: `CHECK ((char_length(email_address) <= 255))`
- `user_email_addresses_email_address_check1`: `CHECK ((email_address = lower(email_address)))`
- `user_email_addresses_email_address_check2`: `CHECK ((email_address = TRIM(BOTH FROM email_address)))`

**Foreign keys:**

- `user_email_addresses_user_id_fkey`: `FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE`

**Indexes:**

- `idx_user_email_addresses__email`: `CREATE INDEX idx_user_email_addresses__email ON public.user_email_addresses USING btree (email_address)`
- `idx_user_email_addresses__user_primary_created_email`: `CREATE INDEX idx_user_email_addresses__user_primary_created_email ON public.user_email_addresses USING btree (user_id, ((is_primary)::integer) DESC, created_at, email_address)`
- `idx_user_email_addresses_email_primary`: `CREATE UNIQUE INDEX idx_user_email_addresses_email_primary ON public.user_email_addresses USING btree (email_address) WHERE (is_primary = true)`
- `idx_user_email_addresses_user_primary`: `CREATE UNIQUE INDEX idx_user_email_addresses_user_primary ON public.user_email_addresses USING btree (user_id) WHERE (is_primary = true)`
- `user_email_addresses_pkey`: `CREATE UNIQUE INDEX user_email_addresses_pkey ON public.user_email_addresses USING btree (user_id, email_address)`

**Triggers:**

- `trigger_user_email_addresses_updated_at`: `CREATE TRIGGER trigger_user_email_addresses_updated_at BEFORE UPDATE ON public.user_email_addresses FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
