# Table `oauth_authorization_requests`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Short-lived browser consent requests bound to one Voucha user session.

Not partitioned — growth: bounded.

| Column                 | Type                       | Nullable | Default                      | Identity | Generated | Collation | Comment                                                                   |
| ---------------------- | -------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | ------------------------------------------------------------------------- |
| `id`                   | `uuid`                     | no       | `uuidv7()`                   |          |           |           |                                                                           |
| `client_id`            | `uuid`                     | no       |                              |          |           |           | Client requesting delegated authorization.                                |
| `user_id`              | `uuid`                     | no       |                              |          |           |           | Authenticated user who owns the consent decision.                         |
| `browser_binding_hash` | `text`                     | no       |                              |          |           |           | Purpose-bound hash binding the request to one browser device and session. |
| `redirect_uri`         | `text`                     | no       |                              |          |           |           | Validated callback URI for this authorization request.                    |
| `state`                | `text`                     | no       |                              |          |           |           | Opaque client state returned unchanged to the callback.                   |
| `resource`             | `text`                     | no       |                              |          |           |           | Protected resource audience requested by the client.                      |
| `scopes`               | `text[]`                   | no       |                              |          |           |           | Canonical scopes presented for user consent.                              |
| `code_challenge`       | `text`                     | no       |                              |          |           |           | Base64url SHA-256 PKCE challenge for the authorization code.              |
| `expires_at`           | `timestamp with time zone` | no       |                              |          |           |           | Time after which the pending request cannot be decided.                   |
| `approved_at`          | `timestamp with time zone` | yes      |                              |          |           |           | Time at which the owning user approved the request.                       |
| `denied_at`            | `timestamp with time zone` | yes      |                              |          |           |           | Time at which the request was denied or superseded.                       |
| `created_at`           | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                                           |
| `updated_at`           | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP`          |          |           |           |                                                                           |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**
_none_

**Check constraints:**

- `oauth_authorization_requests_browser_binding_hash_check`: `CHECK ((char_length(browser_binding_hash) = 64))`
- `oauth_authorization_requests_check`: `CHECK ((num_nonnulls(approved_at, denied_at) <= 1))`
- `oauth_authorization_requests_code_challenge_check`: `CHECK ((code_challenge ~ '^[A-Za-z0-9_-]{43}$'::text))`
- `oauth_authorization_requests_redirect_uri_check`: `CHECK (((char_length(redirect_uri) >= 1) AND (char_length(redirect_uri) <= 2048)))`
- `oauth_authorization_requests_resource_check`: `CHECK (((char_length(resource) >= 1) AND (char_length(resource) <= 2048)))`
- `oauth_authorization_requests_scopes_check`: `CHECK ((cardinality(scopes) > 0))`
- `oauth_authorization_requests_state_check`: `CHECK (((char_length(state) >= 1) AND (char_length(state) <= 1024)))`

**Foreign keys:**

- `oauth_authorization_requests_client_id_fkey`: `FOREIGN KEY (client_id) REFERENCES oauth_clients(id) ON DELETE CASCADE`
- `oauth_authorization_requests_user_id_fkey`: `FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE`

**Indexes:**

- `idx_oauth_authorization_requests__browser`: `CREATE INDEX idx_oauth_authorization_requests__browser ON public.oauth_authorization_requests USING btree (browser_binding_hash, client_id, id) WHERE ((approved_at IS NULL) AND (denied_at IS NULL))`
- `idx_oauth_authorization_requests__client`: `CREATE INDEX idx_oauth_authorization_requests__client ON public.oauth_authorization_requests USING btree (client_id, id)`
- `idx_oauth_authorization_requests__expiry`: `CREATE INDEX idx_oauth_authorization_requests__expiry ON public.oauth_authorization_requests USING btree (expires_at, id)`
- `idx_oauth_authorization_requests__user`: `CREATE INDEX idx_oauth_authorization_requests__user ON public.oauth_authorization_requests USING btree (user_id, id)`
- `oauth_authorization_requests_pkey`: `CREATE UNIQUE INDEX oauth_authorization_requests_pkey ON public.oauth_authorization_requests USING btree (id)`

**Triggers:**

- `trigger_oauth_authorization_requests_updated_at`: `CREATE TRIGGER trigger_oauth_authorization_requests_updated_at BEFORE UPDATE ON public.oauth_authorization_requests FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
