# Table `crm_contacts`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

CRM contact records for influencers, customers, and partners.

Not partitioned — growth: unbounded.

| Column                       | Type                       | Nullable | Default                           | Identity | Generated | Collation | Comment                                                          |
| ---------------------------- | -------------------------- | -------- | --------------------------------- | -------- | --------- | --------- | ---------------------------------------------------------------- |
| `id`                         | `uuid`                     | no       | `uuidv7()`                        |          |           |           |                                                                  |
| `name`                       | `text`                     | no       |                                   |          |           |           | Full display name of the contact.                                |
| `email`                      | `text`                     | no       |                                   |          |           |           | Primary email address, unique among non-archived contacts.       |
| `phone`                      | `text`                     | yes      |                                   |          |           |           | Optional phone number.                                           |
| `vertical`                   | `crm_contact_verticals`    | yes      |                                   |          |           |           | Industry vertical the contact operates in.                       |
| `contact_type`               | `crm_contact_types`        | no       | `'influencer'::crm_contact_types` |          |           |           | Classification of the contact: influencer, customer, or partner. |
| `source`                     | `crm_contact_sources`      | no       | `'manual'::crm_contact_sources`   |          |           |           | How the contact was added to the CRM.                            |
| `follower_count`             | `integer`                  | yes      |                                   |          |           |           | Approximate total social media follower count.                   |
| `notes`                      | `text`                     | yes      |                                   |          |           |           | Free-form internal notes about the contact.                      |
| `metadata`                   | `jsonb`                    | yes      |                                   |          |           |           | Additional structured metadata (e.g. from CSV import).           |
| `user_id`                    | `uuid`                     | yes      |                                   |          |           |           | Linked Voucha user account, set when contact converts.           |
| `assigned_to_id`             | `uuid`                     | yes      |                                   |          |           |           | Admin user responsible for this contact.                         |
| `created_by_id`              | `uuid`                     | no       |                                   |          |           |           | Admin user who created this contact record.                      |
| `contacted_at`               | `timestamp with time zone` | yes      |                                   |          |           |           | When the first outbound message was sent to this contact.        |
| `responded_at`               | `timestamp with time zone` | yes      |                                   |          |           |           | When the contact first replied.                                  |
| `converted_at`               | `timestamp with time zone` | yes      |                                   |          |           |           | When the contact signed up / converted.                          |
| `opted_out_at`               | `timestamp with time zone` | yes      |                                   |          |           |           | When the contact opted out of further communication.             |
| `archived_at`                | `timestamp with time zone` | yes      |                                   |          |           |           | When the contact was archived and removed from active lists.     |
| `latest_lifecycle_change_id` | `uuid`                     | yes      |                                   |          |           |           | Latest append-only lifecycle transition for this CRM contact.    |
| `created_at`                 | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)`      |          | virtual   |           | Timestamp derived from the UUIDv7 id.                            |
| `updated_at`                 | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP`               |          |           |           | Last modification timestamp, maintained by trigger.              |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**
_none_

**Check constraints:**

- `chk_crm_contacts__email_not_empty`: `CHECK ((TRIM(BOTH FROM email) <> ''::text))`
- `crm_contacts_email_check`: `CHECK ((email = lower(TRIM(BOTH FROM email))))`
- `crm_contacts_email_check1`: `CHECK ((char_length(email) <= 320))`
- `crm_contacts_follower_count_check`: `CHECK ((follower_count >= 0))`
- `crm_contacts_name_check`: `CHECK ((name = TRIM(BOTH FROM name)))`
- `crm_contacts_name_check1`: `CHECK (((char_length(name) >= 1) AND (char_length(name) <= 500)))`

**Foreign keys:**

- `crm_contacts_assigned_to_id_fkey`: `FOREIGN KEY (assigned_to_id) REFERENCES users(id) ON DELETE SET NULL`
- `crm_contacts_created_by_id_fkey`: `FOREIGN KEY (created_by_id) REFERENCES users(id) ON DELETE RESTRICT`
- `crm_contacts_user_id_fkey`: `FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE SET NULL`
- `fk_crm_contacts_latest_lifecycle_change_id`: `FOREIGN KEY (id, latest_lifecycle_change_id) REFERENCES crm_contact_lifecycle_changes(crm_contact_id, id) ON DELETE SET NULL (latest_lifecycle_change_id)`

**Indexes:**

- `crm_contacts_pkey`: `CREATE UNIQUE INDEX crm_contacts_pkey ON public.crm_contacts USING btree (id)`
- `idx_crm_contacts__assigned_to_id`: `CREATE INDEX idx_crm_contacts__assigned_to_id ON public.crm_contacts USING btree (assigned_to_id) WHERE (assigned_to_id IS NOT NULL)`
- `idx_crm_contacts__created_by_id`: `CREATE INDEX idx_crm_contacts__created_by_id ON public.crm_contacts USING btree (created_by_id) WHERE (created_by_id IS NOT NULL)`
- `idx_crm_contacts__email`: `CREATE UNIQUE INDEX idx_crm_contacts__email ON public.crm_contacts USING btree (email) WHERE (archived_at IS NULL)`
- `idx_crm_contacts__email_trgm`: `CREATE INDEX idx_crm_contacts__email_trgm ON public.crm_contacts USING gin (email gin_trgm_ops)`
- `idx_crm_contacts__name__id`: `CREATE INDEX idx_crm_contacts__name__id ON public.crm_contacts USING btree (name, id)`
- `idx_crm_contacts__name_trgm`: `CREATE INDEX idx_crm_contacts__name_trgm ON public.crm_contacts USING gin (name gin_trgm_ops)`
- `idx_crm_contacts__user_id`: `CREATE INDEX idx_crm_contacts__user_id ON public.crm_contacts USING btree (user_id) WHERE (user_id IS NOT NULL)`
- `idx_crm_contacts__vertical`: `CREATE INDEX idx_crm_contacts__vertical ON public.crm_contacts USING btree (vertical) WHERE (vertical IS NOT NULL)`

**Triggers:**

- `trigger_crm_contacts_updated_at`: `CREATE TRIGGER trigger_crm_contacts_updated_at BEFORE UPDATE ON public.crm_contacts FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
