# Table `countries`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Lookup table of supported countries with ISO 3166-1 alpha-2 codes.

Not partitioned — growth: bounded.

| Column       | Type                       | Nullable | Default             | Identity | Generated | Collation | Comment                                             |
| ------------ | -------------------------- | -------- | ------------------- | -------- | --------- | --------- | --------------------------------------------------- |
| `id`         | `smallint`                 | no       |                     | always   |           |           |                                                     |
| `code`       | `text`                     | no       |                     |          |           |           | ISO 3166-1 alpha-2 country code (e.g., US, GB, JP). |
| `name`       | `text`                     | no       |                     |          |           |           | Full country name in English.                       |
| `created_at` | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                     |
| `updated_at` | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                     |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**

- `countries_code_key`: `UNIQUE (code)`

**Check constraints:**

- `countries_code_check`: `CHECK ((code ~ '^[A-Z]{2}$'::text))`
- `countries_code_check1`: `CHECK ((code = upper(code)))`
- `countries_code_check2`: `CHECK ((code = TRIM(BOTH FROM code)))`
- `countries_name_check`: `CHECK ((char_length(name) <= 255))`
- `countries_name_check1`: `CHECK ((name = TRIM(BOTH FROM name)))`

**Foreign keys:**
_none_

**Indexes:**

- `countries_code_key`: `CREATE UNIQUE INDEX countries_code_key ON public.countries USING btree (code)`
- `countries_pkey`: `CREATE UNIQUE INDEX countries_pkey ON public.countries USING btree (id)`

**Triggers:**

- `trigger_countries_updated_at`: `CREATE TRIGGER trigger_countries_updated_at BEFORE UPDATE ON public.countries FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
