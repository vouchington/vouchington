# Table `support_contacts`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Individuals who have interacted with customer support. May be linked to a registered user.

Not partitioned — growth: unbounded.

| Column          | Type                       | Nullable | Default                      | Identity | Generated | Collation | Comment                                          |
| --------------- | -------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | ------------------------------------------------ |
| `id`            | `uuid`                     | no       | `uuidv7()`                   |          |           |           |                                                  |
| `email_address` | `text`                     | no       |                              |          |           |           | Unique lowercase email address for this contact. |
| `name`          | `text`                     | no       | `''::text`                   |          |           |           | Display name of the contact.                     |
| `user_id`       | `uuid`                     | yes      |                              |          |           |           | Linked registered user account, if any.          |
| `notes`         | `text`                     | no       | `''::text`                   |          |           |           | Internal admin notes about this contact.         |
| `created_at`    | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                  |
| `updated_at`    | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP`          |          |           |           |                                                  |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**
_none_

**Check constraints:**

- `chk_support_contacts__email_address_length`: `CHECK ((length(email_address) <= 320))`
- `chk_support_contacts__email_address_lowercase`: `CHECK ((email_address = lower(TRIM(BOTH FROM email_address))))`
- `chk_support_contacts__email_address_not_empty`: `CHECK ((TRIM(BOTH FROM email_address) <> ''::text))`

**Foreign keys:**

- `support_contacts_user_id_fkey`: `FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE SET NULL`

**Indexes:**

- `idx_support_contacts__email_address`: `CREATE UNIQUE INDEX idx_support_contacts__email_address ON public.support_contacts USING btree (email_address)`
- `idx_support_contacts__email_address_trgm`: `CREATE INDEX idx_support_contacts__email_address_trgm ON public.support_contacts USING gin (email_address gin_trgm_ops)`
- `idx_support_contacts__name_trgm`: `CREATE INDEX idx_support_contacts__name_trgm ON public.support_contacts USING gin (name gin_trgm_ops)`
- `idx_support_contacts__user_id`: `CREATE INDEX idx_support_contacts__user_id ON public.support_contacts USING btree (user_id) WHERE (user_id IS NOT NULL)`
- `support_contacts_pkey`: `CREATE UNIQUE INDEX support_contacts_pkey ON public.support_contacts USING btree (id)`

**Triggers:**

- `trigger_support_contacts_updated_at`: `CREATE TRIGGER trigger_support_contacts_updated_at BEFORE UPDATE ON public.support_contacts FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
