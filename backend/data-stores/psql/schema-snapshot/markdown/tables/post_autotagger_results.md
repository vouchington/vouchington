# Table `post_autotagger_results`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Autotagger LLM results for posts. Keyed by (post, prompt). RANGE-partitioned by post_id.

RANGE partitioned on `post_id` (children: default, no retention owner, access class: target-scoped, growth: unbounded).

| Column           | Type                       | Nullable | Default             | Identity | Generated | Collation | Comment                                        |
| ---------------- | -------------------------- | -------- | ------------------- | -------- | --------- | --------- | ---------------------------------------------- |
| `post_id`        | `uuid`                     | no       |                     |          |           |           | The post that was auto-tagged.                 |
| `prompt_id`      | `uuid`                     | no       |                     |          |           |           | The agent prompt version used for tagging.     |
| `content_sha256` | `bytea`                    | no       |                     |          |           |           | SHA-256 of the post content that was analyzed. |
| `created_at`     | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                |
| `created_by_id`  | `uuid`                     | yes      |                     |          |           |           |                                                |
| `updated_at`     | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                |
| `updated_by_id`  | `uuid`                     | yes      |                     |          |           |           |                                                |
| `deleted_at`     | `timestamp with time zone` | yes      |                     |          |           |           |                                                |
| `deleted_by_id`  | `uuid`                     | yes      |                     |          |           |           |                                                |

**Primary key:** `PRIMARY KEY (post_id, prompt_id)`

**Unique constraints:**
_none_

**Check constraints:**

- `post_autotagger_results_content_sha256_check`: `CHECK ((octet_length(content_sha256) = 32))`

**Foreign keys:**

- `post_autotagger_results_created_by_id_fkey`: `FOREIGN KEY (created_by_id) REFERENCES users(id) ON DELETE SET NULL`
- `post_autotagger_results_deleted_by_id_fkey`: `FOREIGN KEY (deleted_by_id) REFERENCES users(id) ON DELETE SET NULL`
- `post_autotagger_results_post_id_fkey`: `FOREIGN KEY (post_id) REFERENCES posts(id) ON DELETE CASCADE`
- `post_autotagger_results_prompt_id_fkey`: `FOREIGN KEY (prompt_id) REFERENCES agent_prompts(id) ON DELETE CASCADE`
- `post_autotagger_results_updated_by_id_fkey`: `FOREIGN KEY (updated_by_id) REFERENCES users(id) ON DELETE SET NULL`

**Indexes:**

- `idx_post_autotagger_results__lookup`: `CREATE INDEX idx_post_autotagger_results__lookup ON ONLY public.post_autotagger_results USING btree (post_id, prompt_id) WHERE (deleted_at IS NULL)`
- `idx_post_autotagger_results__prompt_id`: `CREATE INDEX idx_post_autotagger_results__prompt_id ON ONLY public.post_autotagger_results USING btree (prompt_id)`
- `post_autotagger_results_pkey`: `CREATE UNIQUE INDEX post_autotagger_results_pkey ON ONLY public.post_autotagger_results USING btree (post_id, prompt_id)`

**Triggers:**

- `trigger_post_autotagger_results_updated_at`: `CREATE TRIGGER trigger_post_autotagger_results_updated_at BEFORE UPDATE ON public.post_autotagger_results FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
