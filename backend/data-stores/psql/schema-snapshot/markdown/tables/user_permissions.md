# Table `user_permissions`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Direct permission grants to individual users, bypassing roles.

Not partitioned — growth: unbounded.

| Column               | Type                       | Nullable | Default             | Identity | Generated | Collation | Comment                                   |
| -------------------- | -------------------------- | -------- | ------------------- | -------- | --------- | --------- | ----------------------------------------- |
| `user_id`            | `uuid`                     | no       |                     |          |           |           | The user who has this permission.         |
| `permission_type_id` | `bigint`                   | no       |                     |          |           |           | The permission type granted to this user. |
| `created_at`         | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                           |
| `updated_at`         | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                           |

**Primary key:** `PRIMARY KEY (user_id, permission_type_id)`

**Unique constraints:**
_none_

**Check constraints:**
_none_

**Foreign keys:**

- `user_permissions_permission_type_id_fkey`: `FOREIGN KEY (permission_type_id) REFERENCES user_permission_types(id) ON DELETE CASCADE`
- `user_permissions_user_id_fkey`: `FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE`

**Indexes:**

- `idx_user_permissions__permission_type_id`: `CREATE INDEX idx_user_permissions__permission_type_id ON public.user_permissions USING btree (permission_type_id) WHERE (permission_type_id IS NOT NULL)`
- `user_permissions_pkey`: `CREATE UNIQUE INDEX user_permissions_pkey ON public.user_permissions USING btree (user_id, permission_type_id)`

**Triggers:**

- `trigger_user_permissions_updated_at`: `CREATE TRIGGER trigger_user_permissions_updated_at BEFORE UPDATE ON public.user_permissions FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
