# Table `individual_cards`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Credit/debit cards held by individuals. An individual can hold multiple cards of the same type.

Not partitioned — growth: unbounded.

| Column                      | Type                       | Nullable | Default                      | Identity | Generated | Collation | Comment                                                          |
| --------------------------- | -------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | ---------------------------------------------------------------- |
| `id`                        | `uuid`                     | no       | `uuidv7()`                   |          |           |           |                                                                  |
| `individual_id`             | `uuid`                     | no       |                              |          |           |           | The individual who holds this card.                              |
| `card_id`                   | `uuid`                     | no       |                              |          |           |           | The card topic (references topics where topic_type = 'card').    |
| `opened_on`                 | `date`                     | yes      |                              |          |           |           | Date the card account was opened.                                |
| `closed_on`                 | `date`                     | yes      |                              |          |           |           | Date the card account was closed. NULL if still open.            |
| `credit_limit_minor_units`  | `bigint`                   | yes      |                              |          |           |           | Credit limit in the currency minor unit.                         |
| `currency_code`             | `text`                     | yes      |                              |          |           |           | Currency for the credit limit.                                   |
| `received_sign_up_bonus_on` | `date`                     | yes      |                              |          |           |           | Date the sign-up bonus was received.                             |
| `is_authorized_user`        | `boolean`                  | yes      | `false`                      |          |           |           | TRUE if this card is an authorized user card on another account. |
| `authorized_user_of_id`     | `uuid`                     | yes      |                              |          |           |           | The primary cardholder's individual_cards row, if this is an AU. |
| `note`                      | `text`                     | yes      |                              |          |           |           | Free-text note about this card.                                  |
| `created_at`                | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                                  |
| `updated_at`                | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP`          |          |           |           |                                                                  |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**
_none_

**Check constraints:**

- `individual_cards_check`: `CHECK (((credit_limit_minor_units IS NULL) = (currency_code IS NULL)))`
- `individual_cards_check1`: `CHECK ((NOT ((authorized_user_of_id IS NOT NULL) AND (is_authorized_user IS FALSE))))`
- `individual_cards_credit_limit_minor_units_check`: `CHECK (((credit_limit_minor_units >= 0) AND (credit_limit_minor_units <= '9007199254740991'::bigint)))`

**Foreign keys:**

- `individual_cards_authorized_user_of_id_fkey`: `FOREIGN KEY (authorized_user_of_id) REFERENCES individual_cards(id) ON DELETE SET NULL`
- `individual_cards_card_id_fkey`: `FOREIGN KEY (card_id) REFERENCES topics(id) ON DELETE CASCADE`
- `individual_cards_currency_code_fkey`: `FOREIGN KEY (currency_code) REFERENCES currencies(code) ON DELETE RESTRICT`
- `individual_cards_individual_id_fkey`: `FOREIGN KEY (individual_id) REFERENCES individuals(id) ON DELETE CASCADE`

**Indexes:**

- `idx_individual_cards__authorized_user_of_id`: `CREATE INDEX idx_individual_cards__authorized_user_of_id ON public.individual_cards USING btree (authorized_user_of_id) WHERE (authorized_user_of_id IS NOT NULL)`
- `idx_individual_cards__individual_id_id`: `CREATE INDEX idx_individual_cards__individual_id_id ON public.individual_cards USING btree (individual_id, id)`
- `individual_cards__card_id`: `CREATE INDEX individual_cards__card_id ON public.individual_cards USING btree (card_id)`
- `individual_cards__currency_code`: `CREATE INDEX individual_cards__currency_code ON public.individual_cards USING btree (currency_code) WHERE (currency_code IS NOT NULL)`
- `individual_cards_pkey`: `CREATE UNIQUE INDEX individual_cards_pkey ON public.individual_cards USING btree (id)`

**Triggers:**

- `trigger_individual_cards_fn_update_updated_at`: `CREATE TRIGGER trigger_individual_cards_fn_update_updated_at BEFORE UPDATE ON public.individual_cards FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
