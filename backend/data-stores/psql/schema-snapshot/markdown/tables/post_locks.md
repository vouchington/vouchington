# Table `post_locks`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Append-only history of post locks. A post is locked as long as any active row (lifted_at IS NULL) exists. Prevents new replies. Canonical insert-then-cancel pattern. See community_bans for reference.

Not partitioned — growth: unbounded.

| Column         | Type                       | Nullable | Default                      | Identity | Generated | Collation | Comment                                            |
| -------------- | -------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | -------------------------------------------------- |
| `id`           | `uuid`                     | no       | `uuidv7()`                   |          |           |           |                                                    |
| `post_id`      | `uuid`                     | no       |                              |          |           |           | The locked post.                                   |
| `locked_by_id` | `uuid`                     | yes      |                              |          |           |           | Moderator or author who locked the post.           |
| `reason`       | `text`                     | yes      |                              |          |           |           | Optional reason for the lock.                      |
| `created_at`   | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                    |
| `updated_at`   | `timestamp with time zone` | yes      | `CURRENT_TIMESTAMP`          |          |           |           |                                                    |
| `lifted_at`    | `timestamp with time zone` | yes      |                              |          |           |           | When the lock was lifted. NULL means still active. |
| `lifted_by_id` | `uuid`                     | yes      |                              |          |           |           | Who lifted the lock.                               |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**
_none_

**Check constraints:**
_none_

**Foreign keys:**

- `post_locks_lifted_by_id_fkey`: `FOREIGN KEY (lifted_by_id) REFERENCES users(id) ON DELETE SET NULL`
- `post_locks_locked_by_id_fkey`: `FOREIGN KEY (locked_by_id) REFERENCES users(id) ON DELETE SET NULL`
- `post_locks_post_id_fkey`: `FOREIGN KEY (post_id) REFERENCES posts(id) ON DELETE CASCADE`

**Indexes:**

- `idx_post_locks__post`: `CREATE INDEX idx_post_locks__post ON public.post_locks USING btree (post_id) WHERE (lifted_at IS NULL)`
- `idx_post_locks__post_id`: `CREATE INDEX idx_post_locks__post_id ON public.post_locks USING btree (post_id) WHERE (post_id IS NOT NULL)`
- `post_locks_pkey`: `CREATE UNIQUE INDEX post_locks_pkey ON public.post_locks USING btree (id)`

**Triggers:**

- `trigger_post_locks_updated_at`: `CREATE TRIGGER trigger_post_locks_updated_at BEFORE UPDATE ON public.post_locks FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
