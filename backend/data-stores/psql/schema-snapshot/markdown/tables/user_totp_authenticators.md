# Table `user_totp_authenticators`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

TOTP authenticator apps registered by users for multi-factor authentication. Any single verified authenticator satisfies the MFA requirement.

Not partitioned — growth: unbounded.

| Column              | Type                       | Nullable | Default                      | Identity | Generated | Collation | Comment                                                                                                                                                        |
| ------------------- | -------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `id`                | `uuid`                     | no       | `uuidv7()`                   |          |           |           |                                                                                                                                                                |
| `user_id`           | `uuid`                     | no       |                              |          |           |           | The user who registered this TOTP authenticator.                                                                                                               |
| `secret_ciphertext` | `text`                     | no       |                              |          |           |           | Encrypted TOTP shared secret ciphertext used to generate and verify time-based one-time passwords.                                                             |
| `name`              | `text`                     | no       |                              |          |           |           | User-chosen display name for this authenticator (1-100 chars, trimmed).                                                                                        |
| `verified_at`       | `timestamp with time zone` | yes      |                              |          |           |           | When the user first verified a code from this authenticator, confirming setup is complete. NULL means setup is incomplete and the authenticator is not active. |
| `created_at`        | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                                                                                                                                |
| `updated_at`        | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP`          |          |           |           |                                                                                                                                                                |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**
_none_

**Check constraints:**

- `user_totp_authenticators_name_check`: `CHECK (((char_length(name) >= 1) AND (char_length(name) <= 100) AND (TRIM(BOTH FROM name) = name)))`

**Foreign keys:**

- `user_totp_authenticators_user_id_fkey`: `FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE`

**Indexes:**

- `idx_user_totp_authenticators__user_id`: `CREATE INDEX idx_user_totp_authenticators__user_id ON public.user_totp_authenticators USING btree (user_id)`
- `user_totp_authenticators_pkey`: `CREATE UNIQUE INDEX user_totp_authenticators_pkey ON public.user_totp_authenticators USING btree (id)`

**Triggers:**

- `trigger_user_totp_authenticators_updated_at`: `CREATE TRIGGER trigger_user_totp_authenticators_updated_at BEFORE UPDATE ON public.user_totp_authenticators FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
