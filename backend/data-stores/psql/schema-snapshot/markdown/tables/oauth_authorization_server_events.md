# Table `oauth_authorization_server_events`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Append-only durable evidence of OAuth consent and token revocation lifecycle events; identity columns intentionally have no foreign keys so retention and identity deletion cannot erase the audit record.

Not partitioned — growth: unbounded.

| Column                     | Type                       | Nullable | Default                      | Identity | Generated | Collation | Comment                                                                                                                  |
| -------------------------- | -------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | ------------------------------------------------------------------------------------------------------------------------ |
| `id`                       | `uuid`                     | no       | `uuidv7()`                   |          |           |           |                                                                                                                          |
| `event_type`               | `text`                     | no       |                              |          |           |           | Immutable consent, explicit revocation, or refresh-reuse lifecycle outcome.                                              |
| `authorization_request_id` | `uuid`                     | yes      |                              |          |           |           | Short-lived authorization request that produced a consent event; intentionally not a foreign key.                        |
| `access_token_id`          | `uuid`                     | yes      |                              |          |           |           | Short-lived access token affected by a revocation event; intentionally not a foreign key.                                |
| `refresh_token_family_id`  | `uuid`                     | yes      |                              |          |           |           | Short-lived refresh family affected by revocation or reuse; intentionally not a foreign key.                             |
| `user_id`                  | `uuid`                     | no       |                              |          |           |           | Grant owner at event time; intentionally not a foreign key so hard deletion preserves the audit record.                  |
| `client_id`                | `uuid`                     | no       |                              |          |           |           | Client internal identifier at event time; intentionally not a foreign key so client deletion preserves the audit record. |
| `grant_id`                 | `uuid`                     | yes      |                              |          |           |           | Grant internal identifier at event time; intentionally not a foreign key so grant deletion preserves the audit record.   |
| `resource`                 | `text`                     | no       |                              |          |           |           | Protected resource audience affected by the event.                                                                       |
| `scopes`                   | `text[]`                   | no       |                              |          |           |           | Canonical scope set affected by the event.                                                                               |
| `occurred_at`              | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           | UUIDv7-derived time at which the event was recorded.                                                                     |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**
_none_

**Check constraints:**

- `oauth_authorization_server_events_check`: `CHECK ((num_nonnulls(authorization_request_id, access_token_id, refresh_token_family_id) = 1))`
- `oauth_authorization_server_events_check1`: `CHECK ((((event_type = ANY (ARRAY['consent_approved'::text, 'consent_denied'::text])) AND (authorization_request_id IS NOT NULL)) OR ((event_type = 'access_token_revoked'::text) AND (access_token_id IS NOT NULL)) OR ((event_type = ANY (ARRAY['refresh_family_revoked'::text, 'refresh_reuse_detected'::text])) AND (refresh_token_family_id IS NOT NULL))))`
- `oauth_authorization_server_events_check2`: `CHECK ((((event_type = 'consent_denied'::text) AND (grant_id IS NULL)) OR ((event_type <> 'consent_denied'::text) AND (grant_id IS NOT NULL))))`
- `oauth_authorization_server_events_event_type_check`: `CHECK ((event_type = ANY (ARRAY['consent_approved'::text, 'consent_denied'::text, 'access_token_revoked'::text, 'refresh_family_revoked'::text, 'refresh_reuse_detected'::text])))`
- `oauth_authorization_server_events_resource_check`: `CHECK (((char_length(resource) >= 1) AND (char_length(resource) <= 2048)))`
- `oauth_authorization_server_events_scopes_check`: `CHECK ((cardinality(scopes) > 0))`

**Foreign keys:**
_none_

**Indexes:**

- `idx_oauth_authorization_server_events__access_revocation`: `CREATE UNIQUE INDEX idx_oauth_authorization_server_events__access_revocation ON public.oauth_authorization_server_events USING btree (access_token_id) WHERE (access_token_id IS NOT NULL)`
- `idx_oauth_authorization_server_events__client`: `CREATE INDEX idx_oauth_authorization_server_events__client ON public.oauth_authorization_server_events USING btree (client_id, id)`
- `idx_oauth_authorization_server_events__consent`: `CREATE UNIQUE INDEX idx_oauth_authorization_server_events__consent ON public.oauth_authorization_server_events USING btree (authorization_request_id) WHERE (authorization_request_id IS NOT NULL)`
- `idx_oauth_authorization_server_events__family_lifecycle`: `CREATE UNIQUE INDEX idx_oauth_authorization_server_events__family_lifecycle ON public.oauth_authorization_server_events USING btree (refresh_token_family_id, event_type) WHERE (refresh_token_family_id IS NOT NULL)`
- `idx_oauth_authorization_server_events__user`: `CREATE INDEX idx_oauth_authorization_server_events__user ON public.oauth_authorization_server_events USING btree (user_id, id)`
- `oauth_authorization_server_events_pkey`: `CREATE UNIQUE INDEX oauth_authorization_server_events_pkey ON public.oauth_authorization_server_events USING btree (id)`

**Triggers:**

- `trigger_oauth_authorization_server_events_append_only`: `CREATE TRIGGER trigger_oauth_authorization_server_events_append_only BEFORE DELETE OR UPDATE ON public.oauth_authorization_server_events FOR EACH ROW EXECUTE FUNCTION fn_reject_oauth_authorization_server_event_mutation()`
