# Table `ap_actor_keys`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

RSA signing keypair for a local user's ActivityPub actor (/ap/users/:userId). Private key is encrypted at rest; only ever decrypted in-process to sign outbound HTTP Signatures.

Not partitioned — growth: unbounded.

| Column                   | Type                       | Nullable | Default             | Identity | Generated | Collation | Comment                                                                                                                                                                    |
| ------------------------ | -------------------------- | -------- | ------------------- | -------- | --------- | --------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `user_id`                | `uuid`                     | no       |                     |          |           |           | The local user this actor keypair belongs to. One keypair per federated user.                                                                                              |
| `key_id`                 | `text`                     | no       |                     |          |           |           | The public keyId URI advertised on the actor document and used in the HTTP Signature keyId parameter, e.g. https://host/ap/users/:userId#main-key.                         |
| `public_key_pem`         | `text`                     | no       |                     |          |           |           | PEM-encoded RSA public key, published on the actor document for remote signature verification.                                                                             |
| `private_key_ciphertext` | `text`                     | no       |                     |          |           |           | AES-256-GCM ciphertext (via @modules/token-secrets encryptSecret, purpose ap:actor-key:<userId>) of the PEM-encoded RSA private key. Never store or log the plaintext PEM. |
| `created_at`             | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                                                                                                                                            |
| `updated_at`             | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                                                                                                                                            |

**Primary key:** `PRIMARY KEY (user_id)`

**Unique constraints:**
_none_

**Check constraints:**
_none_

**Foreign keys:**

- `ap_actor_keys_user_id_fkey`: `FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE`

**Indexes:**

- `ap_actor_keys_pkey`: `CREATE UNIQUE INDEX ap_actor_keys_pkey ON public.ap_actor_keys USING btree (user_id)`
- `idx_ap_actor_keys__key_id`: `CREATE UNIQUE INDEX idx_ap_actor_keys__key_id ON public.ap_actor_keys USING btree (key_id)`

**Triggers:**

- `trigger_ap_actor_keys_updated_at`: `CREATE TRIGGER trigger_ap_actor_keys_updated_at BEFORE UPDATE ON public.ap_actor_keys FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
