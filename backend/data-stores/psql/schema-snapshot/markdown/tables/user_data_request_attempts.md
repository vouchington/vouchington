# Table `user_data_request_attempts`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Durable ledger of deterministic S3 object keys that an export worker may still upload.

Not partitioned — growth: unbounded.

| Column                    | Type                       | Nullable | Default             | Identity | Generated | Collation | Comment                                                                                                      |
| ------------------------- | -------------------------- | -------- | ------------------- | -------- | --------- | --------- | ------------------------------------------------------------------------------------------------------------ |
| `request_id`              | `uuid`                     | no       |                     |          |           |           | Owning account-data export request; part of the composite attempt identity.                                  |
| `processing_attempt_id`   | `uuid`                     | no       |                     |          |           |           | Claimed export attempt token retained across stale recovery so deletion can purge every possible object key. |
| `started_at`              | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           | Timestamp copied from the export claim that first made this deterministic object key possible.               |
| `updated_at`              | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                                                                              |
| `upload_lease_expires_at` | `timestamp with time zone` | yes      |                     |          |           |           | Bounded provider-effect lease; deletion waits through this deadline before acknowledging cleanup.            |

**Primary key:** `PRIMARY KEY (request_id, processing_attempt_id)`

**Unique constraints:**
_none_

**Check constraints:**
_none_

**Foreign keys:**

- `user_data_request_attempts_request_id_fkey`: `FOREIGN KEY (request_id) REFERENCES user_data_requests(id) ON DELETE CASCADE`

**Indexes:**

- `user_data_request_attempts_pkey`: `CREATE UNIQUE INDEX user_data_request_attempts_pkey ON public.user_data_request_attempts USING btree (request_id, processing_attempt_id)`

**Triggers:**

- `trigger_user_data_request_attempts_updated_at`: `CREATE TRIGGER trigger_user_data_request_attempts_updated_at BEFORE UPDATE ON public.user_data_request_attempts FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
