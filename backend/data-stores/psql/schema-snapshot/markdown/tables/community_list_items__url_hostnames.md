# Table `community_list_items__url_hostnames`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

URL hostnames (domains) curated into a community list by community leads.

Not partitioned — growth: unbounded.

| Column            | Type                       | Nullable | Default                      | Identity | Generated | Collation | Comment                                            |
| ----------------- | -------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | -------------------------------------------------- |
| `id`              | `uuid`                     | no       | `uuidv7()`                   |          |           |           |                                                    |
| `community_id`    | `uuid`                     | no       |                              |          |           |           | The community this list item belongs to.           |
| `url_hostname_id` | `uuid`                     | no       |                              |          |           |           | The URL hostname added to the list.                |
| `order_index`     | `integer`                  | no       | `0`                          |          |           |           | Manual sort order within the list (lower = first). |
| `added_by_id`     | `uuid`                     | yes      |                              |          |           |           | User who added this item to the list.              |
| `created_at`      | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                    |
| `removed_at`      | `timestamp with time zone` | yes      |                              |          |           |           | When set, the item has been removed from the list. |
| `removed_by_id`   | `uuid`                     | yes      |                              |          |           |           | User who removed this item from the list.          |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**
_none_

**Check constraints:**
_none_

**Foreign keys:**

- `community_list_items__url_hostnames_added_by_id_fkey`: `FOREIGN KEY (added_by_id) REFERENCES users(id) ON DELETE SET NULL`
- `community_list_items__url_hostnames_community_id_fkey`: `FOREIGN KEY (community_id) REFERENCES communities(id) ON DELETE CASCADE`
- `community_list_items__url_hostnames_removed_by_id_fkey`: `FOREIGN KEY (removed_by_id) REFERENCES users(id) ON DELETE SET NULL`
- `community_list_items__url_hostnames_url_hostname_id_fkey`: `FOREIGN KEY (url_hostname_id) REFERENCES url_hostnames(id) ON DELETE CASCADE`

**Indexes:**

- `community_list_items__url_hostnames_pkey`: `CREATE UNIQUE INDEX community_list_items__url_hostnames_pkey ON public.community_list_items__url_hostnames USING btree (id)`
- `idx_cli_url_hostnames__community`: `CREATE INDEX idx_cli_url_hostnames__community ON public.community_list_items__url_hostnames USING btree (community_id)`
- `idx_cli_url_hostnames__uniq`: `CREATE UNIQUE INDEX idx_cli_url_hostnames__uniq ON public.community_list_items__url_hostnames USING btree (community_id, url_hostname_id) WHERE (removed_at IS NULL)`
- `idx_cli_url_hostnames__url_hostname`: `CREATE INDEX idx_cli_url_hostnames__url_hostname ON public.community_list_items__url_hostnames USING btree (url_hostname_id)`

**Triggers:**
_none_
