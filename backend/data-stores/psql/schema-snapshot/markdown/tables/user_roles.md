# Table `user_roles`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Join table assigning role types to users.

Not partitioned — growth: unbounded.

| Column         | Type                       | Nullable | Default             | Identity | Generated | Collation | Comment                              |
| -------------- | -------------------------- | -------- | ------------------- | -------- | --------- | --------- | ------------------------------------ |
| `user_id`      | `uuid`                     | no       |                     |          |           |           | The user who has this role.          |
| `role_type_id` | `bigint`                   | no       |                     |          |           |           | The role type assigned to this user. |
| `created_at`   | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                      |
| `updated_at`   | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                      |

**Primary key:** `PRIMARY KEY (user_id, role_type_id)`

**Unique constraints:**
_none_

**Check constraints:**
_none_

**Foreign keys:**

- `user_roles_role_type_id_fkey`: `FOREIGN KEY (role_type_id) REFERENCES user_roles_types(id) ON DELETE CASCADE`
- `user_roles_user_id_fkey`: `FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE`

**Indexes:**

- `idx_user_roles__role_type_id`: `CREATE INDEX idx_user_roles__role_type_id ON public.user_roles USING btree (role_type_id)`
- `user_roles_pkey`: `CREATE UNIQUE INDEX user_roles_pkey ON public.user_roles USING btree (user_id, role_type_id)`

**Triggers:**

- `trigger_user_roles_updated_at`: `CREATE TRIGGER trigger_user_roles_updated_at BEFORE UPDATE ON public.user_roles FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
