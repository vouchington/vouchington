# Table `agent_responses`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Standalone agent task runs with SSE streaming support and quota enforcement.

RANGE partitioned on `id` (children: monthly, retention owner `cleanupPartitions`, access class: retention-window, growth: unbounded).

| Column               | Type                                | Nullable | Default                      | Identity | Generated | Collation | Comment                                                  |
| -------------------- | ----------------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | -------------------------------------------------------- |
| `id`                 | `uuid`                              | no       | `uuidv7()`                   |          |           |           | UUIDv7 primary key, used for time-range partitioning.    |
| `created_by_id`      | `uuid`                              | no       |                              |          |           |           | The user who requested the agent response.               |
| `agent`              | `agent_name`                        | no       |                              |          |           |           | Which agent handled this response (e.g. research).       |
| `model_name`         | `agent_models`                      | yes      |                              |          |           |           | LLM model used; NULL until the worker starts.            |
| `model_provider`     | `agent_model_providers`             | yes      |                              |          |           |           | LLM provider; NULL until the worker starts.              |
| `job_id`             | `text`                              | yes      |                              |          |           |           | glide-mq job ID; NULL until enqueued.                    |
| `input`              | `jsonb`                             | no       | `'{}'::jsonb`                |          |           |           | Structured input payload (task, context).                |
| `output`             | `jsonb`                             | yes      |                              |          |           |           | Final structured output (content); NULL until completed. |
| `error`              | `jsonb`                             | yes      |                              |          |           |           | Error details; NULL unless failed.                       |
| `termination_reason` | `agent_response_termination_reason` | yes      |                              |          |           |           | How the run ended.                                       |
| `started_at`         | `timestamp with time zone`          | yes      |                              |          |           |           | When the worker began executing; NULL until started.     |
| `completed_at`       | `timestamp with time zone`          | yes      |                              |          |           |           | When the run completed successfully.                     |
| `failed_at`          | `timestamp with time zone`          | yes      |                              |          |           |           | When the run failed.                                     |
| `deleted_at`         | `timestamp with time zone`          | yes      |                              |          |           |           | Soft-delete / cancellation timestamp.                    |
| `created_at`         | `timestamp with time zone`          | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                          |
| `updated_at`         | `timestamp with time zone`          | no       | `now()`                      |          |           |           | Automatically updated on each row change.                |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**
_none_

**Check constraints:**

- `chk_agent_responses__one_terminal_state`: `CHECK (((completed_at IS NULL) OR (failed_at IS NULL)))`

**Foreign keys:**

- `agent_responses_created_by_id_fkey`: `FOREIGN KEY (created_by_id) REFERENCES users(id) ON DELETE CASCADE`

**Indexes:**

- `agent_responses_created_by_id_id_idx`: `CREATE INDEX agent_responses_created_by_id_id_idx ON ONLY public.agent_responses USING btree (created_by_id, id DESC)`
- `agent_responses_pkey`: `CREATE UNIQUE INDEX agent_responses_pkey ON ONLY public.agent_responses USING btree (id)`
- `agent_responses_running_idx`: `CREATE INDEX agent_responses_running_idx ON ONLY public.agent_responses USING btree (created_by_id) WHERE ((deleted_at IS NULL) AND (completed_at IS NULL) AND (failed_at IS NULL))`

**Triggers:**

- `trigger_agent_responses_guard_terminal_lifecycle`: `CREATE TRIGGER trigger_agent_responses_guard_terminal_lifecycle BEFORE UPDATE ON public.agent_responses FOR EACH ROW EXECUTE FUNCTION fn_guard_terminal_lifecycle('completed_at', 'failed_at')`
- `trigger_agent_responses_updated_at`: `CREATE TRIGGER trigger_agent_responses_updated_at BEFORE UPDATE ON public.agent_responses FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
