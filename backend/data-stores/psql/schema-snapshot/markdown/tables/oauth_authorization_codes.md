# Table `oauth_authorization_codes`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Single-use S256 authorization codes stored only as purpose-bound hashes.

Not partitioned — growth: unbounded.

| Column           | Type                       | Nullable | Default                      | Identity | Generated | Collation | Comment                                                |
| ---------------- | -------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | ------------------------------------------------------ |
| `id`             | `uuid`                     | no       | `uuidv7()`                   |          |           |           |                                                        |
| `code_hash`      | `text`                     | no       |                              |          |           |           | Purpose-bound hash of the one-time authorization code. |
| `grant_id`       | `uuid`                     | no       |                              |          |           |           | Delegated grant represented by the code.               |
| `redirect_uri`   | `text`                     | no       |                              |          |           |           | Callback URI bound to the code exchange.               |
| `resource`       | `text`                     | no       |                              |          |           |           | Protected resource audience bound to the code.         |
| `scopes`         | `text[]`                   | no       |                              |          |           |           | Canonical scope set bound to the code.                 |
| `code_challenge` | `text`                     | no       |                              |          |           |           | Base64url SHA-256 PKCE challenge bound to the code.    |
| `expires_at`     | `timestamp with time zone` | no       |                              |          |           |           | Time after which the code cannot be exchanged.         |
| `consumed_at`    | `timestamp with time zone` | yes      |                              |          |           |           | Time at which the code was exchanged successfully.     |
| `created_at`     | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                        |
| `updated_at`     | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP`          |          |           |           |                                                        |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**

- `oauth_authorization_codes_code_hash_key`: `UNIQUE (code_hash)`

**Check constraints:**

- `oauth_authorization_codes_code_challenge_check`: `CHECK ((code_challenge ~ '^[A-Za-z0-9_-]{43}$'::text))`
- `oauth_authorization_codes_code_hash_check`: `CHECK ((char_length(code_hash) = 64))`
- `oauth_authorization_codes_redirect_uri_check`: `CHECK (((char_length(redirect_uri) >= 1) AND (char_length(redirect_uri) <= 2048)))`
- `oauth_authorization_codes_resource_check`: `CHECK (((char_length(resource) >= 1) AND (char_length(resource) <= 2048)))`
- `oauth_authorization_codes_scopes_check`: `CHECK ((cardinality(scopes) > 0))`

**Foreign keys:**

- `oauth_authorization_codes_grant_id_fkey`: `FOREIGN KEY (grant_id) REFERENCES oauth_grants(id) ON DELETE CASCADE`

**Indexes:**

- `idx_oauth_authorization_codes__expiry`: `CREATE INDEX idx_oauth_authorization_codes__expiry ON public.oauth_authorization_codes USING btree (expires_at, id)`
- `idx_oauth_authorization_codes__grant`: `CREATE INDEX idx_oauth_authorization_codes__grant ON public.oauth_authorization_codes USING btree (grant_id, id)`
- `oauth_authorization_codes_code_hash_key`: `CREATE UNIQUE INDEX oauth_authorization_codes_code_hash_key ON public.oauth_authorization_codes USING btree (code_hash)`
- `oauth_authorization_codes_pkey`: `CREATE UNIQUE INDEX oauth_authorization_codes_pkey ON public.oauth_authorization_codes USING btree (id)`

**Triggers:**

- `trigger_oauth_authorization_codes_updated_at`: `CREATE TRIGGER trigger_oauth_authorization_codes_updated_at BEFORE UPDATE ON public.oauth_authorization_codes FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
