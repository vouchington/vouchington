# Table `post_publication_rss_feed_identities`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Repair-only identity bridge; unreferenced rows are reclaimed in bounded pages.

Not partitioned — growth: bounded.

| Column        | Type                       | Nullable | Default                      | Identity | Generated | Collation | Comment                                                                                |
| ------------- | -------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | -------------------------------------------------------------------------------------- |
| `id`          | `uuid`                     | no       |                              |          |           |           |                                                                                        |
| `rss_feed_id` | `uuid`                     | yes      |                              |          |           |           | Live entity FK; deletion clears this link without deleting historical repair identity. |
| `created_at`  | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                                                        |
| `updated_at`  | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP`          |          |           |           |                                                                                        |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**

- `post_publication_rss_feed_identities_rss_feed_id_key`: `UNIQUE (rss_feed_id)`

**Check constraints:**

- `post_publication_rss_feed_identities_check`: `CHECK (((rss_feed_id IS NULL) OR (rss_feed_id = id)))`

**Foreign keys:**

- `post_publication_rss_feed_identities_rss_feed_id_fkey`: `FOREIGN KEY (rss_feed_id) REFERENCES rss_feeds(id) ON DELETE SET NULL`

**Indexes:**

- `post_publication_rss_feed_identities_pkey`: `CREATE UNIQUE INDEX post_publication_rss_feed_identities_pkey ON public.post_publication_rss_feed_identities USING btree (id)`
- `post_publication_rss_feed_identities_rss_feed_id_key`: `CREATE UNIQUE INDEX post_publication_rss_feed_identities_rss_feed_id_key ON public.post_publication_rss_feed_identities USING btree (rss_feed_id)`

**Triggers:**

- `trigger_pub_rss_feed_identities_updated_at`: `CREATE TRIGGER trigger_pub_rss_feed_identities_updated_at BEFORE UPDATE ON public.post_publication_rss_feed_identities FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
