# Table `user_consents`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Records user consent to legal agreements such as privacy policy and terms of service.

Not partitioned — growth: unbounded.

| Column         | Type                       | Nullable | Default                      | Identity | Generated | Collation | Comment                                                                 |
| -------------- | -------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | ----------------------------------------------------------------------- |
| `id`           | `uuid`                     | no       | `uuidv7()`                   |          |           |           |                                                                         |
| `user_id`      | `uuid`                     | no       |                              |          |           |           | The user who gave consent.                                              |
| `consent_type` | `consent_types`            | no       |                              |          |           |           | Type of consent: privacy_policy, terms_of_service, or cookie_analytics. |
| `version`      | `text`                     | no       |                              |          |           |           | Version identifier of the agreement the user consented to.              |
| `created_at`   | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                                         |
| `updated_at`   | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP`          |          |           |           |                                                                         |
| `revoked_at`   | `timestamp with time zone` | yes      |                              |          |           |           | When the user revoked this consent.                                     |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**
_none_

**Check constraints:**

- `user_consents_version_check`: `CHECK (((char_length(version) >= 1) AND (char_length(version) <= 50)))`
- `user_consents_version_check1`: `CHECK ((version = TRIM(BOTH FROM version)))`

**Foreign keys:**

- `user_consents_user_id_fkey`: `FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE`

**Indexes:**

- `idx_user_consents__user_id__fk`: `CREATE INDEX idx_user_consents__user_id__fk ON public.user_consents USING btree (user_id) WHERE (user_id IS NOT NULL)`
- `idx_user_consents__user_id__unique`: `CREATE UNIQUE INDEX idx_user_consents__user_id__unique ON public.user_consents USING btree (user_id, consent_type) WHERE (revoked_at IS NULL)`
- `user_consents_pkey`: `CREATE UNIQUE INDEX user_consents_pkey ON public.user_consents USING btree (id)`

**Triggers:**

- `trigger_user_consents_updated_at`: `CREATE TRIGGER trigger_user_consents_updated_at BEFORE UPDATE ON public.user_consents FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
