# Table `memberships`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Current and retained historical user-facing access projection rows derived from a single membership source.

Not partitioned — growth: unbounded.

| Column                                                | Type                       | Nullable | Default                      | Identity | Generated | Collation | Comment                                                                                                       |
| ----------------------------------------------------- | -------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | ------------------------------------------------------------------------------------------------------------- |
| `id`                                                  | `uuid`                     | no       | `uuidv7()`                   |          |           |           |                                                                                                               |
| `user_id`                                             | `uuid`                     | no       |                              |          |           |           | User currently receiving projected membership access.                                                         |
| `membership_source_id`                                | `uuid`                     | no       |                              |          |           |           | Source that currently owns this access projection.                                                            |
| `membership_product_id`                               | `uuid`                     | no       |                              |          |           |           | Canonical product currently projected as access.                                                              |
| `effective_at`                                        | `timestamp with time zone` | no       |                              |          |           |           | When the projected membership became effective.                                                               |
| `expires_at`                                          | `timestamp with time zone` | yes      |                              |          |           |           | When the projected membership expires, if finite.                                                             |
| `cancelled_at`                                        | `timestamp with time zone` | yes      |                              |          |           |           | When the projection entered a cancelled state.                                                                |
| `expired_at`                                          | `timestamp with time zone` | yes      |                              |          |           |           | When the projection entered an expired state.                                                                 |
| `past_due_at`                                         | `timestamp with time zone` | yes      |                              |          |           |           | When the projection entered a past-due state.                                                                 |
| `paused_at`                                           | `timestamp with time zone` | yes      |                              |          |           |           | When the projection entered a paused state.                                                                   |
| `cancel_at_period_end`                                | `boolean`                  | no       | `false`                      |          |           |           | Whether cancellation takes effect after the current period.                                                   |
| `latest_change_id`                                    | `uuid`                     | yes      |                              |          |           |           | Latest append-only audit row for this projection.                                                             |
| `renewal_price_increase_notified_observation_id`      | `uuid`                     | yes      |                              |          |           |           | Provider observation claimed for the most recent renewal price notification.                                  |
| `renewal_price_increase_notified_provider_product_id` | `uuid`                     | yes      |                              |          |           |           | Provider product for the most recent renewal price notification.                                              |
| `renewal_price_increase_notified_minor_units`         | `bigint`                   | yes      |                              |          |           |           | Notified future renewal price in the currency minor unit.                                                     |
| `renewal_price_increase_notified_currency_code`       | `text`                     | yes      |                              |          |           |           | Currency of the notified future renewal price.                                                                |
| `renewal_price_increase_notified_effective_at`        | `timestamp with time zone` | yes      |                              |          |           |           | Effective time of the provider renewal price most recently notified.                                          |
| `renewal_price_increase_notified_at`                  | `timestamp with time zone` | yes      |                              |          |           |           | When the price notification was delivered.                                                                    |
| `renewal_price_increase_claim_token`                  | `uuid`                     | yes      |                              |          |           |           | Fence identifying the worker that owns the current delivery claim.                                            |
| `renewal_price_increase_claimed_at`                   | `timestamp with time zone` | yes      |                              |          |           |           | When delivery of the price notification was claimed.                                                          |
| `renewal_price_increase_delivery_attempted_at`        | `timestamp with time zone` | yes      |                              |          |           |           | When delivery of the price notification was attempted.                                                        |
| `created_at`                                          | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                                                                               |
| `updated_at`                                          | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP`          |          |           |           |                                                                                                               |
| `projection_ended_at`                                 | `timestamp with time zone` | yes      |                              |          |           |           | When this projection stopped being the user's live membership projection; its historical ID remains retained. |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**
_none_

**Check constraints:**

- `memberships_check`: `CHECK ((num_nonnulls(cancelled_at, expired_at, past_due_at, paused_at) <= 1))`
- `memberships_check1`: `CHECK ((num_nonnulls(renewal_price_increase_notified_observation_id, renewal_price_increase_notified_provider_product_id, renewal_price_increase_notified_minor_units, renewal_price_increase_notified_currency_code, renewal_price_increase_notified_effective_at) = ANY (ARRAY[0, 5])))`
- `memberships_check2`: `CHECK (((renewal_price_increase_notified_at IS NULL) OR (renewal_price_increase_delivery_attempted_at IS NOT NULL)))`
- `memberships_check3`: `CHECK (((renewal_price_increase_claim_token IS NULL) = (renewal_price_increase_claimed_at IS NULL)))`
- `memberships_check4`: `CHECK (((renewal_price_increase_claimed_at IS NULL) OR (renewal_price_increase_notified_observation_id IS NOT NULL)))`
- `memberships_check5`: `CHECK (((renewal_price_increase_delivery_attempted_at IS NULL) OR (renewal_price_increase_notified_observation_id IS NOT NULL)))`
- `memberships_renewal_price_increase_notified_minor_units_check`: `CHECK (((renewal_price_increase_notified_minor_units IS NULL) OR ((renewal_price_increase_notified_minor_units >= 0) AND (renewal_price_increase_notified_minor_units <= '9007199254740991'::bigint))))`

**Foreign keys:**

- `fk_memberships__renewal_observation_snapshot`: `FOREIGN KEY (renewal_price_increase_notified_observation_id, renewal_price_increase_notified_provider_product_id, renewal_price_increase_notified_minor_units, renewal_price_increase_notified_currency_code, renewal_price_increase_notified_effective_at) REFERENCES membership_provider_observations(id, renewal_membership_provider_product_id, renewal_price_minor_units, renewal_price_currency_code, renewal_effective_at) ON DELETE RESTRICT`
- `fk_memberships__source_user`: `FOREIGN KEY (membership_source_id, user_id) REFERENCES membership_sources(id, user_id) ON DELETE RESTRICT`
- `fk_memberships_latest_change_id`: `FOREIGN KEY (latest_change_id) REFERENCES membership_changes(id) ON DELETE SET NULL`
- `memberships_membership_product_id_fkey`: `FOREIGN KEY (membership_product_id) REFERENCES membership_products(id) ON DELETE RESTRICT`
- `memberships_membership_source_id_fkey`: `FOREIGN KEY (membership_source_id) REFERENCES membership_sources(id) ON DELETE RESTRICT`
- `memberships_renewal_price_increase_notified_currency_code_fkey`: `FOREIGN KEY (renewal_price_increase_notified_currency_code) REFERENCES currencies(code) ON DELETE RESTRICT`
- `memberships_renewal_price_increase_notified_observation_id_fkey`: `FOREIGN KEY (renewal_price_increase_notified_observation_id) REFERENCES membership_provider_observations(id) ON DELETE RESTRICT`
- `memberships_user_id_fkey`: `FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE`

**Indexes:**

- `idx_memberships__effective_user`: `CREATE UNIQUE INDEX idx_memberships__effective_user ON public.memberships USING btree (user_id) WHERE (projection_ended_at IS NULL)`
- `idx_memberships__expires_at`: `CREATE INDEX idx_memberships__expires_at ON public.memberships USING btree (expires_at, id) WHERE ((projection_ended_at IS NULL) AND (expires_at IS NOT NULL))`
- `idx_memberships__latest_change_id`: `CREATE INDEX idx_memberships__latest_change_id ON public.memberships USING btree (latest_change_id) WHERE (latest_change_id IS NOT NULL)`
- `idx_memberships__product_id`: `CREATE INDEX idx_memberships__product_id ON public.memberships USING btree (membership_product_id)`
- `idx_memberships__renewal_claim_token`: `CREATE UNIQUE INDEX idx_memberships__renewal_claim_token ON public.memberships USING btree (renewal_price_increase_claim_token) WHERE (renewal_price_increase_claim_token IS NOT NULL)`
- `idx_memberships__renewal_currency_code`: `CREATE INDEX idx_memberships__renewal_currency_code ON public.memberships USING btree (renewal_price_increase_notified_currency_code) WHERE (renewal_price_increase_notified_currency_code IS NOT NULL)`
- `idx_memberships__renewal_observation_id`: `CREATE INDEX idx_memberships__renewal_observation_id ON public.memberships USING btree (renewal_price_increase_notified_observation_id) WHERE (renewal_price_increase_notified_observation_id IS NOT NULL)`
- `idx_memberships__renewal_provider_product_id`: `CREATE INDEX idx_memberships__renewal_provider_product_id ON public.memberships USING btree (renewal_price_increase_notified_provider_product_id) WHERE (renewal_price_increase_notified_provider_product_id IS NOT NULL)`
- `idx_memberships__source_id`: `CREATE INDEX idx_memberships__source_id ON public.memberships USING btree (membership_source_id)`
- `idx_memberships__user_id`: `CREATE INDEX idx_memberships__user_id ON public.memberships USING btree (user_id) WHERE (user_id IS NOT NULL)`
- `memberships_pkey`: `CREATE UNIQUE INDEX memberships_pkey ON public.memberships USING btree (id)`

**Triggers:**

- `trigger_memberships_updated_at`: `CREATE TRIGGER trigger_memberships_updated_at BEFORE UPDATE ON public.memberships FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
