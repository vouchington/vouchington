# Table `post_data_point_topics`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Subject topics for data point posts. A data point can be about multiple topics (e.g., approved for a specific credit card).

RANGE partitioned on `post_id` (children: default, no retention owner, access class: target-scoped, growth: unbounded).

| Column        | Type                       | Nullable | Default             | Identity | Generated | Collation | Comment                                                      |
| ------------- | -------------------------- | -------- | ------------------- | -------- | --------- | --------- | ------------------------------------------------------------ |
| `post_id`     | `uuid`                     | no       |                     |          |           |           | The data point post this topic belongs to.                   |
| `topic_id`    | `uuid`                     | no       |                     |          |           |           | The topic this data point is about.                          |
| `order_index` | `integer`                  | no       | `0`                 |          |           |           | Display order of this topic within the data point (0-based). |
| `created_at`  | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                              |
| `updated_at`  | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                              |

**Primary key:** `PRIMARY KEY (post_id, topic_id)`

**Unique constraints:**
_none_

**Check constraints:**

- `post_data_point_topics_order_index_check`: `CHECK ((order_index >= 0))`

**Foreign keys:**

- `post_data_point_topics_post_id_fkey`: `FOREIGN KEY (post_id) REFERENCES posts(id) ON DELETE CASCADE`
- `post_data_point_topics_topic_id_fkey`: `FOREIGN KEY (topic_id) REFERENCES topics(id) ON DELETE CASCADE`

**Indexes:**

- `idx_post_data_point_topics__topic_id`: `CREATE INDEX idx_post_data_point_topics__topic_id ON ONLY public.post_data_point_topics USING btree (topic_id) INCLUDE (post_id)`
- `post_data_point_topics_pkey`: `CREATE UNIQUE INDEX post_data_point_topics_pkey ON ONLY public.post_data_point_topics USING btree (post_id, topic_id)`

**Triggers:**

- `trigger_post_data_point_topics_updated_at`: `CREATE TRIGGER trigger_post_data_point_topics_updated_at BEFORE UPDATE ON public.post_data_point_topics FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
