# Table `community_auto_tagger_agents`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Per-community enablement for global seeded AI label/tagging agents.

Not partitioned — growth: unbounded.

| Column           | Type                       | Nullable | Default             | Identity | Generated | Collation | Comment                                                                                                            |
| ---------------- | -------------------------- | -------- | ------------------- | -------- | --------- | --------- | ------------------------------------------------------------------------------------------------------------------ |
| `community_id`   | `uuid`                     | no       |                     |          |           |           | Community whose posts may run this global agent.                                                                   |
| `agent_id`       | `uuid`                     | no       |                     |          |           |           | Global agents row enabled for the community.                                                                       |
| `enabled_at`     | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           | When this global agent was enabled for the community.                                                              |
| `enabled_by_id`  | `uuid`                     | yes      |                     |          |           |           | User who enabled this global agent for the community. NULL when the first recorded action was disabling the agent. |
| `disabled_at`    | `timestamp with time zone` | yes      |                     |          |           |           | When this global agent was disabled for the community. NULL means enabled.                                         |
| `disabled_by_id` | `uuid`                     | yes      |                     |          |           |           | User who disabled this global agent for the community.                                                             |
| `updated_at`     | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           | Last modification timestamp, maintained by trigger.                                                                |

**Primary key:** `PRIMARY KEY (community_id, agent_id)`

**Unique constraints:**
_none_

**Check constraints:**

- `community_auto_tagger_agents_check`: `CHECK (((disabled_at IS NULL) OR (disabled_at >= enabled_at)))`

**Foreign keys:**

- `community_auto_tagger_agents_agent_id_fkey`: `FOREIGN KEY (agent_id) REFERENCES agents(id) ON DELETE CASCADE`
- `community_auto_tagger_agents_community_id_fkey`: `FOREIGN KEY (community_id) REFERENCES communities(id) ON DELETE CASCADE`
- `community_auto_tagger_agents_disabled_by_id_fkey`: `FOREIGN KEY (disabled_by_id) REFERENCES users(id) ON DELETE SET NULL`
- `community_auto_tagger_agents_enabled_by_id_fkey`: `FOREIGN KEY (enabled_by_id) REFERENCES users(id) ON DELETE SET NULL`

**Indexes:**

- `community_auto_tagger_agents_pkey`: `CREATE UNIQUE INDEX community_auto_tagger_agents_pkey ON public.community_auto_tagger_agents USING btree (community_id, agent_id)`
- `idx_community_auto_tagger_agents__agent_id`: `CREATE INDEX idx_community_auto_tagger_agents__agent_id ON public.community_auto_tagger_agents USING btree (agent_id)`
- `idx_community_auto_tagger_agents__enabled`: `CREATE INDEX idx_community_auto_tagger_agents__enabled ON public.community_auto_tagger_agents USING btree (community_id) WHERE (disabled_at IS NULL)`

**Triggers:**

- `trigger_community_auto_tagger_agents_updated_at`: `CREATE TRIGGER trigger_community_auto_tagger_agents_updated_at BEFORE UPDATE ON public.community_auto_tagger_agents FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
