# Table `bedrock_embeddings_batches`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Tracks Amazon Bedrock batch embedding jobs and their lifecycle.

Not partitioned — growth: unbounded.

| Column           | Type                                | Nullable | Default             | Identity | Generated | Collation | Comment                                                                                            |
| ---------------- | ----------------------------------- | -------- | ------------------- | -------- | --------- | --------- | -------------------------------------------------------------------------------------------------- |
| `id`             | `text`                              | no       |                     |          |           |           |                                                                                                    |
| `job_arn`        | `text`                              | yes      |                     |          |           |           | The Bedrock model invocation job ARN.                                                              |
| `model_id`       | `text`                              | no       |                     |          |           |           | The Amazon Bedrock model id used for embeddings.                                                   |
| `job_type`       | `bedrock_embedding_batch_job_types` | no       |                     |          |           |           | Which entity type this batch processes.                                                            |
| `data`           | `jsonb`                             | no       | `'{}'::jsonb`       |          |           |           | JSONB metadata about the Bedrock batch job.                                                        |
| `records`        | `integer`                           | no       | `0`                 |          |           |           | Number of records in this batch.                                                                   |
| `created_at`     | `timestamp with time zone`          | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                                                                    |
| `updated_at`     | `timestamp with time zone`          | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                                                                    |
| `submitted_at`   | `timestamp with time zone`          | yes      |                     |          |           |           | When the Bedrock job was successfully submitted (Submitted/Validating/Scheduled).                  |
| `in_progress_at` | `timestamp with time zone`          | yes      |                     |          |           |           | When the Bedrock job first reported InProgress or Stopping.                                        |
| `completed_at`   | `timestamp with time zone`          | yes      |                     |          |           |           | When the Bedrock job first reached a successful terminal status (Completed or PartiallyCompleted). |
| `failed_at`      | `timestamp with time zone`          | yes      |                     |          |           |           | When the Bedrock job terminated unsuccessfully (Failed or Expired).                                |
| `cancelled_at`   | `timestamp with time zone`          | yes      |                     |          |           |           | When the Bedrock job was Stopped before completion.                                                |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**
_none_

**Check constraints:**

- `chk_bedrock_embeddings_batches__lifecycle`: `CHECK ((((submitted_at IS NOT NULL) OR (num_nonnulls(in_progress_at, completed_at, failed_at, cancelled_at) = 0)) AND (num_nonnulls(completed_at, failed_at, cancelled_at) <= 1)))`

**Foreign keys:**
_none_

**Indexes:**

- `bedrock_embeddings_batches_pkey`: `CREATE UNIQUE INDEX bedrock_embeddings_batches_pkey ON public.bedrock_embeddings_batches USING btree (id)`
- `idx_bedrock_embeddings_batches__active`: `CREATE INDEX idx_bedrock_embeddings_batches__active ON public.bedrock_embeddings_batches USING btree (created_at) WHERE ((submitted_at IS NOT NULL) AND (completed_at IS NULL) AND (failed_at IS NULL) AND (cancelled_at IS NULL))`
- `idx_bedrock_embeddings_batches__created_at`: `CREATE INDEX idx_bedrock_embeddings_batches__created_at ON public.bedrock_embeddings_batches USING btree (created_at)`
- `idx_bedrock_embeddings_batches__job_arn`: `CREATE UNIQUE INDEX idx_bedrock_embeddings_batches__job_arn ON public.bedrock_embeddings_batches USING btree (job_arn) WHERE (job_arn IS NOT NULL)`

**Triggers:**

- `trigger_bedrock_embeddings_batches_guard_terminal_lifecycle`: `CREATE TRIGGER trigger_bedrock_embeddings_batches_guard_terminal_lifecycle BEFORE UPDATE ON public.bedrock_embeddings_batches FOR EACH ROW EXECUTE FUNCTION fn_guard_terminal_lifecycle('completed_at', 'failed_at', 'cancelled_at')`
- `trigger_bedrock_embeddings_batches_updated_at`: `CREATE TRIGGER trigger_bedrock_embeddings_batches_updated_at BEFORE UPDATE ON public.bedrock_embeddings_batches FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
