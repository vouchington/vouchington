# Table `queue_reconciliation_checkpoints`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Durable high-water marks advanced only after a reconciliation dispatcher processes every candidate.

Not partitioned — growth: bounded.

| Column              | Type                       | Nullable | Default             | Identity | Generated | Collation | Comment                                                                           |
| ------------------- | -------------------------- | -------- | ------------------- | -------- | --------- | --------- | --------------------------------------------------------------------------------- |
| `queue_name`        | `text`                     | no       |                     |          |           |           | Stable queue-domain identifier owning this high-water mark.                       |
| `completed_through` | `timestamp with time zone` | no       |                     |          |           |           | Latest source timestamp whose complete candidate set was successfully reconciled. |
| `updated_at`        | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                                                   |

**Primary key:** `PRIMARY KEY (queue_name)`

**Unique constraints:**
_none_

**Check constraints:**

- `queue_reconciliation_checkpoints_queue_name_check`: `CHECK (((queue_name = TRIM(BOTH FROM queue_name)) AND ((char_length(queue_name) >= 1) AND (char_length(queue_name) <= 100))))`

**Foreign keys:**
_none_

**Indexes:**

- `queue_reconciliation_checkpoints_pkey`: `CREATE UNIQUE INDEX queue_reconciliation_checkpoints_pkey ON public.queue_reconciliation_checkpoints USING btree (queue_name)`

**Triggers:**

- `trigger_queue_reconciliation_checkpoints_updated_at`: `CREATE TRIGGER trigger_queue_reconciliation_checkpoints_updated_at BEFORE UPDATE ON public.queue_reconciliation_checkpoints FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
