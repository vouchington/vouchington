# Table `boilerplate_removals`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Stores computed boilerplate removal results for a hostname and parent path combination.

Not partitioned — growth: unbounded.

| Column        | Type                       | Nullable | Default                      | Identity | Generated | Collation | Comment                                                                |
| ------------- | -------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | ---------------------------------------------------------------------- |
| `id`          | `uuid`                     | no       | `uuidv7()`                   |          |           |           |                                                                        |
| `hostname_id` | `uuid`                     | no       |                              |          |           |           | The hostname these removal rules apply to.                             |
| `parent_path` | `text`                     | no       |                              |          |           |           | The URL parent path pattern for scoping removal rules.                 |
| `results`     | `jsonb`                    | no       | `'{}'::jsonb`                |          |           |           | JSONB containing the computed boilerplate removal selectors and rules. |
| `created_at`  | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                                        |
| `updated_at`  | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP`          |          |           |           |                                                                        |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**
_none_

**Check constraints:**

- `boilerplate_removals_parent_path_check`: `CHECK (((length(TRIM(BOTH FROM parent_path)) > 0) AND (length(parent_path) <= 255)))`

**Foreign keys:**

- `boilerplate_removals_hostname_id_fkey`: `FOREIGN KEY (hostname_id) REFERENCES url_hostnames(id) ON DELETE CASCADE`

**Indexes:**

- `boilerplate_removals_pkey`: `CREATE UNIQUE INDEX boilerplate_removals_pkey ON public.boilerplate_removals USING btree (id)`
- `idx_boilerplate_removals__hostname_path`: `CREATE INDEX idx_boilerplate_removals__hostname_path ON public.boilerplate_removals USING btree (hostname_id, parent_path)`

**Triggers:**

- `trigger_boilerplate_removals_updated_at`: `CREATE TRIGGER trigger_boilerplate_removals_updated_at BEFORE UPDATE ON public.boilerplate_removals FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
