# Table `crm_contact_social_accounts`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Social media handles for CRM contacts, one row per platform.

Not partitioned — growth: unbounded.

| Column                      | Type                       | Nullable | Default                      | Identity | Generated | Collation | Comment                                                |
| --------------------------- | -------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | ------------------------------------------------------ |
| `id`                        | `uuid`                     | no       | `uuidv7()`                   |          |           |           |                                                        |
| `contact_id`                | `uuid`                     | no       |                              |          |           |           | The contact this social account belongs to.            |
| `platform`                  | `crm_social_platforms`     | no       |                              |          |           |           | Social media platform.                                 |
| `handle`                    | `text`                     | no       |                              |          |           |           | Username or handle on the platform (without @ prefix). |
| `profile_url`               | `text`                     | yes      |                              |          |           |           | Full URL to the profile page.                          |
| `follower_count`            | `integer`                  | yes      |                              |          |           |           | Follower count on this platform at last check.         |
| `follower_count_updated_at` | `timestamp with time zone` | yes      |                              |          |           |           | When follower_count was last updated.                  |
| `created_at`                | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           | Timestamp derived from the UUIDv7 id.                  |
| `updated_at`                | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP`          |          |           |           | Last modification timestamp, maintained by trigger.    |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**

- `crm_contact_social_accounts_contact_id_platform_key`: `UNIQUE (contact_id, platform)`

**Check constraints:**

- `crm_contact_social_accounts_follower_count_check`: `CHECK ((follower_count >= 0))`
- `crm_contact_social_accounts_handle_check`: `CHECK ((handle = TRIM(BOTH FROM handle)))`
- `crm_contact_social_accounts_handle_check1`: `CHECK (((char_length(handle) >= 1) AND (char_length(handle) <= 200)))`

**Foreign keys:**

- `crm_contact_social_accounts_contact_id_fkey`: `FOREIGN KEY (contact_id) REFERENCES crm_contacts(id) ON DELETE CASCADE`

**Indexes:**

- `crm_contact_social_accounts_contact_id_platform_key`: `CREATE UNIQUE INDEX crm_contact_social_accounts_contact_id_platform_key ON public.crm_contact_social_accounts USING btree (contact_id, platform)`
- `crm_contact_social_accounts_pkey`: `CREATE UNIQUE INDEX crm_contact_social_accounts_pkey ON public.crm_contact_social_accounts USING btree (id)`

**Triggers:**

- `trigger_crm_contact_social_accounts_updated_at`: `CREATE TRIGGER trigger_crm_contact_social_accounts_updated_at BEFORE UPDATE ON public.crm_contact_social_accounts FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
