# Table `rss_feed_enablement_changes`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Append-only audit log of enablement state for RSS feeds. Current state is the latest row per rss_feed_id.

Not partitioned — growth: unbounded.

| Column          | Type                       | Nullable | Default                      | Identity | Generated | Collation | Comment                                                              |
| --------------- | -------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | -------------------------------------------------------------------- |
| `id`            | `uuid`                     | no       | `uuidv7()`                   |          |           |           |                                                                      |
| `rss_feed_id`   | `uuid`                     | no       |                              |          |           |           | RSS feed whose fetch enablement state changed.                       |
| `enabled`       | `boolean`                  | no       |                              |          |           |           | Whether the feed is enabled for fetching after this change.          |
| `created_by_id` | `uuid`                     | yes      |                              |          |           |           |                                                                      |
| `reason`        | `text`                     | yes      |                              |          |           |           | Optional human- or system-readable reason for the enablement change. |
| `created_at`    | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                                      |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**
_none_

**Check constraints:**

- `rss_feed_enablement_changes_reason_check`: `CHECK (((reason IS NULL) OR (char_length(reason) <= 1000)))`

**Foreign keys:**

- `rss_feed_enablement_changes_created_by_id_fkey`: `FOREIGN KEY (created_by_id) REFERENCES users(id) ON DELETE SET NULL`
- `rss_feed_enablement_changes_rss_feed_id_fkey`: `FOREIGN KEY (rss_feed_id) REFERENCES rss_feeds(id) ON DELETE CASCADE`

**Indexes:**

- `idx_rss_feed_enablement_changes__rss_feed_id__id`: `CREATE INDEX idx_rss_feed_enablement_changes__rss_feed_id__id ON public.rss_feed_enablement_changes USING btree (rss_feed_id, id DESC)`
- `rss_feed_enablement_changes_pkey`: `CREATE UNIQUE INDEX rss_feed_enablement_changes_pkey ON public.rss_feed_enablement_changes USING btree (id)`

**Triggers:**

- `trigger_sync_rss_feed_is_enabled`: `CREATE TRIGGER trigger_sync_rss_feed_is_enabled AFTER INSERT ON public.rss_feed_enablement_changes FOR EACH ROW EXECUTE FUNCTION fn_sync_rss_feed_is_enabled()`
