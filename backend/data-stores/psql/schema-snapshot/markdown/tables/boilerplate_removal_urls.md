# Table `boilerplate_removal_urls`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Join table linking boilerplate removal rules to the specific URLs they were computed from.

Not partitioned — growth: unbounded.

| Column                   | Type   | Nullable | Default | Identity | Generated | Collation | Comment                                         |
| ------------------------ | ------ | -------- | ------- | -------- | --------- | --------- | ----------------------------------------------- |
| `boilerplate_removal_id` | `uuid` | no       |         |          |           |           | The boilerplate removal rule set.               |
| `url_id`                 | `uuid` | no       |         |          |           |           | A URL used to compute this boilerplate removal. |

**Primary key:** `PRIMARY KEY (boilerplate_removal_id, url_id)`

**Unique constraints:**
_none_

**Check constraints:**
_none_

**Foreign keys:**

- `boilerplate_removal_urls_boilerplate_removal_id_fkey`: `FOREIGN KEY (boilerplate_removal_id) REFERENCES boilerplate_removals(id) ON DELETE CASCADE`
- `boilerplate_removal_urls_url_id_fkey`: `FOREIGN KEY (url_id) REFERENCES urls(id) ON DELETE CASCADE`

**Indexes:**

- `boilerplate_removal_urls_pkey`: `CREATE UNIQUE INDEX boilerplate_removal_urls_pkey ON public.boilerplate_removal_urls USING btree (boilerplate_removal_id, url_id)`
- `idx_boilerplate_removal_urls__url_id`: `CREATE INDEX idx_boilerplate_removal_urls__url_id ON public.boilerplate_removal_urls USING btree (url_id)`

**Triggers:**
_none_
