# Table `membership_grant_activation_periods`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Activation history for administrator grants; one open period per user and grant.

Not partitioned — growth: unbounded.

| Column                | Type                       | Nullable | Default                      | Identity | Generated | Collation | Comment                                               |
| --------------------- | -------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | ----------------------------------------------------- |
| `id`                  | `uuid`                     | no       | `uuidv7()`                   |          |           |           |                                                       |
| `membership_grant_id` | `uuid`                     | no       |                              |          |           |           | Grant activated for this period.                      |
| `user_id`             | `uuid`                     | no       |                              |          |           |           | Recipient user for the activation period.             |
| `started_at`          | `timestamp with time zone` | no       |                              |          |           |           | When the grant began providing access.                |
| `ended_at`            | `timestamp with time zone` | yes      |                              |          |           |           | When this activation period stopped providing access. |
| `created_at`          | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                       |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**
_none_

**Check constraints:**

- `membership_grant_activation_periods_check`: `CHECK (((ended_at IS NULL) OR (ended_at >= started_at)))`

**Foreign keys:**

- `fk_membership_grant_activation_periods__grant_user`: `FOREIGN KEY (membership_grant_id, user_id) REFERENCES membership_grants(id, user_id) ON DELETE RESTRICT`
- `membership_grant_activation_periods_membership_grant_id_fkey`: `FOREIGN KEY (membership_grant_id) REFERENCES membership_grants(id) ON DELETE RESTRICT`

**Indexes:**

- `idx_membership_grant_activation_periods__grant_id`: `CREATE INDEX idx_membership_grant_activation_periods__grant_id ON public.membership_grant_activation_periods USING btree (membership_grant_id, id DESC)`
- `idx_membership_grant_activation_periods__open_grant`: `CREATE UNIQUE INDEX idx_membership_grant_activation_periods__open_grant ON public.membership_grant_activation_periods USING btree (membership_grant_id) WHERE (ended_at IS NULL)`
- `idx_membership_grant_activation_periods__open_user`: `CREATE UNIQUE INDEX idx_membership_grant_activation_periods__open_user ON public.membership_grant_activation_periods USING btree (user_id) WHERE (ended_at IS NULL)`
- `membership_grant_activation_periods_pkey`: `CREATE UNIQUE INDEX membership_grant_activation_periods_pkey ON public.membership_grant_activation_periods USING btree (id)`

**Triggers:**

- `trigger_membership_grant_activation_periods_guard`: `CREATE TRIGGER trigger_membership_grant_activation_periods_guard BEFORE DELETE OR UPDATE ON public.membership_grant_activation_periods FOR EACH ROW EXECUTE FUNCTION fn_guard_membership_grant_activation_period_mutation()`
