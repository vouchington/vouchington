# Table `referral_program_link_validations`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Named validation rule sets that can be attached to referral programs to verify submitted URLs.

Not partitioned — growth: unbounded.

| Column           | Type                       | Nullable | Default                      | Identity | Generated | Collation | Comment                                                           |
| ---------------- | -------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | ----------------------------------------------------------------- |
| `id`             | `uuid`                     | no       | `uuidv7()`                   |          |           |           |                                                                   |
| `slug`           | `text`                     | no       |                              |          |           |           | Unique lowercase identifier for this validation rule set.         |
| `user_help_text` | `text`                     | no       | `''::text`                   |          |           |           | Help text shown to the user when they are adding a referral link. |
| `created_at`     | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                                   |
| `updated_at`     | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP`          |          |           |           |                                                                   |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**

- `referral_program_link_validations_slug_key`: `UNIQUE (slug)`

**Check constraints:**

- `referral_program_link_validations_slug_check`: `CHECK ((char_length(slug) <= 255))`
- `referral_program_link_validations_slug_check1`: `CHECK ((slug = lower(slug)))`
- `referral_program_link_validations_slug_check2`: `CHECK ((slug ~ '^[a-z0-9_]+$'::text))`

**Foreign keys:**
_none_

**Indexes:**

- `idx_referral_program_link_validations__slug_prefix`: `CREATE INDEX idx_referral_program_link_validations__slug_prefix ON public.referral_program_link_validations USING btree (slug text_pattern_ops)`
- `referral_program_link_validations_pkey`: `CREATE UNIQUE INDEX referral_program_link_validations_pkey ON public.referral_program_link_validations USING btree (id)`
- `referral_program_link_validations_slug_key`: `CREATE UNIQUE INDEX referral_program_link_validations_slug_key ON public.referral_program_link_validations USING btree (slug)`

**Triggers:**

- `trigger_referral_program_link_validations_updated_at`: `CREATE TRIGGER trigger_referral_program_link_validations_updated_at BEFORE UPDATE ON public.referral_program_link_validations FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
