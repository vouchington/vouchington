# Table `user_permission_types`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Lookup table of granular permission types that can be granted to roles or users.

Not partitioned — growth: bounded.

| Column       | Type                       | Nullable | Default             | Identity | Generated | Collation | Comment                                               |
| ------------ | -------------------------- | -------- | ------------------- | -------- | --------- | --------- | ----------------------------------------------------- |
| `id`         | `bigint`                   | no       |                     | always   |           |           |                                                       |
| `slug`       | `text`                     | no       |                     |          |           |           | Unique lowercase identifier for this permission type. |
| `created_at` | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                       |
| `updated_at` | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                       |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**

- `user_permission_types_slug_key`: `UNIQUE (slug)`

**Check constraints:**

- `user_permission_types_slug_check`: `CHECK ((slug = lower(slug)))`
- `user_permission_types_slug_check1`: `CHECK ((char_length(slug) <= 255))`
- `user_permission_types_slug_check2`: `CHECK ((slug ~ '^[a-z0-9_]+$'::text))`

**Foreign keys:**
_none_

**Indexes:**

- `user_permission_types_pkey`: `CREATE UNIQUE INDEX user_permission_types_pkey ON public.user_permission_types USING btree (id)`
- `user_permission_types_slug_key`: `CREATE UNIQUE INDEX user_permission_types_slug_key ON public.user_permission_types USING btree (slug)`

**Triggers:**

- `trigger_user_permission_types_updated_at`: `CREATE TRIGGER trigger_user_permission_types_updated_at BEFORE UPDATE ON public.user_permission_types FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
