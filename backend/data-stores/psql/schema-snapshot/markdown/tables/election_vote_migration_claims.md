# Table `election_vote_migration_claims`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Durable claims that make one-shot election vote migrations converge without rescanning vote tables.

Not partitioned — growth: bounded.

| Column         | Type                       | Nullable | Default             | Identity | Generated | Collation | Comment                                                                          |
| -------------- | -------------------------- | -------- | ------------------- | -------- | --------- | --------- | -------------------------------------------------------------------------------- |
| `migration_id` | `text`                     | no       |                     |          |           |           | Stable identifier for the one-shot election vote migration that owns this claim. |
| `claimed_at`   | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           | Time the migration claim was inserted; claim rows are immutable.                 |

**Primary key:** `PRIMARY KEY (migration_id)`

**Unique constraints:**
_none_

**Check constraints:**

- `election_vote_migration_claims_migration_id_check`: `CHECK (((migration_id = TRIM(BOTH FROM migration_id)) AND ((char_length(migration_id) >= 1) AND (char_length(migration_id) <= 255))))`

**Foreign keys:**
_none_

**Indexes:**

- `election_vote_migration_claims_pkey`: `CREATE UNIQUE INDEX election_vote_migration_claims_pkey ON public.election_vote_migration_claims USING btree (migration_id)`

**Triggers:**
_none_
