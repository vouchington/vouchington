# Table `topics__spending_categories`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Extension table for spending category topics (e.g. Restaurants, Gas, Travel).

Not partitioned — growth: unbounded.

| Column                       | Type                       | Nullable | Default                           | Identity | Generated | Collation | Comment                                                                |
| ---------------------------- | -------------------------- | -------- | --------------------------------- | -------- | --------- | --------- | ---------------------------------------------------------------------- |
| `topic_id`                   | `uuid`                     | no       |                                   |          |           |           | The topic that is a spending category (PK, 1:1 with topics).           |
| `is_foreign_transaction`     | `boolean`                  | no       | `false`                           |          |           |           | Whether this category applies to foreign transactions.                 |
| `default_spending_frequency` | `spending_frequencies`     | no       | `'monthly'::spending_frequencies` |          |           |           | Default frequency for spending in this category (monthly or annually). |
| `created_at`                 | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP`               |          |           |           |                                                                        |
| `updated_at`                 | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP`               |          |           |           |                                                                        |

**Primary key:** `PRIMARY KEY (topic_id)`

**Unique constraints:**
_none_

**Check constraints:**
_none_

**Foreign keys:**

- `topics__spending_categories_topic_id_fkey`: `FOREIGN KEY (topic_id) REFERENCES topics(id) ON DELETE CASCADE`

**Indexes:**

- `topics__spending_categories_pkey`: `CREATE UNIQUE INDEX topics__spending_categories_pkey ON public.topics__spending_categories USING btree (topic_id)`

**Triggers:**

- `trigger_topics__spending_categories_updated_at`: `CREATE TRIGGER trigger_topics__spending_categories_updated_at BEFORE UPDATE ON public.topics__spending_categories FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
