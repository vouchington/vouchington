# Table `post_moderation_versions`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Immutable moderation generations keyed by canonical post content and policy revision.

Not partitioned — growth: unbounded.

| Column            | Type                       | Nullable | Default                                      | Identity | Generated | Collation | Comment                                                                                            |
| ----------------- | -------------------------- | -------- | -------------------------------------------- | -------- | --------- | --------- | -------------------------------------------------------------------------------------------------- |
| `id`              | `uuid`                     | no       | `uuidv7()`                                   |          |           |           |                                                                                                    |
| `post_id`         | `uuid`                     | no       |                                              |          |           |           | Post whose canonical content version this moderation generation evaluates.                         |
| `content_sha256`  | `bytea`                    | no       |                                              |          |           |           | SHA-256 digest of the exact post content evaluated by this version.                                |
| `policy_revision` | `text`                     | no       |                                              |          |           |           | Stable moderation-policy revision applied to this content version.                                 |
| `deadline_at`     | `timestamp with time zone` | no       | `(CURRENT_TIMESTAMP + '00:30:00'::interval)` |          |           |           | Hard recovery deadline; unresolved work becomes staff review rather than publication or rejection. |
| `created_at`      | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)`                 |          | virtual   |           |                                                                                                    |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**

- `post_moderation_versions_post_id_content_sha256_policy_revi_key`: `UNIQUE (post_id, content_sha256, policy_revision)`

**Check constraints:**

- `post_moderation_versions_content_sha256_check`: `CHECK ((octet_length(content_sha256) = 32))`
- `post_moderation_versions_policy_revision_check`: `CHECK (((char_length(policy_revision) >= 1) AND (char_length(policy_revision) <= 100)))`

**Foreign keys:**

- `post_moderation_versions_post_id_fkey`: `FOREIGN KEY (post_id) REFERENCES posts(id) ON DELETE CASCADE`

**Indexes:**

- `idx_post_moderation_versions__post_id__id`: `CREATE INDEX idx_post_moderation_versions__post_id__id ON public.post_moderation_versions USING btree (post_id, id DESC)`
- `post_moderation_versions_pkey`: `CREATE UNIQUE INDEX post_moderation_versions_pkey ON public.post_moderation_versions USING btree (id)`
- `post_moderation_versions_post_id_content_sha256_policy_revi_key`: `CREATE UNIQUE INDEX post_moderation_versions_post_id_content_sha256_policy_revi_key ON public.post_moderation_versions USING btree (post_id, content_sha256, policy_revision)`

**Triggers:**
_none_
