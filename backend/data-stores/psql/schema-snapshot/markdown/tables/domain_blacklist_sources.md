# Table `domain_blacklist_sources`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

External sources of domain blacklists (e.g. spam lists, disposable email lists).

Not partitioned — growth: bounded.

| Column             | Type                       | Nullable | Default             | Identity | Generated | Collation | Comment                                                  |
| ------------------ | -------------------------- | -------- | ------------------- | -------- | --------- | --------- | -------------------------------------------------------- |
| `id`               | `bigint`                   | no       |                     | always   |           |           |                                                          |
| `type`             | `domain_blacklist_types`   | no       |                     |          |           |           | Whether this source blocks URL domains or email domains. |
| `name`             | `text`                     | no       |                     |          |           |           | Unique lowercase identifier for this blacklist source.   |
| `url`              | `text`                     | no       |                     |          |           |           | URL where the blacklist can be fetched from.             |
| `etag`             | `text`                     | yes      |                     |          |           |           | HTTP ETag from the last fetch, for conditional requests. |
| `last_modified_at` | `timestamp with time zone` | yes      |                     |          |           |           | HTTP Last-Modified from the last fetch.                  |
| `last_fetched_at`  | `timestamp with time zone` | yes      |                     |          |           |           | When this source was last successfully fetched.          |
| `created_at`       | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                          |
| `updated_at`       | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                          |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**

- `domain_blacklist_sources_name_key`: `UNIQUE (name)`

**Check constraints:**

- `domain_blacklist_sources_name_check`: `CHECK ((name ~ '^[a-z-]+$'::text))`
- `domain_blacklist_sources_name_check1`: `CHECK ((name = lower(name)))`
- `domain_blacklist_sources_name_check2`: `CHECK ((name = TRIM(BOTH FROM name)))`
- `domain_blacklist_sources_name_check3`: `CHECK ((char_length(name) <= 255))`

**Foreign keys:**
_none_

**Indexes:**

- `domain_blacklist_sources_name_key`: `CREATE UNIQUE INDEX domain_blacklist_sources_name_key ON public.domain_blacklist_sources USING btree (name)`
- `domain_blacklist_sources_pkey`: `CREATE UNIQUE INDEX domain_blacklist_sources_pkey ON public.domain_blacklist_sources USING btree (id)`

**Triggers:**

- `trigger_domain_blacklist_sources_updated_at`: `CREATE TRIGGER trigger_domain_blacklist_sources_updated_at BEFORE UPDATE ON public.domain_blacklist_sources FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
