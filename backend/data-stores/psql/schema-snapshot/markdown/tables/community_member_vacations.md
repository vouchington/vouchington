# Table `community_member_vacations`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Self-service vacation flag for community moderators. Active while starts_at <= now() AND (ends_at IS NULL OR ends_at > now()).

Not partitioned — growth: unbounded.

| Column         | Type                       | Nullable | Default             | Identity | Generated | Collation | Comment                                                                                      |
| -------------- | -------------------------- | -------- | ------------------- | -------- | --------- | --------- | -------------------------------------------------------------------------------------------- |
| `community_id` | `uuid`                     | no       |                     |          |           |           | The community this vacation applies to.                                                      |
| `user_id`      | `uuid`                     | no       |                     |          |           |           | The moderator on vacation.                                                                   |
| `starts_at`    | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           | When this vacation period began. Reset to now() on each upsert (re-enable resets the clock). |
| `ends_at`      | `timestamp with time zone` | yes      |                     |          |           |           | When this vacation ends. NULL means indefinite (until explicitly cleared).                   |
| `created_at`   | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                                                              |
| `updated_at`   | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                                                              |

**Primary key:** `PRIMARY KEY (community_id, user_id)`

**Unique constraints:**
_none_

**Check constraints:**
_none_

**Foreign keys:**

- `community_member_vacations_community_id_fkey`: `FOREIGN KEY (community_id) REFERENCES communities(id) ON DELETE CASCADE`
- `community_member_vacations_user_id_fkey`: `FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE`

**Indexes:**

- `community_member_vacations_pkey`: `CREATE UNIQUE INDEX community_member_vacations_pkey ON public.community_member_vacations USING btree (community_id, user_id)`
- `idx_community_member_vacations__user_id`: `CREATE INDEX idx_community_member_vacations__user_id ON public.community_member_vacations USING btree (user_id) WHERE (user_id IS NOT NULL)`

**Triggers:**
_none_
