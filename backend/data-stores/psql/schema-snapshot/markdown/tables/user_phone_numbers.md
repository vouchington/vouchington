# Table `user_phone_numbers`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Phone numbers associated with user accounts. Composite PK on (user_id, phone_number).

Not partitioned — growth: unbounded.

| Column         | Type                       | Nullable | Default             | Identity | Generated | Collation | Comment                                          |
| -------------- | -------------------------- | -------- | ------------------- | -------- | --------- | --------- | ------------------------------------------------ |
| `user_id`      | `uuid`                     | no       |                     |          |           |           | The user who owns this phone number.             |
| `phone_number` | `text`                     | no       |                     |          |           |           | Normalized (lowercase, trimmed) phone number.    |
| `created_at`   | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                  |
| `updated_at`   | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                  |
| `is_primary`   | `boolean`                  | yes      | `true`              |          |           |           | Whether this is the user's primary phone number. |

**Primary key:** `PRIMARY KEY (user_id, phone_number)`

**Unique constraints:**
_none_

**Check constraints:**

- `user_phone_numbers_phone_number_check`: `CHECK ((char_length(phone_number) <= 255))`
- `user_phone_numbers_phone_number_check1`: `CHECK ((phone_number = lower(phone_number)))`
- `user_phone_numbers_phone_number_check2`: `CHECK ((phone_number = TRIM(BOTH FROM phone_number)))`

**Foreign keys:**

- `user_phone_numbers_user_id_fkey`: `FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE`

**Indexes:**

- `idx_user_phone_numbers__phone`: `CREATE INDEX idx_user_phone_numbers__phone ON public.user_phone_numbers USING btree (phone_number)`
- `idx_user_phone_numbers_phone_primary`: `CREATE UNIQUE INDEX idx_user_phone_numbers_phone_primary ON public.user_phone_numbers USING btree (phone_number) WHERE (is_primary = true)`
- `idx_user_phone_numbers_user_primary`: `CREATE UNIQUE INDEX idx_user_phone_numbers_user_primary ON public.user_phone_numbers USING btree (user_id) WHERE (is_primary = true)`
- `user_phone_numbers_pkey`: `CREATE UNIQUE INDEX user_phone_numbers_pkey ON public.user_phone_numbers USING btree (user_id, phone_number)`

**Triggers:**

- `trigger_user_phone_numbers_updated_at`: `CREATE TRIGGER trigger_user_phone_numbers_updated_at BEFORE UPDATE ON public.user_phone_numbers FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
