# Table `topics__fediverse_instances`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Extension table for topics of type fediverse_instance. NodeInfo-derived classification plus admin allowlist state for a federated server.

Not partitioned — growth: unbounded.

| Column                      | Type                             | Nullable | Default                                     | Identity | Generated | Collation | Comment                                                                                                               |
| --------------------------- | -------------------------------- | -------- | ------------------------------------------- | -------- | --------- | --------- | --------------------------------------------------------------------------------------------------------------------- |
| `topic_id`                  | `uuid`                           | no       |                                             |          |           |           | The topic that is a fediverse instance (PK, 1:1 with topics).                                                         |
| `software`                  | `text`                           | yes      |                                             |          |           |           | NodeInfo software.name (e.g. mastodon, lemmy, peertube). NULL until classified.                                       |
| `protocol`                  | `text`                           | yes      |                                             |          |           |           | Primary federation protocol reported by NodeInfo (e.g. activitypub). NULL until classified.                           |
| `nodeinfo_software_version` | `text`                           | yes      |                                             |          |           |           | NodeInfo software.version string. NULL until classified.                                                              |
| `total_users`               | `integer`                        | yes      |                                             |          |           |           | NodeInfo usage.users.total. NULL until classified or unreported.                                                      |
| `monthly_active_users`      | `integer`                        | yes      |                                             |          |           |           | NodeInfo usage.users.activeMonth. NULL until classified or unreported.                                                |
| `open_registrations`        | `boolean`                        | yes      |                                             |          |           |           | NodeInfo openRegistrations flag. NULL until classified.                                                               |
| `nodeinfo_raw`              | `jsonb`                          | yes      |                                             |          |           |           | Full NodeInfo 2.0 document as fetched, for fields not individually modeled.                                           |
| `integration_status`        | `fediverse_integration_statuses` | no       | `'pending'::fediverse_integration_statuses` |          |           |           | Admin allowlist decision (pending, approved, blocked). Trigger-maintained from an append-only decision history table. |
| `created_at`                | `timestamp with time zone`       | no       | `CURRENT_TIMESTAMP`                         |          |           |           |                                                                                                                       |
| `updated_at`                | `timestamp with time zone`       | no       | `CURRENT_TIMESTAMP`                         |          |           |           |                                                                                                                       |

**Primary key:** `PRIMARY KEY (topic_id)`

**Unique constraints:**
_none_

**Check constraints:**
_none_

**Foreign keys:**

- `topics__fediverse_instances_topic_id_fkey`: `FOREIGN KEY (topic_id) REFERENCES topics(id) ON DELETE CASCADE`

**Indexes:**

- `topics__fediverse_instances_pkey`: `CREATE UNIQUE INDEX topics__fediverse_instances_pkey ON public.topics__fediverse_instances USING btree (topic_id)`

**Triggers:**

- `trigger_topics__fediverse_instances_updated_at`: `CREATE TRIGGER trigger_topics__fediverse_instances_updated_at BEFORE UPDATE ON public.topics__fediverse_instances FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
