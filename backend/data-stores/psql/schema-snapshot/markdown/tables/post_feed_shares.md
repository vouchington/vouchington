# Table `post_feed_shares`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Records of posts shared to another user's feed, range-partitioned by recipient UUIDv7.

RANGE partitioned on `recipient_user_id` (children: default, no retention owner, access class: target-scoped, growth: unbounded).

| Column              | Type                       | Nullable | Default                      | Identity | Generated | Collation | Comment                                                                       |
| ------------------- | -------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | ----------------------------------------------------------------------------- |
| `recipient_user_id` | `uuid`                     | no       |                              |          |           |           | The user who receives this shared post in their feed; also the partition key. |
| `id`                | `uuid`                     | no       | `uuidv7()`                   |          |           |           |                                                                               |
| `shared_by_user_id` | `uuid`                     | no       |                              |          |           |           | The user who shared the post.                                                 |
| `post_id`           | `uuid`                     | no       |                              |          |           |           | The post being shared.                                                        |
| `sort_at`           | `timestamp with time zone` | no       |                              |          |           |           | Timestamp used for sorting this share in the recipient's feed.                |
| `created_at`        | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                                               |
| `updated_at`        | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP`          |          |           |           |                                                                               |

**Primary key:** `PRIMARY KEY (recipient_user_id, id)`

**Unique constraints:**
_none_

**Check constraints:**
_none_

**Foreign keys:**

- `post_feed_shares_post_id_fkey`: `FOREIGN KEY (post_id) REFERENCES posts(id) ON DELETE CASCADE`
- `post_feed_shares_recipient_user_id_fkey`: `FOREIGN KEY (recipient_user_id) REFERENCES users(id) ON DELETE CASCADE`
- `post_feed_shares_shared_by_user_id_fkey`: `FOREIGN KEY (shared_by_user_id) REFERENCES users(id) ON DELETE CASCADE`

**Indexes:**

- `idx_post_feed_shares__post_id`: `CREATE INDEX idx_post_feed_shares__post_id ON ONLY public.post_feed_shares USING btree (post_id)`
- `idx_post_feed_shares__recipient__sort`: `CREATE INDEX idx_post_feed_shares__recipient__sort ON ONLY public.post_feed_shares USING btree (recipient_user_id, sort_at DESC, id DESC)`
- `idx_post_feed_shares__sharer__post__id_desc`: `CREATE INDEX idx_post_feed_shares__sharer__post__id_desc ON ONLY public.post_feed_shares USING btree (shared_by_user_id, post_id, id DESC)`
- `post_feed_shares_pkey`: `CREATE UNIQUE INDEX post_feed_shares_pkey ON ONLY public.post_feed_shares USING btree (recipient_user_id, id)`

**Triggers:**

- `trigger_post_feed_shares_updated_at`: `CREATE TRIGGER trigger_post_feed_shares_updated_at BEFORE UPDATE ON public.post_feed_shares FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
