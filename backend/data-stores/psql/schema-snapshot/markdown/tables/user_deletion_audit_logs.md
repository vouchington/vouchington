# Table `user_deletion_audit_logs`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Compliance audit trail for account deletions; no FK so records survive after user hard-delete.

Not partitioned — growth: unbounded.

| Column            | Type                       | Nullable | Default                      | Identity | Generated | Collation | Comment                                                                         |
| ----------------- | -------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | ------------------------------------------------------------------------------- |
| `id`              | `uuid`                     | no       | `uuidv7()`                   |          |           |           |                                                                                 |
| `user_id`         | `uuid`                     | no       |                              |          |           |           | The user whose account was deleted (no FK, intentional for audit persistence).  |
| `requested_by_id` | `uuid`                     | yes      |                              |          |           |           | The user who initiated the deletion (no FK, intentional for audit persistence). |
| `created_at`      | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                                                 |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**
_none_

**Check constraints:**
_none_

**Foreign keys:**
_none_

**Indexes:**

- `idx_user_deletion_audit_logs__user_id`: `CREATE INDEX idx_user_deletion_audit_logs__user_id ON public.user_deletion_audit_logs USING btree (user_id)`
- `user_deletion_audit_logs_pkey`: `CREATE UNIQUE INDEX user_deletion_audit_logs_pkey ON public.user_deletion_audit_logs USING btree (id)`

**Triggers:**
_none_
