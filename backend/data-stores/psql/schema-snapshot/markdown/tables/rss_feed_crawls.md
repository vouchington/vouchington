# Table `rss_feed_crawls`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Append-only crawl history per RSS feed. Monthly RANGE-partitioned by id for retention.

RANGE partitioned on `id` (children: monthly, retention owner `cleanupPartitions`, access class: retention-window, growth: unbounded).

| Column             | Type                       | Nullable | Default                      | Identity | Generated | Collation | Comment                                                           |
| ------------------ | -------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | ----------------------------------------------------------------- |
| `id`               | `uuid`                     | no       | `uuidv7()`                   |          |           |           |                                                                   |
| `rss_feed_id`      | `uuid`                     | no       |                              |          |           |           | The feed that was crawled.                                        |
| `response_code`    | `smallint`                 | no       |                              |          |           |           | HTTP response status code from the crawl.                         |
| `feed_data`        | `jsonb`                    | yes      |                              |          |           |           | Raw JSONB feed payload, if the response was successful.           |
| `feed_data_sha256` | `bytea`                    | yes      |                              |          |           |           | SHA-256 of the feed payload, for change detection.                |
| `redirect_url_id`  | `uuid`                     | yes      |                              |          |           |           | The URL this crawl redirected to, if a 3xx redirect was received. |
| `created_at`       | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                                   |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**
_none_

**Check constraints:**

- `rss_feed_crawls_check`: `CHECK ((id > rss_feed_id))`
- `rss_feed_crawls_feed_data_sha256_check`: `CHECK (((feed_data_sha256 IS NULL) OR (octet_length(feed_data_sha256) = 32)))`

**Foreign keys:**

- `rss_feed_crawls_redirect_url_id_fkey`: `FOREIGN KEY (redirect_url_id) REFERENCES urls(id) ON DELETE SET NULL`
- `rss_feed_crawls_rss_feed_id_fkey`: `FOREIGN KEY (rss_feed_id) REFERENCES rss_feeds(id) ON DELETE CASCADE`

**Indexes:**

- `idx_rss_feed_crawls__redirect_url_id`: `CREATE INDEX idx_rss_feed_crawls__redirect_url_id ON ONLY public.rss_feed_crawls USING btree (redirect_url_id) WHERE (redirect_url_id IS NOT NULL)`
- `idx_rss_feed_crawls__rss_feed_id`: `CREATE INDEX idx_rss_feed_crawls__rss_feed_id ON ONLY public.rss_feed_crawls USING btree (rss_feed_id, id DESC)`
- `rss_feed_crawls_pkey`: `CREATE UNIQUE INDEX rss_feed_crawls_pkey ON ONLY public.rss_feed_crawls USING btree (id)`

**Triggers:**
_none_
