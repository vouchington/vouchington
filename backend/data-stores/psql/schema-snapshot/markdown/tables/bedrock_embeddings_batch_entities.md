# Table `bedrock_embeddings_batch_entities`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Lock table tracking which entities are currently being processed in a Bedrock embeddings batch.

Not partitioned — growth: unbounded.

| Column              | Type                                | Nullable | Default             | Identity | Generated | Collation | Comment                                                                                   |
| ------------------- | ----------------------------------- | -------- | ------------------- | -------- | --------- | --------- | ----------------------------------------------------------------------------------------- |
| `batch_id`          | `text`                              | no       |                     |          |           |           | The batch job processing this entity.                                                     |
| `entity_type`       | `bedrock_embedding_batch_job_types` | no       |                     |          |           |           | The entity type being processed.                                                          |
| `topic_id`          | `uuid`                              | yes      |                     |          |           |           | Topic currently being processed in a Bedrock embeddings batch.                            |
| `post_id`           | `uuid`                              | yes      |                     |          |           |           | Post currently being processed in a Bedrock embeddings batch.                             |
| `rss_feed_item_id`  | `uuid`                              | yes      |                     |          |           |           | RSS feed item currently being processed in a Bedrock embeddings batch.                    |
| `crawl_id`          | `uuid`                              | yes      |                     |          |           |           | Crawl containing the crawl chunk currently being processed in a Bedrock embeddings batch. |
| `crawl_order_index` | `integer`                           | yes      |                     |          |           |           | Order index of the crawl chunk currently being processed in a Bedrock embeddings batch.   |
| `image_id`          | `uuid`                              | yes      |                     |          |           |           | Image currently being processed in a Bedrock embeddings batch.                            |
| `created_at`        | `timestamp with time zone`          | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                                                           |
| `updated_at`        | `timestamp with time zone`          | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                                                           |

**Primary key:** _none_

**Unique constraints:**
_none_

**Check constraints:**

- `chk_bedrock_embeddings_batch_entities__entity_columns`: `CHECK ((((entity_type = 'topics'::bedrock_embedding_batch_job_types) AND (topic_id IS NOT NULL) AND (post_id IS NULL) AND (rss_feed_item_id IS NULL) AND (crawl_id IS NULL) AND (crawl_order_index IS NULL) AND (image_id IS NULL)) OR ((entity_type = 'posts'::bedrock_embedding_batch_job_types) AND (topic_id IS NULL) AND (post_id IS NOT NULL) AND (rss_feed_item_id IS NULL) AND (crawl_id IS NULL) AND (crawl_order_index IS NULL) AND (image_id IS NULL)) OR ((entity_type = 'rss_feed_items'::bedrock_embedding_batch_job_types) AND (topic_id IS NULL) AND (post_id IS NULL) AND (rss_feed_item_id IS NOT NULL) AND (crawl_id IS NULL) AND (crawl_order_index IS NULL) AND (image_id IS NULL)) OR ((entity_type = 'crawl_chunks'::bedrock_embedding_batch_job_types) AND (topic_id IS NULL) AND (post_id IS NULL) AND (rss_feed_item_id IS NULL) AND (crawl_id IS NOT NULL) AND (crawl_order_index IS NOT NULL) AND (image_id IS NULL)) OR ((entity_type = 'images'::bedrock_embedding_batch_job_types) AND (topic_id IS NULL) AND (post_id IS NULL) AND (rss_feed_item_id IS NULL) AND (crawl_id IS NULL) AND (crawl_order_index IS NULL) AND (image_id IS NOT NULL))))`

**Foreign keys:**

- `bedrock_embeddings_batch_entities_batch_id_fkey`: `FOREIGN KEY (batch_id) REFERENCES bedrock_embeddings_batches(id) ON DELETE CASCADE`
- `bedrock_embeddings_batch_entities_image_id_fkey`: `FOREIGN KEY (image_id) REFERENCES images(id) ON DELETE CASCADE`
- `bedrock_embeddings_batch_entities_post_id_fkey`: `FOREIGN KEY (post_id) REFERENCES posts(id) ON DELETE CASCADE`
- `bedrock_embeddings_batch_entities_rss_feed_item_id_fkey`: `FOREIGN KEY (rss_feed_item_id) REFERENCES rss_feed_items(id) ON DELETE CASCADE`
- `bedrock_embeddings_batch_entities_topic_id_fkey`: `FOREIGN KEY (topic_id) REFERENCES topics(id) ON DELETE CASCADE`
- `fk_bedrock_embeddings_batch_entities__crawl_chunk`: `FOREIGN KEY (crawl_id, crawl_order_index) REFERENCES crawl_chunks(crawl_id, order_index) ON DELETE CASCADE`

**Indexes:**

- `idx_bedrock_embeddings_batch_entities__batch_id`: `CREATE INDEX idx_bedrock_embeddings_batch_entities__batch_id ON public.bedrock_embeddings_batch_entities USING btree (batch_id)`
- `idx_bedrock_embeddings_batch_entities__crawl_chunk`: `CREATE UNIQUE INDEX idx_bedrock_embeddings_batch_entities__crawl_chunk ON public.bedrock_embeddings_batch_entities USING btree (crawl_id, crawl_order_index) WHERE ((crawl_id IS NOT NULL) AND (crawl_order_index IS NOT NULL))`
- `idx_bedrock_embeddings_batch_entities__crawl_id`: `CREATE INDEX idx_bedrock_embeddings_batch_entities__crawl_id ON public.bedrock_embeddings_batch_entities USING btree (crawl_id) WHERE (crawl_id IS NOT NULL)`
- `idx_bedrock_embeddings_batch_entities__image`: `CREATE UNIQUE INDEX idx_bedrock_embeddings_batch_entities__image ON public.bedrock_embeddings_batch_entities USING btree (image_id) WHERE (image_id IS NOT NULL)`
- `idx_bedrock_embeddings_batch_entities__post`: `CREATE UNIQUE INDEX idx_bedrock_embeddings_batch_entities__post ON public.bedrock_embeddings_batch_entities USING btree (post_id) WHERE (post_id IS NOT NULL)`
- `idx_bedrock_embeddings_batch_entities__rss_feed_item`: `CREATE UNIQUE INDEX idx_bedrock_embeddings_batch_entities__rss_feed_item ON public.bedrock_embeddings_batch_entities USING btree (rss_feed_item_id) WHERE (rss_feed_item_id IS NOT NULL)`
- `idx_bedrock_embeddings_batch_entities__topic`: `CREATE UNIQUE INDEX idx_bedrock_embeddings_batch_entities__topic ON public.bedrock_embeddings_batch_entities USING btree (topic_id) WHERE (topic_id IS NOT NULL)`

**Triggers:**

- `trigger_bedrock_embeddings_batch_entities_updated_at`: `CREATE TRIGGER trigger_bedrock_embeddings_batch_entities_updated_at BEFORE UPDATE ON public.bedrock_embeddings_batch_entities FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
