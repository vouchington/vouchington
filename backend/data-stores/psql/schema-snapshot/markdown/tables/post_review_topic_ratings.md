# Table `post_review_topic_ratings`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Individual topic ratings within review posts. A review can rate multiple topics.

RANGE partitioned on `post_id` (children: default, no retention owner, access class: target-scoped, growth: unbounded).

| Column        | Type                       | Nullable | Default             | Identity | Generated | Collation | Comment                                                   |
| ------------- | -------------------------- | -------- | ------------------- | -------- | --------- | --------- | --------------------------------------------------------- |
| `post_id`     | `uuid`                     | no       |                     |          |           |           | The review post this rating belongs to.                   |
| `topic_id`    | `uuid`                     | no       |                     |          |           |           | The topic being rated.                                    |
| `rating`      | `smallint`                 | no       |                     |          |           |           | Rating value from 1 to 5 stars.                           |
| `order_index` | `integer`                  | no       | `0`                 |          |           |           | Display order of this rating within the review (0-based). |
| `created_at`  | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                           |
| `updated_at`  | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                           |

**Primary key:** `PRIMARY KEY (post_id, topic_id)`

**Unique constraints:**
_none_

**Check constraints:**

- `post_review_topic_ratings_order_index_check`: `CHECK ((order_index >= 0))`
- `post_review_topic_ratings_rating_check`: `CHECK (((rating >= 1) AND (rating <= 5)))`

**Foreign keys:**

- `post_review_topic_ratings_post_id_fkey`: `FOREIGN KEY (post_id) REFERENCES posts(id) ON DELETE CASCADE`
- `post_review_topic_ratings_topic_id_fkey`: `FOREIGN KEY (topic_id) REFERENCES topics(id) ON DELETE CASCADE`

**Indexes:**

- `idx_post_review_topic_ratings__topic_id`: `CREATE INDEX idx_post_review_topic_ratings__topic_id ON ONLY public.post_review_topic_ratings USING btree (topic_id) INCLUDE (post_id, rating)`
- `post_review_topic_ratings_pkey`: `CREATE UNIQUE INDEX post_review_topic_ratings_pkey ON ONLY public.post_review_topic_ratings USING btree (post_id, topic_id)`

**Triggers:**

- `trigger_post_review_topic_ratings_updated_at`: `CREATE TRIGGER trigger_post_review_topic_ratings_updated_at BEFORE UPDATE ON public.post_review_topic_ratings FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
