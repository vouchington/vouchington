# Table `user_topic_import_attempts`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Bounded actor-owned exact response replays for topic-import batches; completed and abandoned attempts expire after 48 hours.

Not partitioned — growth: bounded.

| Column                 | Type                       | Nullable | Default                          | Identity | Generated | Collation | Comment                                                                                   |
| ---------------------- | -------------------------- | -------- | -------------------------------- | -------- | --------- | --------- | ----------------------------------------------------------------------------------------- |
| `id`                   | `uuid`                     | no       | `uuidv7()`                       |          |           |           |                                                                                           |
| `user_id`              | `uuid`                     | no       |                                  |          |           |           | Authenticated user who owns this idempotency key and exact import replay.                 |
| `idempotency_key`      | `uuid`                     | no       |                                  |          |           |           | Client-supplied UUID that identifies one exact ordered topic-import intent for this user. |
| `intent_sha256`        | `text`                     | no       |                                  |          |           |           | SHA-256 of the canonical ordered import batch; reuse with different input is rejected.    |
| `response`             | `jsonb`                    | yes      |                                  |          |           |           | Exact ordered successful response returned by the completed import batch.                 |
| `created_at`           | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)`     |          | virtual   |           |                                                                                           |
| `updated_at`           | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP`              |          |           |           |                                                                                           |
| `completed_at`         | `timestamp with time zone` | yes      |                                  |          |           |           | Clock timestamp at which audit, follow, and exact replay state committed atomically.      |
| `retention_expires_at` | `timestamp with time zone` | no       | `(now() + '48:00:00'::interval)` |          |           |           | Indexed deletion boundary for completed replays and abandoned pending attempts.           |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**

- `user_topic_import_attempts_user_key_unique`: `UNIQUE (user_id, idempotency_key)`

**Check constraints:**

- `user_topic_import_attempts_intent_sha256_check`: `CHECK (((char_length(intent_sha256) = 64) AND (intent_sha256 = lower(intent_sha256))))`
- `user_topic_import_attempts_response_check`: `CHECK ((((response IS NULL) AND (completed_at IS NULL)) OR ((response IS NOT NULL) AND (completed_at IS NOT NULL) AND (jsonb_typeof(response) = 'array'::text) AND (octet_length((response)::text) <= 4194304))))`

**Foreign keys:**

- `user_topic_import_attempts_user_id_fkey`: `FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE`

**Indexes:**

- `idx_user_topic_import_attempts__retention`: `CREATE INDEX idx_user_topic_import_attempts__retention ON public.user_topic_import_attempts USING btree (retention_expires_at, id)`
- `user_topic_import_attempts_pkey`: `CREATE UNIQUE INDEX user_topic_import_attempts_pkey ON public.user_topic_import_attempts USING btree (id)`
- `user_topic_import_attempts_user_key_unique`: `CREATE UNIQUE INDEX user_topic_import_attempts_user_key_unique ON public.user_topic_import_attempts USING btree (user_id, idempotency_key)`

**Triggers:**

- `trigger_user_topic_import_attempts_updated_at`: `CREATE TRIGGER trigger_user_topic_import_attempts_updated_at BEFORE UPDATE ON public.user_topic_import_attempts FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
