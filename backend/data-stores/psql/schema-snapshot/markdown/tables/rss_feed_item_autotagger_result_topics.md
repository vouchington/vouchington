# Table `rss_feed_item_autotagger_result_topics`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Topics suggested by the autotagger for a given RSS feed item result.

Not partitioned — growth: unbounded.

| Column                               | Type                       | Nullable | Default             | Identity | Generated | Collation | Comment                                                       |
| ------------------------------------ | -------------------------- | -------- | ------------------- | -------- | --------- | --------- | ------------------------------------------------------------- |
| `rss_feed_item_autotagger_result_id` | `uuid`                     | no       |                     |          |           |           | The autotagger result this topic belongs to.                  |
| `topic_id`                           | `uuid`                     | no       |                     |          |           |           | The topic suggested by the autotagger.                        |
| `topic_order`                        | `integer`                  | no       |                     |          |           |           | Relevance order of this topic suggestion (0 = most relevant). |
| `created_at`                         | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                               |
| `updated_at`                         | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                               |

**Primary key:** `PRIMARY KEY (rss_feed_item_autotagger_result_id, topic_id)`

**Unique constraints:**
_none_

**Check constraints:**
_none_

**Foreign keys:**

- `rss_feed_item_autotagger_resu_rss_feed_item_autotagger_res_fkey`: `FOREIGN KEY (rss_feed_item_autotagger_result_id) REFERENCES rss_feed_item_autotagger_results(id) ON DELETE CASCADE`
- `rss_feed_item_autotagger_result_topics_topic_id_fkey`: `FOREIGN KEY (topic_id) REFERENCES topics(id) ON DELETE CASCADE`

**Indexes:**

- `idx_rss_feed_item_autotagger_result_topics__topic_id`: `CREATE INDEX idx_rss_feed_item_autotagger_result_topics__topic_id ON public.rss_feed_item_autotagger_result_topics USING btree (topic_id)`
- `rss_feed_item_autotagger_result_topics_pkey`: `CREATE UNIQUE INDEX rss_feed_item_autotagger_result_topics_pkey ON public.rss_feed_item_autotagger_result_topics USING btree (rss_feed_item_autotagger_result_id, topic_id)`

**Triggers:**
_none_
