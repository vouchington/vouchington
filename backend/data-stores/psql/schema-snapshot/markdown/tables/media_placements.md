# Table `media_placements`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Media-neutral, durable hosted-use placements. Delivery availability is revision-fenced and never inferred from an asset alone.

Not partitioned — growth: bounded.

| Column                  | Type                       | Nullable | Default                      | Identity | Generated | Collation | Comment                                                                                                                               |
| ----------------------- | -------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| `id`                    | `uuid`                     | no       | `uuidv7()`                   |          |           |           |                                                                                                                                       |
| `placement_kind`        | `text`                     | no       |                              |          |           |           | Typed binding kind. Additional media kinds receive their own immutable binding tables.                                                |
| `revision`              | `integer`                  | no       | `0`                          |          |           |           | Monotonic delivery revision; every availability change advances it exactly once.                                                      |
| `retired_at`            | `timestamp with time zone` | yes      |                              |          |           |           | Placement is no longer attached to its host surface; retained so stale routes fail closed.                                            |
| `retirement_reason`     | `text`                     | yes      |                              |          |           |           | Why the placement retired. Owner removal supersedes an asset retirement, fencing image-delete rollback from restoring a detached use. |
| `copyright_withheld_at` | `timestamp with time zone` | yes      |                              |          |           |           | Placement-specific copyright withholding state; shared source assets remain recoverable.                                              |
| `created_at`            | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                                                                                                       |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**
_none_

**Check constraints:**

- `media_placements_check`: `CHECK ((((retired_at IS NULL) AND (retirement_reason IS NULL)) OR ((retired_at IS NOT NULL) AND (retirement_reason IS NOT NULL))))`
- `media_placements_placement_kind_check`: `CHECK ((placement_kind = 'image'::text))`
- `media_placements_retirement_reason_check`: `CHECK ((retirement_reason = ANY (ARRAY['asset_deleted'::text, 'owner_removed'::text])))`
- `media_placements_revision_check`: `CHECK ((revision >= 0))`

**Foreign keys:**
_none_

**Indexes:**

- `media_placements_pkey`: `CREATE UNIQUE INDEX media_placements_pkey ON public.media_placements USING btree (id)`

**Triggers:**

- `trigger_media_placement_guard`: `CREATE TRIGGER trigger_media_placement_guard BEFORE DELETE OR UPDATE ON public.media_placements FOR EACH ROW EXECUTE FUNCTION fn_guard_media_placement()`
