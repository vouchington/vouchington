# Table `bedrock_nova_multimodal_v1_embeddings`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Centralized lookup table for Amazon Nova multimodal text vectors, keyed by content hash.

Not partitioned — growth: unbounded.

| Column              | Type                       | Nullable | Default             | Identity | Generated | Collation | Comment                                                                                                                          |
| ------------------- | -------------------------- | -------- | ------------------- | -------- | --------- | --------- | -------------------------------------------------------------------------------------------------------------------------------- |
| `content_sha256`    | `bytea`                    | no       |                     |          |           |           | SHA-256 of the input content. Primary key for deduplication.                                                                     |
| `embedding`         | `vector(1024)`             | no       |                     |          |           |           | 1024-dimensional vector from Amazon Nova 2 Multimodal Embeddings V1.                                                             |
| `input_token_count` | `integer`                  | yes      |                     |          |           |           | Number of tokens Bedrock consumed for this content. NULL until first embedded; populated on insert and backfilled on cache hits. |
| `created_at`        | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                                                                                                  |
| `updated_at`        | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                                                                                                  |

**Primary key:** `PRIMARY KEY (content_sha256)`

**Unique constraints:**
_none_

**Check constraints:**

- `bedrock_nova_multimodal_v1_embeddings_content_sha256_check`: `CHECK ((octet_length(content_sha256) = 32))`
- `bedrock_nova_multimodal_v1_embeddings_input_token_count_check`: `CHECK (((input_token_count IS NULL) OR (input_token_count >= 0)))`

**Foreign keys:**
_none_

**Indexes:**

- `bedrock_nova_multimodal_v1_embeddings_pkey`: `CREATE UNIQUE INDEX bedrock_nova_multimodal_v1_embeddings_pkey ON public.bedrock_nova_multimodal_v1_embeddings USING btree (content_sha256)`

**Triggers:**

- `trigger_bedrock_nova_multimodal_v1_embeddings_updated_at`: `CREATE TRIGGER trigger_bedrock_nova_multimodal_v1_embeddings_updated_at BEFORE UPDATE ON public.bedrock_nova_multimodal_v1_embeddings FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
