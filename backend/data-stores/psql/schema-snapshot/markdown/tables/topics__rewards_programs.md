# Table `topics__rewards_programs`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Extension table for topics of type rewards_program. Links program to its parent company.

Not partitioned — growth: unbounded.

| Column       | Type                       | Nullable | Default             | Identity | Generated | Collation | Comment                                                    |
| ------------ | -------------------------- | -------- | ------------------- | -------- | --------- | --------- | ---------------------------------------------------------- |
| `topic_id`   | `uuid`                     | no       |                     |          |           |           | The topic that is a rewards program (PK, 1:1 with topics). |
| `company_id` | `uuid`                     | yes      |                     |          |           |           | The company topic that operates this rewards program.      |
| `created_at` | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                            |
| `updated_at` | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                            |

**Primary key:** `PRIMARY KEY (topic_id)`

**Unique constraints:**
_none_

**Check constraints:**
_none_

**Foreign keys:**

- `topics__rewards_programs_company_id_fkey`: `FOREIGN KEY (company_id) REFERENCES topics(id) ON DELETE CASCADE`
- `topics__rewards_programs_topic_id_fkey`: `FOREIGN KEY (topic_id) REFERENCES topics(id) ON DELETE CASCADE`

**Indexes:**

- `idx_topics__rewards_programs__company_id`: `CREATE INDEX idx_topics__rewards_programs__company_id ON public.topics__rewards_programs USING btree (company_id) WHERE (company_id IS NOT NULL)`
- `topics__rewards_programs_pkey`: `CREATE UNIQUE INDEX topics__rewards_programs_pkey ON public.topics__rewards_programs USING btree (topic_id)`

**Triggers:**

- `trigger_topics__rewards_programs_updated_at`: `CREATE TRIGGER trigger_topics__rewards_programs_updated_at BEFORE UPDATE ON public.topics__rewards_programs FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
