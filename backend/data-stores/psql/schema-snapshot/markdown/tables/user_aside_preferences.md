# Table `user_aside_preferences`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Tracks which sidebar asides a user has dismissed.

Not partitioned — growth: unbounded.

| Column         | Type                       | Nullable | Default                      | Identity | Generated | Collation | Comment                                                               |
| -------------- | -------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | --------------------------------------------------------------------- |
| `id`           | `uuid`                     | no       | `uuidv7()`                   |          |           |           |                                                                       |
| `user_id`      | `uuid`                     | no       |                              |          |           |           | The user who dismissed the aside.                                     |
| `aside_key`    | `text`                     | no       |                              |          |           |           | Stable string key identifying the aside panel (e.g. trending-topics). |
| `dismissed_at` | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP`          |          |           |           | When the user last dismissed this aside.                              |
| `created_at`   | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                                       |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**

- `uc_user_aside_preferences__user_aside`: `UNIQUE (user_id, aside_key)`

**Check constraints:**
_none_

**Foreign keys:**

- `user_aside_preferences_user_id_fkey`: `FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE`

**Indexes:**

- `uc_user_aside_preferences__user_aside`: `CREATE UNIQUE INDEX uc_user_aside_preferences__user_aside ON public.user_aside_preferences USING btree (user_id, aside_key)`
- `user_aside_preferences_pkey`: `CREATE UNIQUE INDEX user_aside_preferences_pkey ON public.user_aside_preferences USING btree (id)`

**Triggers:**
_none_
