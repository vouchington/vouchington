# Table `post__stories`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Junction table linking story posts to stories. One post per story.

Not partitioned — growth: unbounded.

| Column            | Type                       | Nullable | Default             | Identity | Generated | Collation | Comment                                         |
| ----------------- | -------------------------- | -------- | ------------------- | -------- | --------- | --------- | ----------------------------------------------- |
| `post_id`         | `uuid`                     | no       |                     |          |           |           | FK to the story post.                           |
| `story_id`        | `uuid`                     | no       |                     |          |           |           | FK to the story being discussed.                |
| `initiated_by_id` | `uuid`                     | no       |                     |          |           |           | The user who requested the story post creation. |
| `created_at`      | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                 |
| `updated_at`      | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                 |

**Primary key:** `PRIMARY KEY (post_id)`

**Unique constraints:**
_none_

**Check constraints:**
_none_

**Foreign keys:**

- `post__stories_initiated_by_id_fkey`: `FOREIGN KEY (initiated_by_id) REFERENCES users(id) ON DELETE CASCADE`
- `post__stories_post_id_fkey`: `FOREIGN KEY (post_id) REFERENCES posts(id) ON DELETE CASCADE`
- `post__stories_story_id_fkey`: `FOREIGN KEY (story_id) REFERENCES stories(id) ON DELETE CASCADE`

**Indexes:**

- `idx_post__stories__initiated_by_id`: `CREATE INDEX idx_post__stories__initiated_by_id ON public.post__stories USING btree (initiated_by_id) WHERE (initiated_by_id IS NOT NULL)`
- `idx_post__stories__story_id`: `CREATE UNIQUE INDEX idx_post__stories__story_id ON public.post__stories USING btree (story_id)`
- `post__stories_pkey`: `CREATE UNIQUE INDEX post__stories_pkey ON public.post__stories USING btree (post_id)`

**Triggers:**

- `trigger_post__stories_updated_at`: `CREATE TRIGGER trigger_post__stories_updated_at BEFORE UPDATE ON public.post__stories FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
