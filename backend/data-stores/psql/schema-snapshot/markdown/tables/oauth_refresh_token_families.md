# Table `oauth_refresh_token_families`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Rotating refresh-token family lifecycle and reuse detection.

Not partitioned — growth: unbounded.

| Column              | Type                       | Nullable | Default                      | Identity | Generated | Collation | Comment                                                        |
| ------------------- | -------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | -------------------------------------------------------------- |
| `id`                | `uuid`                     | no       | `uuidv7()`                   |          |           |           |                                                                |
| `grant_id`          | `uuid`                     | no       |                              |          |           |           | Delegated grant represented by the rotating family.            |
| `resource`          | `text`                     | no       |                              |          |           |           | Protected resource audience bound to the family.               |
| `scopes`            | `text[]`                   | no       |                              |          |           |           | Maximum canonical scope set available to family members.       |
| `expires_at`        | `timestamp with time zone` | no       |                              |          |           |           | Absolute expiry shared by every family member.                 |
| `revoked_at`        | `timestamp with time zone` | yes      |                              |          |           |           | Time at which the complete token family was revoked.           |
| `reuse_detected_at` | `timestamp with time zone` | yes      |                              |          |           |           | Time at which replay of a consumed family member was detected. |
| `created_at`        | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                                |
| `updated_at`        | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP`          |          |           |           |                                                                |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**

- `oauth_refresh_token_families_id_grant_id_key`: `UNIQUE (id, grant_id)`

**Check constraints:**

- `oauth_refresh_token_families_check`: `CHECK (((reuse_detected_at IS NULL) OR (revoked_at IS NOT NULL)))`
- `oauth_refresh_token_families_resource_check`: `CHECK (((char_length(resource) >= 1) AND (char_length(resource) <= 2048)))`
- `oauth_refresh_token_families_scopes_check`: `CHECK ((cardinality(scopes) > 0))`

**Foreign keys:**

- `oauth_refresh_token_families_grant_id_fkey`: `FOREIGN KEY (grant_id) REFERENCES oauth_grants(id) ON DELETE CASCADE`

**Indexes:**

- `idx_oauth_refresh_token_families__expiry`: `CREATE INDEX idx_oauth_refresh_token_families__expiry ON public.oauth_refresh_token_families USING btree (expires_at, id)`
- `idx_oauth_refresh_token_families__grant`: `CREATE INDEX idx_oauth_refresh_token_families__grant ON public.oauth_refresh_token_families USING btree (grant_id, id)`
- `oauth_refresh_token_families_id_grant_id_key`: `CREATE UNIQUE INDEX oauth_refresh_token_families_id_grant_id_key ON public.oauth_refresh_token_families USING btree (id, grant_id)`
- `oauth_refresh_token_families_pkey`: `CREATE UNIQUE INDEX oauth_refresh_token_families_pkey ON public.oauth_refresh_token_families USING btree (id)`

**Triggers:**

- `trigger_oauth_refresh_token_families_updated_at`: `CREATE TRIGGER trigger_oauth_refresh_token_families_updated_at BEFORE UPDATE ON public.oauth_refresh_token_families FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
