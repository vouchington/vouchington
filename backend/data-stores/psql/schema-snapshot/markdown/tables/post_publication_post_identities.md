# Table `post_publication_post_identities`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Durable post identity referenced by accepted receipts and snapshots; UUIDv7 range partitions bound target-scoped access and bounded sweeps reclaim unreferenced rows.

RANGE partitioned on `id` (children: default, no retention owner, access class: target-scoped, growth: unbounded).

| Column       | Type                       | Nullable | Default                      | Identity | Generated | Collation | Comment                                                                                |
| ------------ | -------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | -------------------------------------------------------------------------------------- |
| `id`         | `uuid`                     | no       |                              |          |           |           |                                                                                        |
| `post_id`    | `uuid`                     | yes      |                              |          |           |           | Live entity FK; deletion clears this link without deleting historical repair identity. |
| `created_at` | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                                                        |
| `updated_at` | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP`          |          |           |           |                                                                                        |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**
_none_

**Check constraints:**

- `post_publication_post_identities_check`: `CHECK (((post_id IS NULL) OR (post_id = id)))`

**Foreign keys:**

- `post_publication_post_identities_post_id_fkey`: `FOREIGN KEY (post_id) REFERENCES posts(id) ON DELETE SET NULL`

**Indexes:**

- `idx_post_publication_post_identities__post_id`: `CREATE INDEX idx_post_publication_post_identities__post_id ON ONLY public.post_publication_post_identities USING btree (post_id) WHERE (post_id IS NOT NULL)`
- `post_publication_post_identities_pkey`: `CREATE UNIQUE INDEX post_publication_post_identities_pkey ON ONLY public.post_publication_post_identities USING btree (id)`

**Triggers:**

- `trigger_pub_post_identities_updated_at`: `CREATE TRIGGER trigger_pub_post_identities_updated_at BEFORE UPDATE ON public.post_publication_post_identities FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
