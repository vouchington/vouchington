# Table `post_publication_identity_cleanup_progress`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Bounded cyclic snapshot-header sweep, independent from the shared protocol writer lock.

Not partitioned — growth: bounded.

| Column               | Type                       | Nullable | Default             | Identity | Generated | Collation | Comment                                                            |
| -------------------- | -------------------------- | -------- | ------------------- | -------- | --------- | --------- | ------------------------------------------------------------------ |
| `singleton`          | `boolean`                  | no       | `true`              |          |           |           | Checked singleton key serializes only cleanup sweeps.              |
| `cursor_snapshot_id` | `uuid`                     | yes      |                     |          |           |           | Last examined header; no FK because reclaimed headers are deleted. |
| `updated_at`         | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           | Last committed sweep progress.                                     |

**Primary key:** `PRIMARY KEY (singleton)`

**Unique constraints:**
_none_

**Check constraints:**

- `post_publication_identity_cleanup_progress_singleton_check`: `CHECK (singleton)`

**Foreign keys:**
_none_

**Indexes:**

- `post_publication_identity_cleanup_progress_pkey`: `CREATE UNIQUE INDEX post_publication_identity_cleanup_progress_pkey ON public.post_publication_identity_cleanup_progress USING btree (singleton)`

**Triggers:**
_none_
