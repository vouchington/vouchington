# Table `topics__cards`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Extension table for topics of type card (credit/debit cards).

Not partitioned — growth: unbounded.

| Column                   | Type                       | Nullable | Default             | Identity | Generated | Collation | Comment                                                    |
| ------------------------ | -------------------------- | -------- | ------------------- | -------- | --------- | --------- | ---------------------------------------------------------- |
| `topic_id`               | `uuid`                     | no       |                     |          |           |           | The topic that is a card (PK, 1:1 with topics).            |
| `bank_id`                | `uuid`                     | yes      |                     |          |           |           | The issuing bank topic (e.g. Chase, Citi).                 |
| `brand_id`               | `uuid`                     | yes      |                     |          |           |           | The co-brand topic (e.g. Marriott, Hilton).                |
| `created_at`             | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                            |
| `updated_at`             | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                            |
| `annual_fee_minor_units` | `bigint`                   | yes      |                     |          |           |           | Annual fee in the currency minor unit. NULL if unknown.    |
| `currency_code`          | `text`                     | yes      |                     |          |           |           | Currency for the annual fee. NULL when the fee is unknown. |

**Primary key:** `PRIMARY KEY (topic_id)`

**Unique constraints:**
_none_

**Check constraints:**

- `topics__cards_annual_fee_minor_units_check`: `CHECK (((annual_fee_minor_units >= 0) AND (annual_fee_minor_units <= '9007199254740991'::bigint)))`
- `topics__cards_check`: `CHECK (((annual_fee_minor_units IS NULL) = (currency_code IS NULL)))`

**Foreign keys:**

- `topics__cards_bank_id_fkey`: `FOREIGN KEY (bank_id) REFERENCES topics(id) ON DELETE CASCADE`
- `topics__cards_brand_id_fkey`: `FOREIGN KEY (brand_id) REFERENCES topics(id) ON DELETE CASCADE`
- `topics__cards_currency_code_fkey`: `FOREIGN KEY (currency_code) REFERENCES currencies(code) ON DELETE RESTRICT`
- `topics__cards_topic_id_fkey`: `FOREIGN KEY (topic_id) REFERENCES topics(id) ON DELETE CASCADE`

**Indexes:**

- `idx_topics__cards__bank_id`: `CREATE INDEX idx_topics__cards__bank_id ON public.topics__cards USING btree (bank_id) WHERE (bank_id IS NOT NULL)`
- `idx_topics__cards__brand_id`: `CREATE INDEX idx_topics__cards__brand_id ON public.topics__cards USING btree (brand_id) WHERE (brand_id IS NOT NULL)`
- `idx_topics__cards__currency_code`: `CREATE INDEX idx_topics__cards__currency_code ON public.topics__cards USING btree (currency_code) WHERE (currency_code IS NOT NULL)`
- `topics__cards_pkey`: `CREATE UNIQUE INDEX topics__cards_pkey ON public.topics__cards USING btree (topic_id)`

**Triggers:**

- `trigger_topics__cards_updated_at`: `CREATE TRIGGER trigger_topics__cards_updated_at BEFORE UPDATE ON public.topics__cards FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
