# Table `membership_sources`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Stable entitlement sources that project provider or administrator access onto a user.

Not partitioned — growth: unbounded.

| Column                           | Type                       | Nullable | Default                      | Identity | Generated | Collation | Comment                                                             |
| -------------------------------- | -------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | ------------------------------------------------------------------- |
| `id`                             | `uuid`                     | no       | `uuidv7()`                   |          |           |           |                                                                     |
| `user_id`                        | `uuid`                     | yes      |                              |          |           |           | Current user receiving access; detached during final account purge. |
| `source_kind`                    | `membership_source_kinds`  | no       |                              |          |           |           | Origin category of the entitlement source.                          |
| `membership_provider_lineage_id` | `uuid`                     | yes      |                              |          |           |           | Provider lineage for non-admin sources.                             |
| `created_at`                     | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                                     |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**
_none_

**Check constraints:**

- `membership_sources_check`: `CHECK (((source_kind = 'admin_grant'::membership_source_kinds) = (membership_provider_lineage_id IS NULL)))`

**Foreign keys:**

- `membership_sources_membership_provider_lineage_id_fkey`: `FOREIGN KEY (membership_provider_lineage_id) REFERENCES membership_provider_lineages(id) ON DELETE RESTRICT`

**Indexes:**

- `idx_membership_sources__current_family_lineage_user`: `CREATE UNIQUE INDEX idx_membership_sources__current_family_lineage_user ON public.membership_sources USING btree (membership_provider_lineage_id, user_id) WHERE ((membership_provider_lineage_id IS NOT NULL) AND (user_id IS NOT NULL) AND (source_kind = 'family'::membership_source_kinds))`
- `idx_membership_sources__direct_lineage`: `CREATE UNIQUE INDEX idx_membership_sources__direct_lineage ON public.membership_sources USING btree (membership_provider_lineage_id) WHERE ((membership_provider_lineage_id IS NOT NULL) AND (source_kind = 'direct'::membership_source_kinds))`
- `idx_membership_sources__id_lineage_id`: `CREATE UNIQUE INDEX idx_membership_sources__id_lineage_id ON public.membership_sources USING btree (id, membership_provider_lineage_id)`
- `idx_membership_sources__id_source_kind`: `CREATE UNIQUE INDEX idx_membership_sources__id_source_kind ON public.membership_sources USING btree (id, source_kind)`
- `idx_membership_sources__id_user_id`: `CREATE UNIQUE INDEX idx_membership_sources__id_user_id ON public.membership_sources USING btree (id, user_id)`
- `idx_membership_sources__lineage_id`: `CREATE INDEX idx_membership_sources__lineage_id ON public.membership_sources USING btree (membership_provider_lineage_id) WHERE (membership_provider_lineage_id IS NOT NULL)`
- `idx_membership_sources__user_id`: `CREATE INDEX idx_membership_sources__user_id ON public.membership_sources USING btree (user_id, id DESC)`
- `membership_sources_pkey`: `CREATE UNIQUE INDEX membership_sources_pkey ON public.membership_sources USING btree (id)`

**Triggers:**
_none_
