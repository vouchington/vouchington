# Table `post_explicit_topic_categories`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Explicit topic categories authored for a post, independent of relation votes and structured data-point topics.

RANGE partitioned on `post_id` (children: default, no retention owner, access class: target-scoped, growth: unbounded).

| Column       | Type                       | Nullable | Default             | Identity | Generated | Collation | Comment                                    |
| ------------ | -------------------------- | -------- | ------------------- | -------- | --------- | --------- | ------------------------------------------ |
| `post_id`    | `uuid`                     | no       |                     |          |           |           | The post with the explicit topic category. |
| `topic_id`   | `uuid`                     | no       |                     |          |           |           | The explicitly selected topic.             |
| `created_at` | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                            |
| `updated_at` | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                            |

**Primary key:** `PRIMARY KEY (post_id, topic_id)`

**Unique constraints:**
_none_

**Check constraints:**
_none_

**Foreign keys:**

- `post_explicit_topic_categories_post_id_fkey`: `FOREIGN KEY (post_id) REFERENCES posts(id) ON DELETE CASCADE`
- `post_explicit_topic_categories_topic_id_fkey`: `FOREIGN KEY (topic_id) REFERENCES topics(id) ON DELETE CASCADE`

**Indexes:**

- `idx_post_explicit_topic_categories__topic_id`: `CREATE INDEX idx_post_explicit_topic_categories__topic_id ON ONLY public.post_explicit_topic_categories USING btree (topic_id) INCLUDE (post_id)`
- `post_explicit_topic_categories_pkey`: `CREATE UNIQUE INDEX post_explicit_topic_categories_pkey ON ONLY public.post_explicit_topic_categories USING btree (post_id, topic_id)`

**Triggers:**

- `trigger_post_explicit_topic_categories_updated_at`: `CREATE TRIGGER trigger_post_explicit_topic_categories_updated_at BEFORE UPDATE ON public.post_explicit_topic_categories FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
