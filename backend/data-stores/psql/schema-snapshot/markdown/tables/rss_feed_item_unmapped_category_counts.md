# Table `rss_feed_item_unmapped_category_counts`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Transactionally refreshed counts of normalized RSS item category strings whose topic_id is NULL.

Not partitioned — growth: unbounded.

| Column          | Type                       | Nullable | Default             | Identity | Generated | Collation | Comment                                                             |
| --------------- | -------------------------- | -------- | ------------------- | -------- | --------- | --------- | ------------------------------------------------------------------- |
| `category_text` | `text`                     | no       |                     |          |           |           | Lowercase RSS category text with no current topic mapping.          |
| `item_count`    | `bigint`                   | no       |                     |          |           |           | Number of RSS feed items currently carrying this unmapped category. |
| `updated_at`    | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           | When this category count last changed.                              |

**Primary key:** `PRIMARY KEY (category_text)`

**Unique constraints:**
_none_

**Check constraints:**

- `rss_feed_item_unmapped_category_counts_item_count_check`: `CHECK ((item_count > 0))`

**Foreign keys:**
_none_

**Indexes:**

- `rss_feed_item_unmapped_category_counts_pkey`: `CREATE UNIQUE INDEX rss_feed_item_unmapped_category_counts_pkey ON public.rss_feed_item_unmapped_category_counts USING btree (category_text)`

**Triggers:**
_none_
