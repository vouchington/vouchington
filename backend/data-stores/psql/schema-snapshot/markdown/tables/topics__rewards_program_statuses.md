# Table `topics__rewards_program_statuses`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Extension table for topics of type rewards_program_status (e.g. Marriott Platinum Elite).

Not partitioned — growth: unbounded.

| Column                | Type                       | Nullable | Default             | Identity | Generated | Collation | Comment                                                                        |
| --------------------- | -------------------------- | -------- | ------------------- | -------- | --------- | --------- | ------------------------------------------------------------------------------ |
| `topic_id`            | `uuid`                     | no       |                     |          |           |           | The topic that is a rewards program status (PK, 1:1 with topics).              |
| `lifetime_version_id` | `uuid`                     | yes      |                     |          |           |           | Points to the lifetime version of this status (e.g. Hyatt Lifetime Globalist). |
| `order_index`         | `smallint`                 | no       | `0`                 |          |           |           | Sort order: lowest tier = 0, ascending for higher tiers.                       |
| `created_at`          | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                                                |
| `updated_at`          | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                                                |

**Primary key:** `PRIMARY KEY (topic_id)`

**Unique constraints:**
_none_

**Check constraints:**
_none_

**Foreign keys:**

- `topics__rewards_program_statuses_lifetime_version_id_fkey`: `FOREIGN KEY (lifetime_version_id) REFERENCES topics__rewards_program_statuses(topic_id) ON DELETE CASCADE`
- `topics__rewards_program_statuses_topic_id_fkey`: `FOREIGN KEY (topic_id) REFERENCES topics(id) ON DELETE CASCADE`

**Indexes:**

- `idx_topics__rewards_program_statuses__lifetime_version_id`: `CREATE UNIQUE INDEX idx_topics__rewards_program_statuses__lifetime_version_id ON public.topics__rewards_program_statuses USING btree (lifetime_version_id) WHERE (lifetime_version_id IS NOT NULL)`
- `topics__rewards_program_statuses_pkey`: `CREATE UNIQUE INDEX topics__rewards_program_statuses_pkey ON public.topics__rewards_program_statuses USING btree (topic_id)`

**Triggers:**

- `trigger_topics__rewards_program_statuses_updated_at`: `CREATE TRIGGER trigger_topics__rewards_program_statuses_updated_at BEFORE UPDATE ON public.topics__rewards_program_statuses FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
