# Table `app_attestation_keys`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Apple App Attest keys, one row per device-generated attestation key, used to verify subsequent per-request assertions.

Not partitioned — growth: unbounded.

| Column         | Type                           | Nullable | Default                      | Identity | Generated | Collation | Comment                                                                                                                                                                                                                                                                                                                                                                     |
| -------------- | ------------------------------ | -------- | ---------------------------- | -------- | --------- | --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `id`           | `uuid`                         | no       | `uuidv7()`                   |          |           |           |                                                                                                                                                                                                                                                                                                                                                                             |
| `key_id`       | `bytea`                        | no       |                              |          |           |           | Apple App Attest key identifier: raw 32-byte SHA-256 of the attested public key, as returned by DCAppAttestService.                                                                                                                                                                                                                                                         |
| `did`          | `uuid`                         | no       |                              |          |           |           | The dt device-id claim bound to this key at attest time. There is no devices table (did is an ephemeral JWT claim, not a durable identity) so this is a plain UUID, not a foreign key. Assertion verification rejects a valid signature if the caller's current did does not match, so one attested device cannot be used as a signing oracle for another device's session. |
| `public_key`   | `bytea`                        | no       |                              |          |           |           | Attested EC P-256 public key (DER, SPKI format).                                                                                                                                                                                                                                                                                                                            |
| `sign_count`   | `bigint`                       | no       | `0`                          |          |           |           | Highest assertion sign count verified for this key. Assertions must present a strictly greater sign count.                                                                                                                                                                                                                                                                  |
| `bundle_id`    | `text`                         | no       |                              |          |           |           | App bundle identifier the key was attested for.                                                                                                                                                                                                                                                                                                                             |
| `environment`  | `app_attestation_environments` | no       |                              |          |           |           | Apple App Attest environment (production or development) the key was attested under. Development-environment keys are rejected at assertion time once development attestation is disabled.                                                                                                                                                                                  |
| `created_at`   | `timestamp with time zone`     | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                                                                                                                                                                                                                                                                                                                                             |
| `last_used_at` | `timestamp with time zone`     | yes      |                              |          |           |           | Timestamp of the most recently verified assertion for this key.                                                                                                                                                                                                                                                                                                             |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**

- `app_attestation_keys_key_id_key`: `UNIQUE (key_id)`

**Check constraints:**

- `app_attestation_keys_key_id_check`: `CHECK ((length(key_id) = 32))`

**Foreign keys:**
_none_

**Indexes:**

- `app_attestation_keys_key_id_key`: `CREATE UNIQUE INDEX app_attestation_keys_key_id_key ON public.app_attestation_keys USING btree (key_id)`
- `app_attestation_keys_pkey`: `CREATE UNIQUE INDEX app_attestation_keys_pkey ON public.app_attestation_keys USING btree (id)`

**Triggers:**
_none_
