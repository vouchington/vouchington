# Table `membership_entitlement_effects`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Durable at-least-once delivery intents for derived membership entitlement consumers.

Not partitioned — growth: unbounded.

| Column                 | Type                       | Nullable | Default                      | Identity | Generated | Collation | Comment                                                        |
| ---------------------- | -------------------------- | -------- | ---------------------------- | -------- | --------- | --------- | -------------------------------------------------------------- |
| `id`                   | `uuid`                     | no       | `uuidv7()`                   |          |           |           |                                                                |
| `membership_change_id` | `uuid`                     | no       |                              |          |           |           | Immutable membership change that created this delivery intent. |
| `user_id`              | `uuid`                     | no       |                              |          |           |           | User whose derived entitlement consumers must be refreshed.    |
| `delivered_at`         | `timestamp with time zone` | yes      |                              |          |           |           | Time both idempotent downstream entitlement effects completed. |
| `delivery_claim_token` | `text`                     | yes      |                              |          |           |           | Opaque lease token held by the current delivery attempt.       |
| `delivery_claimed_at`  | `timestamp with time zone` | yes      |                              |          |           |           | Time the current delivery lease was acquired.                  |
| `updated_at`           | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP`          |          |           |           |                                                                |
| `created_at`           | `timestamp with time zone` | yes      | `uuid_extract_timestamp(id)` |          | virtual   |           |                                                                |

**Primary key:** `PRIMARY KEY (id)`

**Unique constraints:**
_none_

**Check constraints:**

- `membership_entitlement_effects_check`: `CHECK (((delivery_claim_token IS NULL) = (delivery_claimed_at IS NULL)))`
- `membership_entitlement_effects_delivery_claim_token_check`: `CHECK (((delivery_claim_token IS NULL) OR (char_length(delivery_claim_token) = 36)))`

**Foreign keys:**

- `membership_entitlement_effects_membership_change_id_fkey`: `FOREIGN KEY (membership_change_id) REFERENCES membership_changes(id) ON DELETE RESTRICT`
- `membership_entitlement_effects_user_id_fkey`: `FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE`

**Indexes:**

- `idx_membership_entitlement_effects__change_id`: `CREATE UNIQUE INDEX idx_membership_entitlement_effects__change_id ON public.membership_entitlement_effects USING btree (membership_change_id)`
- `idx_membership_entitlement_effects__pending`: `CREATE INDEX idx_membership_entitlement_effects__pending ON public.membership_entitlement_effects USING btree (id) WHERE (delivered_at IS NULL)`
- `idx_membership_entitlement_effects__user_id`: `CREATE INDEX idx_membership_entitlement_effects__user_id ON public.membership_entitlement_effects USING btree (user_id)`
- `membership_entitlement_effects_pkey`: `CREATE UNIQUE INDEX membership_entitlement_effects_pkey ON public.membership_entitlement_effects USING btree (id)`

**Triggers:**

- `trigger_membership_entitlement_effects_updated_at`: `CREATE TRIGGER trigger_membership_entitlement_effects_updated_at BEFORE UPDATE ON public.membership_entitlement_effects FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
