# Table `media_delivery_repair_markers`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

FK-free committed repair intent for a successful pre-commit edge denial; drained by the existing media delivery reconciler.

Not partitioned — growth: bounded.

| Column               | Type                       | Nullable | Default             | Identity | Generated | Collation | Comment                                                                                             |
| -------------------- | -------------------------- | -------- | ------------------- | -------- | --------- | --------- | --------------------------------------------------------------------------------------------------- |
| `delivery_key`       | `text`                     | no       |                     |          |           |           | Exact immutable delivery URL identity; coalesces outstanding repair work.                           |
| `marker_token`       | `bigint`                   | no       |                     |          |           |           | Monotonic wakeup identity allocated on coalescing; acknowledgement deletes only its observed token. |
| `route_kind`         | `text`                     | no       |                     |          |           |           | Typed placement or legacy-image URL namespace.                                                      |
| `placement_id`       | `uuid`                     | yes      |                     |          |           |           | Placement snapshot without an FK so repair survives owner rollback and never-committed placements.  |
| `placement_revision` | `integer`                  | yes      |                     |          |           |           | Exact immutable URL revision captured before denial.                                                |
| `asset_id`           | `uuid`                     | no       |                     |          |           |           | Image snapshot without an FK so repair survives owner rollback and never-committed images.          |
| `created_at`         | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                                                                     |
| `updated_at`         | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                                                                                     |

**Primary key:** `PRIMARY KEY (delivery_key)`

**Unique constraints:**
_none_

**Check constraints:**

- `media_delivery_repair_markers_check`: `CHECK ((((route_kind = 'placement'::text) AND (placement_id IS NOT NULL) AND (placement_revision IS NOT NULL)) OR ((route_kind = 'legacy-image'::text) AND (placement_id IS NULL) AND (placement_revision IS NULL))))`
- `media_delivery_repair_markers_delivery_key_check`: `CHECK (((char_length(delivery_key) >= 1) AND (char_length(delivery_key) <= 512)))`
- `media_delivery_repair_markers_placement_revision_check`: `CHECK (((placement_revision IS NULL) OR (placement_revision >= 0)))`
- `media_delivery_repair_markers_route_kind_check`: `CHECK ((route_kind = ANY (ARRAY['placement'::text, 'legacy-image'::text])))`

**Foreign keys:**
_none_

**Indexes:**

- `idx_media_delivery_repair_markers__oldest`: `CREATE INDEX idx_media_delivery_repair_markers__oldest ON public.media_delivery_repair_markers USING btree (created_at, delivery_key)`
- `media_delivery_repair_markers_pkey`: `CREATE UNIQUE INDEX media_delivery_repair_markers_pkey ON public.media_delivery_repair_markers USING btree (delivery_key)`

**Triggers:**
_none_
