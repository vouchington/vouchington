# Table `topics__retailers`

Generated by `pnpm run db:snapshot:update`. Do not hand-edit. [Schema index](../README.md).

Extension table for topics of type retailer, representing a store or marketplace that sells products.

Not partitioned — growth: unbounded.

| Column       | Type                       | Nullable | Default             | Identity | Generated | Collation | Comment                               |
| ------------ | -------------------------- | -------- | ------------------- | -------- | --------- | --------- | ------------------------------------- |
| `topic_id`   | `uuid`                     | no       |                     |          |           |           | The topic representing this retailer. |
| `created_at` | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                       |
| `updated_at` | `timestamp with time zone` | no       | `CURRENT_TIMESTAMP` |          |           |           |                                       |

**Primary key:** `PRIMARY KEY (topic_id)`

**Unique constraints:**
_none_

**Check constraints:**
_none_

**Foreign keys:**

- `topics__retailers_topic_id_fkey`: `FOREIGN KEY (topic_id) REFERENCES topics(id) ON DELETE CASCADE`

**Indexes:**

- `topics__retailers_pkey`: `CREATE UNIQUE INDEX topics__retailers_pkey ON public.topics__retailers USING btree (topic_id)`

**Triggers:**

- `trigger_topics__retailers_updated_at`: `CREATE TRIGGER trigger_topics__retailers_updated_at BEFORE UPDATE ON public.topics__retailers FOR EACH ROW EXECUTE FUNCTION fn_update_updated_at()`
