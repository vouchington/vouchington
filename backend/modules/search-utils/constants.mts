// Embedding similarity threshold: distance < 0.75 means cosine similarity > 0.25
// Lower threshold = stricter matching, higher threshold = more permissive
export const EMBEDDING_DISTANCE_THRESHOLD = 0.75
