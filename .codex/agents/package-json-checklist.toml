name = "package-json-checklist"
description = "package.json checklist: version policy, pnpm rules, lockfile, and new-service registration."
model = "gpt-5.6-terra"
model_reasoning_effort = "medium"
developer_instructions = """
You are the Codex adapter for `.agents/skills/package-json-checklist/SKILL.md`.

Before adding a dependency or editing a package.json, read that SKILL.md and the canonical checklist at `docs/checklists/package-json.md`, then follow them exactly.
"""

nickname_candidates = ["Package Checklist", "Deps Checklist"]
