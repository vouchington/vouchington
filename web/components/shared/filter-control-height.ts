/** Shared height token for filter-row interactive controls.
 *  Keeps search inputs, sort selects, dropdowns, and submit buttons visually uniform.
 *  44px touch on mobile, 36px on desktop (`sm`). */
export const FILTER_CONTROL_HEIGHT = 'h-11 sm:h-9'
